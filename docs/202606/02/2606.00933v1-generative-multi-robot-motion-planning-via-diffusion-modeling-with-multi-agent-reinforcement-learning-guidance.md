---
title: Generative Multi-Robot Motion Planning via Diffusion Modeling with Multi-Agent Reinforcement Learning Guidance
title_zh: 基于扩散建模与多智能体强化学习引导的生成式多机器人运动规划
authors: "Suk Ki Lee, Venkata Sai Deepak Mutta, Hyunwoong Ko"
date: 2026-05-30
pdf: "https://arxiv.org/pdf/2606.00933v1"
tags: ["query:marl"]
score: 9.0
evidence: 结合扩散模型与多智能体强化学习引导的多机器人运动规划
tldr: "多机器人运动规划面临可扩展性与协调的权衡。本文提出去中心化扩散轨迹生成结合集中式MARL价值引导的方法：每个机器人独立使用扩散模型生成多样化轨迹，并通过价值函数梯度引导反向去噪过程以减少冲突。在四机器人迷宫环境中，该方法将干扰率从55.4%降至41.8%，表明无需联合规划即可实现有效协调，兼具可扩展性与交互感知能力。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.00933v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1949, \"height\": 1663, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00933v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1575, \"height\": 821, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00933v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 948, \"height\": 347, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00933v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 712, \"height\": 692, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00933v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 901, \"height\": 420, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00933v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1830, \"height\": 833, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.00933v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 947, \"height\": 944, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00933v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 775, \"height\": 117, \"label\": \"Table\"}]"
motivation: 现有去中心化规划不考虑交互，集中式方法难以扩展，需一种可扩展的交互感知多机器人规划方法。
method: 每个机器人用扩散模型独立生成轨迹，通过MARL价值函数梯度引导反向扩散过程，实现交互感知的协调。
result: "在四机器人迷宫实验中，干扰率从55.4%降至41.8%。"
conclusion: 证明MARL价值引导可有效引入协调，无需联合模型，保持可扩展性。
---

## 摘要
在共享环境中协调多个机器人需要为每个智能体生成可行轨迹，同时考虑智能体之间的交互。集中式规划方法随着机器人数量增加而难以扩展，而允许每个智能体独立规划的分散式方法本身无法考虑智能体间交互。本文提出了一种协调多机器人运动规划的框架，将分散式生成轨迹规划与基于多智能体强化学习（MARL）的协调相结合。每个机器人使用基于单智能体运动数据训练的扩散模型独立生成候选轨迹，利用生成模型产生可行且多样化轨迹的能力。为减少智能体间冲突，通过基于梯度的引导，使用MARL训练的集中式价值函数指导反向扩散过程，从而实现交互感知的轨迹生成，无需集中式联合规划或重新训练生成模型。该引导遵循指数倾斜公式，其中价值函数将去噪分布偏向于具有更高期望多智能体回报的轨迹。该框架在包含四个移动机器人的模拟迷宫环境中进行了评估。实验结果表明，所提出的价值引导扩散规划将智能体间干扰率从55.4%降低到41.8%，表明在保持分散式轨迹生成可扩展性的同时，能够有效实现协调。这些结果表明，基于MARL的价值引导可以在不需要完全联合多机器人模型的情况下，有效地将协调引入分散式生成规划器。

## Abstract
Coordinating multiple robots in shared environments requires generating feasible trajectories for each agent while accounting for interactions among agents. Centralized planning approaches become difficult to scale as the number of robots increases, while decentralized approaches that allow each agent to plan independently do not inherently account for inter-agent interactions. This paper presents a framework for coordinated multi-robot motion planning that combines decentralized generative trajectory planning with multi-agent reinforcement learning (MARL)-based coordination. Each robot independently generates candidate trajectories using a diffusion model trained on single-agent motion data, leveraging the generative model's ability to produce feasible and diverse trajectories. To reduce conflicts between agents, a centralized value function trained via MARL guides the reverse diffusion process through gradient-based steering, enabling interaction-aware trajectory generation without centralized joint planning or retraining of the generative model. This guidance follows an exponential tilting formulation, in which the value function biases the denoising distribution toward trajectories with higher expected multi-agent return. The framework is evaluated in a simulated maze environment with four mobile robots. Experimental results show that the proposed value-guided diffusion planning reduces the inter-agent interference rate from 55.4% to 41.8%, demonstrating that coordination can be effectively achieved while preserving the scalability of decentralized trajectory generation. These results suggest that MARL-based value guidance can effectively introduce coordination into decentralized generative planners without requiring a fully joint multi-robot model.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 论文的核心问题与整体含义（研究动机和背景）
- 多机器人运动规划面临根本性权衡：集中式规划（如联合优化所有智能体轨迹）能够显式建模交互，但随着机器人数量增加，联合状态空间爆炸，可扩展性差；分散式规划（每个智能体独立生成轨迹）具有良好的可扩展性，但无法天然考虑智能体间的相互作用，可能导致冲突和干扰。
- 现有生成式模型（如扩散模型）为每个智能体独立生成多样、可行的轨迹，但仍缺乏协调机制。因此，需要一种既能保持分散式规划的可扩展性，又能引入交互感知协调的新框架。
- 本文提出将**单智能体扩散模型**与**多智能体强化学习（MARL）的集中式价值函数引导**相结合，在不重新训练生成模型的前提下，通过价值梯度引导反向扩散过程，减少智能体间干扰。

## 论文提出的方法论
### 核心思想
每个机器人使用共享的扩散模型独立生成候选轨迹，然后在推理阶段通过一个由 MARL 训练的集中式价值函数 \(V_\phi(X)\) 的梯度来修正每个去噪步骤的均值，使轨迹偏向于具有更高多智能体联合回报的区域，从而在保持分散式生成的同时实现协调。

### 关键技术细节与流程（四个阶段）
1. **单智能体轨迹分布构建**：在迷宫环境中，使用 PPO 训练一个单机器人导航策略，从不同起点–目标对出发收集 2800 条多样化轨迹，构成数据集 \(\mathcal{D}\)。学习到的策略产生一个随机过程，使高回报轨迹更可能被采样，从而捕获可行的运动分布。
2. **单智能体扩散模型训练**：基于数据集 \(\mathcal{D}\)，训练一个条件扩散模型 \(p_\theta(\tau \mid c)\)（条件为起点/目标 \(c\)）。采用噪声预测参数化，最小化均方误差；模型为 Temporal U-Net 结构，规划视界 176 步，256 个去噪步。
3. **分散式多智能体轨迹生成**：多个智能体共享同一个扩散模型，每个智能体独立采样轨迹 \(\tau^i \sim p_\theta(\tau^i \mid c^i)\)，完全去中心化，复杂度随智能体数量线性增长。
4. **集中式价值函数引导**：
   - 在开放空间中，使用 MAPPO 训练一个集中式价值函数 \(V_\phi(X)\)，其输入为所有智能体的联合观测，输出协调质量的估计。
   - 在推理时，定义引导分布 \(\tilde{p}_\theta(X) \propto p_\theta(X) \exp(\lambda V_\phi(X))\)，其中 \(\lambda\) 为引导尺度。
   - 去噪过程中，每个智能体的均值修正为 \(\mu^i_t + \lambda \Sigma_t \nabla_i\)，其中 \(\nabla_i\) 是 \(V_\phi\) 对第 \(i\) 个智能体轨迹的梯度。这样既保持了扩散模型的可行性先验，又通过价值梯度引入了协调信息。
- 算法流程见 Algorithm 1：初始化轨迹为噪声，施加边界条件；从 \(t=T\) 到 1，计算每个智能体的去噪均值，然后计算联合梯度，按修正均值采样，再施加边界条件。

## 实验设计
### 环境与场景
- **模拟器**：NVIDIA Isaac Sim 5.1.0 + Isaac Lab 框架。
- **单智能体数据收集**：30 m × 30 m 自定义迷宫，内含墙壁形成狭窄通道；使用 Leatherback 四轮机器人（半径 0.254 m）。
- **多智能体价值函数训练**：开放空间（无迷宫墙），四个机器人以编队形式导航至共享目标，每个机器人相对编队中心有固定偏移，以此学习协调。
- **测试场景**：同样迷宫环境，四个机器人从左下角起始区域出发，各自前往右上角目标区域；每个机器人起点/目标相对编队中心偏移约 1.5 m 以产生多样性。

### 对比方法与评估指标
- **对比**：基线（无引导扩散） vs. 引导扩散（MARL 价值引导）。
- **主要指标**：**智能体间干扰率** \(R_{\text{agent}}\) = 智能体间距离 < 0.508 m 的时间步比例。
- **辅助指标**：**障碍物干扰率** \(R_{\text{obs}}\) = 机器人距离迷宫墙 < 0.254 m 的时间步比例。
- 每个条件重复 10 次取平均。

## 资源与算力
- 文中仅提及使用 **NVIDIA RTX 4090 GPU**，PyTorch + skrl 库。
- 训练细节：单智能体扩散模型训练 40 个 epoch；MAPPO 在开放空间训练（具体步数未说明）；未给出各阶段的总训练时长、GPU 数量等详细算力消耗数据。

## 实验数量与充分性
- **主要实验**：单一场景（四机器人迷宫导航）下基线与引导对比，每个条件 10 次重复。
- **缺乏**：不同引导尺度 \(\lambda\) 的消融实验、不同机器人数量（如 6、8 个）的扩展性测试、其他协调方法（如集中式优化、基于规则的方法）的对比、成功率或路径长度等更多维度的评估。
- **充分性评价**：实验规模较小，结果客观但覆盖不足；仅在一个特定迷宫和四机器人配置下验证，结论的泛化性有限。定性轨迹图提供了直观说明，但未进行统计分析。

## 论文的主要结论与发现
1. **MARL 价值引导有效减少干扰**：智能体间干扰率从基线 55.4% 降至 41.8%，降低了 13.6 个百分点。
2. **定性轨迹显示更分散**：引导后机器人之间保持更大空间间隔，但仍跟随相近的全局路径。
3. **存在 trade-off**：障碍物干扰率从 4.6% 上升至 7.6%，因为价值函数在开放空间训练，不感知迷宫墙壁，引导可能使机器人靠近边界。
4. **无需联合模型**：协调完全通过推理时的价值梯度实现，无需重新训练扩散模型或显式通信，保持了分散式生成的可扩展性。

## 优点
1. **方法创新**：首次将扩散模型的去中心化生成与 MARL 的集中式价值引导结合，实现交互感知规划，且不牺牲可扩展性。
2. **数学框架清晰**：基于指数倾斜的引导公式，将生成建模与强化学习统一，梯度组合具有理论依据。
3. **实用性强**：训练与推理解耦——单智能体数据和扩散模型可在任意环境中训练，价值函数也可在简化环境（开放空间）训练后迁移到复杂环境（迷宫），降低了对联合多机器人数据的要求。
4. **推理高效**：每个智能体独立采样，梯度计算集中但可并行，复杂度与智能体数量线性相关。
5. **实验指标具体**：定义明确的干扰率，并将智能体间干扰与障碍物干扰分开，揭示了引导带来的 trade-off。

## 不足与局限
1. **实验覆盖不足**：仅在四机器人单一迷宫场景下测试，缺少不同机器人数量、不同环境结构（如更复杂障碍物、狭窄通道）的评估。
2. **缺乏系统消融**：未对引导尺度 \(\lambda\)、去噪步数、训练数据量进行消融，也未与现有协调方法（如基于冲突搜索、集中式优化、其他强化学习方法）进行直接对比。
3. **价值函数环境不匹配**：MARL 价值函数在开放空间训练，导致在迷宫环境中引导时增加了障碍物干扰风险，说明需要环境自适应或迁移学习。
4. **指标单一**：主要依赖干扰率，未评估任务完成率、路径长度、计算时间等关键性能指标。
5. **未验证真实机器人**：仅在模拟中测试，与现实世界中的物理约束、感知噪声等仍有差距。
6. **可扩展性未充分证实**：虽然算法复杂度与智能体数量线性相关，但未在更多机器人（如 10、20 个）上进行实验；价值函数的输入维度随智能体数量增长，可能带来近似误差。

（完）
