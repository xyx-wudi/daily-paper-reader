---
title: "Dreaming Of Others: Latent Teammate Modeling In World Models For Multi-Agent Reinforcement Learning"
title_zh: 梦见他者：多智能体强化学习中世界模型中的潜在队友建模
authors: Tomas Leroy-Stone
date: 2026-05-29
pdf: "https://arxiv.org/pdf/2605.31361v1"
tags: ["query:marl"]
score: 9.0
evidence: 在世界模型中建模潜在队友的合作多智能体强化学习
tldr: 在合作多智能体强化学习（MARL）中，智能体需与内部策略不可直接观测的伙伴协调，而单智能体世界模型如Dreamer难以处理队友导致的不确定性。我们提出将队友视为结构化可学习组件，在Dreamer的RSSM中将潜在状态分解为环境与队友成分，并设计心智理论头从部分轨迹推断队友的行为特征，包括角色、意图和预测动作。这些队友潜在表示用于调节actor和critic，使智能体能想象并适应多样化合作者，实现零样本与小样本协调。本工作将世界模型从环境动力学预测器扩展为社交行为模拟器，为构建可泛化、与人类兼容的AI开辟新方向。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.31361v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1254, \"height\": 754, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.31361v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1097, \"height\": 714, \"label\": \"Figure\"}]"
motivation: 合作多智能体环境中，智能体需与不可观测的队友协调，但现有世界模型（如Dreamer）缺乏对队友不确定性的建模，导致性能受限。
method: 在Dreamer的RSSM中分解潜在状态为环境和队友组件，并学习Theory-of-Mind头从部分轨迹推断队友角色、意图和动作等潜在嵌入，用于条件化策略和价值网络。
result: 智能体通过想象适应多种合作伙伴，在部分可观测环境下实现零样本与小样本协调，提升协作泛化能力。
conclusion: 本工作将世界模型扩展为社交行为模拟器，为可泛化、与人类兼容的多智能体AI提供了新范式。
---

## 摘要
在合作性多智能体强化学习（MARL）中，智能体必须与内部策略和意图不可直接观察的伙伴进行协调。尽管像Dreamer这样的世界模型在单智能体设置中表现出强大的泛化能力和样本效率，但它们在MARL中的应用仍然受到无法处理队友引发的不确定性的限制。我们提出了一种新的视角：将队友视为智能体世界模型中结构化的、可学习的组件。我们引入了一种架构，将Dreamer风格循环状态空间模型（RSSM）的潜在状态分解为环境组件和队友组件，并学习一个辅助的心智理论（ToM）头，从部分轨迹中推断伙伴行为的潜在嵌入，如角色、意图和预测动作。这些队友潜在变量调节了演员和评论家，使智能体能够想象并适应多样化的协作者。我们概述了这种方法如何在部分可观测设置中支持零样本和少样本协调，并提出了一套基准测试和评估协议来评估其影响。这项工作将世界模型不仅定位为环境动态的预测器，而且作为社会行为的模拟器，为可泛化、与人类兼容的人工智能开辟了新的方向。

## Abstract
In cooperative multi-agent reinforcement learning (MARL), agents must coordinate with partners whose internal policies and intentions are not directly observable. While world models such as Dreamer have demonstrated strong generalization and sample efficiency in single-agent settings, their application to MARL remains limited by an inability to handle teammate-induced uncertainty. We propose a new perspective: treat teammates as structured, learnable components within the agent's world model. We introduce an architecture that factorizes the latent state of a Dreamer-style recurrent state-space model (RSSM) into environment and teammate components, and learns an auxiliary Theory-of-Mind (ToM) head to infer latent embeddings of partner behavior such as character, intent, and predicted actions from partial trajectories. These teammate latents condition the actor and critic, enabling the agent to imagine and adapt to diverse collaborators. We outline how this approach can support zero-shot and few-shot coordination in partially observable settings and propose a set of benchmarks and evaluation protocols to assess its impact. This work positions world models as not only predictors of environmental dynamics, but as simulators of social behavior, opening new directions for generalizable, human-compatible AI.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在合作多智能体强化学习（MARL）中，智能体需要与策略和意图不可直接观察的队友进行协调。现有世界模型（如Dreamer）在单智能体场景表现出色，但无法处理队友行为带来的非平稳性和部分可观测性，将队友视为“外生噪声”导致模型难以适应伙伴变化。
- **整体含义**：论文提出将世界模型从“环境动态预测器”扩展为“社会行为模拟器”，通过显式建模队友的潜在行为（角色、意图、动作）来降低非平稳性，实现零样本和少样本协调，为人类-AI协作提供新范式。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- 在Dreamer风格的循环状态空间模型（RSSM）中，将潜在状态分解为两个独立组件：环境潜在变量（\(z^{env}_t\)）和队友潜在变量（\(z^{team}_t\)）。
- 引入一个心智理论（Theory-of-Mind，ToM）头，从智能体自身的历史观测和动作中推断队友的潜在行为嵌入，并用此嵌入调节策略网络（actor）和价值网络（critic），使智能体在想象 rollout 中能够模拟队友行为的多样性。

### 关键技术细节
1. **因子化潜在状态**：在每一时间步 \(t\)，编码器将智能体的观测 \(x_t\) 和自身动作 \(a^0_t\) 映射为确定性隐藏状态 \(h_t\)，然后采样两个独立的随机潜在变量：
   - \(z^{env}_t\)：捕捉环境动态（如物理状态、物体位置）。
   - \(z^{team}_t\)：编码推断的队友行为特征。
2. **双解码器结构**：
   - 观测解码器：从 \(z^{env}_t\) 重构观测 \(\hat{x}_t\)。
   - 队友策略解码器：从 \(z^{team}_t\) 输出队友下一动作的预测分布 \(\hat{\pi}^j_t(\cdot)\)。
3. **转移模型**：所有智能体的动作（自身动作 \(a^0_t\) 和队友动作 \(a^j_t\)）共同驱动隐藏状态从 \(h_t\) 转移到 \(h_{t+1}\)。
4. **ToM训练目标**：最小化队友策略预测的交叉熵损失，并附加一个时间正则化项（KL散度），以保证队友潜在变量在时间上的一致性：
   \[
   \mathcal{L}_{\text{ToM}} = \mathbb{E}_t\left[-\sum_a \pi^j_t(a) \log \hat{\pi}^j_t(a)\right] + \alpha \, \text{KL}\left( q(z^{team}_t \mid h_t) \,\|\, p(z^{team}_t \mid h_{t-1}, a_{t-1}) \right)
   \]
   其中 \(\pi^j_t\) 是从数据中获得的队友动作经验分布（可为one-hot或平滑标签）。
5. **总训练损失**：标准Dreamer训练目标 + \(\lambda_{\text{ToM}} \mathcal{L}_{\text{ToM}}\)。
6. **部署与适应**：测试时，智能体在线推断 \(z^{team}_t\)，用于调节actor和critic；想象 rollout 中采样队友潜在变量以模拟不同伙伴行为，实现零样本协调；随着交互增多可实现少样本改进。无需共享潜在状态、集中式训练或显式通信。

## 3. 实验设计

**注意：本文是提案类论文，未报告任何实证实验结果。** 但论文提出了详细的评估协议和基准建议，具体包括：

- **测试场景/环境**：
  - **Multi-Agent Particle Environments (MPE)**：用于诊断模型是否能学习可区分的队友潜在变量、分离社会动态与物理动态。
  - **Overcooked-AI**：作为零样本协调的主要测试床，使用标准伙伴划分评估对新合作者的适应能力。
  - **Melting Pot**：评估在不同社会背景和伙伴群体下的鲁棒性，测试对规范和惯例分布偏移的处理能力。
- **使用的工具/框架**：JaxMARL、BenchMARL（用于标准化环境、指标和基线）。
- **建议对比方法**：论文未明确列举对比方法，但隐含与现有MARL世界模型（如MA-Dreamer、CoDreamer、GAWM、MACD）以及经典MARL算法（如MAPPO、QMIX）进行对比。
- **建议评估指标**：
  - 累积回报（episodic return）与样本效率。
  - 与未见伙伴的零样本协调分数。
  - 少样本交互后的改进程度。
  - 跨伙伴配对（cross-play）的鲁棒性。

## 4. 资源与算力

- **论文未提及任何关于GPU型号、数量、训练时长等算力资源信息**。由于是提案且无实验，该部分完全缺失。

## 5. 实验数量与充分性

- **该论文没有进行任何实验**，仅提出架构设计和评估协议，属于研究提案（workshop poster）。因此无法评价实验的充分性、客观性或公平性。

## 6. 论文的主要结论与发现

- **主要结论**：通过将队友建模为世界模型中的结构化潜在过程，而非随机噪声，可以：
  - 降低由伙伴策略变化引起的非平稳性。
  - 提升零样本协调能力（即直接与未见过伙伴合作）。
  - 产生可解释的潜在表示（如分离出队友角色、意图）。
  - 增进与人类伙伴的兼容性。
- **论文认为**：该工作为世界模型从“环境模拟器”向“社会行为模拟器”的转变提供了概念框架，可推动可泛化、能与人类协作的AI发展。

## 7. 优点

1. **概念创新**：首次将“心智理论”显式融入Dreamer-style世界模型的潜在状态分解中，区别对待环境与队友动态，理论动机清晰。
2. **设计简洁**：在现有Dreamer架构上仅增加一个队友解码器和ToM损失函数，不改变核心训练范式，易于实现和集成。
3. **强调泛化性**：专门针对零样本和少样本协调设计，关注人类-AI协作的实际需求，避免了传统CTDE对集中式信息的依赖。
4. **评估协议完备**：虽然无实验结果，但提案的评估协议覆盖了从诊断性场景（MPE）到复杂社会交互（Overcooked、Melting Pot）的多层次测试，并推荐了标准化工具（JaxMARL、BenchMARL），体现了方法论上的严谨性。

## 8. 不足与局限

1. **缺乏实证验证**：论文本质为概念提案，没有任何实验结果支持其有效性，无法确认方法在实际环境中的性能。
2. **假设依赖**：方法假设队友动作可被观测（用于训练ToM目标），但在完全部分可观测或队友动作不可见的环境中（如某些机器人协作场景）可能受限。
3. **计算复杂度**：因子化潜在状态和额外ToM解码器增加了模型参数量和训练时间，论文未分析开销。
4. **对队友模型的简化**：该方法假设队友行为可由一个静态潜在变量建模，但真实人类或学习智能体策略可能随时间动态变化（非平稳性可能仍存在）。
5. **未讨论失败模式**：未分析当队友行为与训练分布严重偏移时（如对抗性伙伴）模型可能的表现，也未考虑队友欺骗或误导情况。
6. **可扩展性**：仅在理论上讨论，未证明在大量智能体场景中的扩展性（如超过2个智能体时，如何区分多个队友的潜在表示）。
7. **资源信息缺失**：未提供任何关于训练速度、GPU需求等实践性信息，不利于他人复现或评估可行性。

（完）
