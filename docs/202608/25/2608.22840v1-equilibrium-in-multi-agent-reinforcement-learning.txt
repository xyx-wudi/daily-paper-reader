Title: Equilibrium in Multi-Agent Reinforcement Learning

URL Source: https://arxiv.org/pdf/2608.22840v1

Published Time: Tue, 25 Aug 2026 01:38:23 GMT

Number of Pages: 62

Markdown Content:
# Equilibrium in Multi-Agent Reinforcement Learning 

Maurizio D’Andrea 1 and Bar Light 21Department of Analytics and Operations, National University of Singapore’s Business School, dmaurizio 1@nus.edu.sg 

> 2

Business School and Institute of Operations Research and Analytics, National University of Singapore, barlight@nus.edu.sg 

August 25, 2026 

Abstract 

Standard solution concepts for stochastic games, such as Markov perfect equilibrium and Markov coarse correlated equilibrium, are computationally difficult, and thus, standard de-centralized reinforcement-learning algorithms should not generally be expected to converge to them. In this paper, we study the equilibrium generated by such algorithms. In particular, we introduce a new solution concept for stochastic games, Markov Bayes coarse correlated equilibrium (MBCCE), defined as a distribution over states and stationary policy profiles such that, after observing the state but before observing her recommended action, no player can gain by choosing a different current action, with the sampled policy profile governing play thereafter. We discuss the parallels between MBCCE and coarse correlated equilibrium (CCE) in finite normal-form games and show that MBCCE retains several of its key properties. We then introduce a corresponding regret notion, adaptive Markov coarse regret (AMCR), and show that vanishing AMCR implies that every accumulation point of the empirical distribution of realized states and policy profiles is an MBCCE. Crucially, we show that achieving AMCR reduces to two standard learning tasks: minimizing external regret at each state and accurately evaluating the current joint policy. We then prove that under mild conditions these properties hold for two natural RL algorithmic designs: a decentralized asynchronous actor–critic algorithm through a new two-timescale stochastic-approximation analysis, and a standard episodic multi-agent projected policy-gradient method. Hence, both algorithms generate approximate MBCCEs, and we establish explicit finite-time convergence rates for both. Finally, we relate MBCCE to stationary MCCE and use this connection together with our algorithmic guarantees to identify a natural class of stochastic games in which these algorithms achieve approximate MCCE. Taken together, our results identify MBCCE as a new equilibrium notion for the long-run empirical behavior of standard reinforcement-learning methods in general finite discounted stochastic games. 

1arXiv:2608.22840v1 [cs.GT] 24 Aug 2026 1 Introduction 

A key challenge in multi-agent reinforcement learning is to identify a solution concept that is both strategically meaningful and compatible with decentralized learning. A Markov perfect equilibrium (MPE) is the Markovian refinement of Nash equilibrium for stochastic games: it evaluates one stationary product-policy profile and requires optimal behavior at every state. While MPE therefore has a clear strategic interpretation, it is generally not compatible with decentralized learning. In normal-form games, coarse correlated equilibrium (CCE) provides a natural alternative to Nash that is closely tied to learning: no-regret dynamics generate CCE, providing a learning foundation for the concept, and CCE itself is computationally tractable. This suggests seeking an analogous relaxation of MPE in stochastic games. A stationary Markov coarse correlated equilibrium (MCCE), which evaluates one stationary joint-action law, is a natural first candidate for extending the same connection. The analogy, however, generally breaks down. Daskalakis et al. (2023) show that computing a constant-approximate stationary MCCE is PPAD-hard even in two-player turn-based discounted stochastic games. 1

This computational difficulty has a natural counterpart from the learning perspective. The no-regret route to stationary MCCE would naturally compare realized play with a fixed Markov-policy deviation in hindsight. In the standard reinforcement-learning setting we study, at each date the players observe the current state, choose actions, receive payoffs, and observe the next state, and use the resulting data to revise their policies. The transition law and payoff functions may be unknown, each player may have limited information about the other players, and because all players learn simultaneously, the policy profile generating the data can continue to change. In such a setting, changing a player’s Markov policy generally changes the subsequent state path and therefore the observations used by all players to update their policies. The realized history therefore does not determine the payoff of such a deviation without specifying how the learners would have behaved along the counterfactual history. To the best of our knowledge, whether decentralized learners can achieve sublinear regret against this pathwise Markov-policy benchmark in general stochastic games in the continuing, partial-feedback setting studied here remains open. Our main question is therefore: in a general stochastic game, what equilibrium property can be guaranteed for the adaptive policy path generated by decentralized tabular learning algorithms, even when the policies do not converge? To answer this question, we introduce a new distributional equilibrium notion, Markov Bayes coarse correlated equilibrium (MBCCE), which extends CCE to stochastic games differently from MCCE, as we discuss in detail in Section 4. An MBCCE is a joint distribution over a public state and a stationary product-policy profile. The players observe the state but not the sampled policy profile, so the state induces a common posterior over continuation policies. A mediator who observes 

> 1Daskalakis et al. (2023) also construct an efficient nonstationary Markov CCE. Their policy specifies the future sequence of Markov decision rules in advance. This differs from the learning environments studied here, which are standard in RL, in which future policies are revised after new data are observed and are therefore not known at the initial date.

2the sampled profile recommends a current action drawn according to that profile at the observed state. After observing the state but before seeing her recommendation, a player may instead choose a fixed action at that state. From the next state onward, play follows the sampled stationary profile. MBCCE requires that, conditional on every state that occurs with positive probability, no player gains by choosing such a fixed action rather than following the mediator’s recommendation. 2

The key feature of MBCCE is that each sampled stationary policy profile determines both current play and the continuation value used to evaluate a deviation. Thus, MBCCE preserves the coarse-obedience logic of CCE while accounting for the effect of current actions on future states. In a one-state stochastic game, MBCCE reduces to ordinary CCE. More generally, when a unilateral current action does not affect the next-state distribution, the MBCCE condition at each state becomes the CCE condition of the corresponding stage game. In addition, if the MBCCE distribution is concentrated on one stationary product-policy profile, and every state has positive probability, it reduces to MPE. MBCCE also retains further properties of CCE. First, Section 7 introduces a Markov smoothness condition and shows that the standard smoothness argument in Roughgarden (2009) yields statewise welfare guarantees for MBCCE. Second, MBCCE preserves the linear structure of CCE: for any finite collection of stationary policy profiles, its equilibrium conditions can be formulated as a linear program, as discussed in Section 7. To connect decentralized learning to MBCCE, we introduce adaptive Markov coarse regret 

(AMCR). AMCR evaluates a sequence of stationary product-policy profiles along the realized state path. At every visit to a state, it compares the player’s current mixed action with each fixed action available at that state. For such a comparison, the player changes only her current action: the opponents use their contemporaneous policies at the current state, and the contemporaneous joint policy profile is used to evaluate continuation from the next state onward. Across the realized visits to each state, AMCR then asks, for each player, which single fixed action would have produced the largest cumulative gain in hindsight. Importantly, AMCR accounts for the effect of the current action on future states while requiring only evaluation of the joint policy at that date. It therefore does not require specifying how the other learners would have behaved following a counterfactual deviation, making it compatible with adaptive algorithms that learn from data. We show that a vanishing AMCR gap implies that every accumulation point of the empirical distribution of realized states and contemporaneous policy profiles is an MBCCE. Our first result identifies a simple principle for obtaining AMCR: combine no regret at each state with accurate evaluation of the current joint policy profile. Joint-policy evaluation assigns each current action a score that accounts for both its immediate payoff and its effect on future states, assuming that the current joint policy is followed thereafter. Theorem 3.6 bounds the AMCR gap by the aggregate statewise external regret under these scores and the average joint-policy-evaluation error. With exact policy evaluation, AMCR becomes exactly statewise external regret. Thus, obtaining AMCR reduces to two familiar tasks: evaluating the current joint policy and using 

> 2The term “Bayes” refers to the posterior over continuation policies induced by the observed public state; it does not require private types. The timing is the usual Bayes coarse-obedience timing adapted to a publicly observed state; see Hartline et al. (2015).

3no-regret learning among the actions available at each visited state. Guided by this principle, we consider two natural decentralized reinforcement learning algorithms that yield AMCR. The first is an asynchronous actor–critic algorithm that learns continuously along a single trajectory. Each player observes the current state, the realized joint action, her own payoff, and the next state, but not the opponents’ policies, payoff functions, or learning rules, and the transition kernel is unknown. We use a standard temporal-difference update to estimate action values from realized state–action transitions, while the player’s own policy and the realized actions of the opponents are used to estimate the continuation value of the current joint policy. We use these values to provide statewise feedback to a no-regret actor and we allow each player–state pair to use any actor from a broad family that has sublinear external regret and changes its policy sufficiently slowly; this family includes Hedge, Euclidean dual averaging, projected gradient ascent, and their optimistic variants, and different players and states may use different actors. To generate the coverage needed for learning in such a setting, the implemented policies include fixed uniform exploration, and we impose only that the state chain generated by uniform joint actions is irreducible. The main analytical difficulty is that the policy profile being evaluated continues to change while the value updates are asynchronous and occur on endogenous local clocks. We develop a new two-timescale analysis showing that the value estimates track this moving joint-policy evaluation; Section 5 gives the technical overview. Combining this tracking result with statewise no regret, Theorem 5.5 we show that the behavior-policy path generated by this algorithm is asymptotically an O(ε)-AMCR almost surely, where the approximation comes from fixed exploration. Theorem 5.7 further provides finite-time rates for such a setting. In particular, for every r ∈ (0 , 1/4), the expected AMCR gap is bounded by an O(ε) term plus O(T −r). By the connection between AMCR and MBCCE, these guarantees also produce an approximate MBCCE for the actor-critic algorithm. Our second algorithm is an episodic projected policy-gradient method, similar to the REIN-FORCE procedure studied by Leonardos et al. (2022) for potential Markov games. We show that the same type of decentralized procedure can obtain AMCR in any finite stochastic game. In this setting, the joint policy is held fixed within each episode and each player observes only the states, her own actions, and her own realized payoffs; episodes provide joint-policy evaluation, while projected policy-gradient updates provide statewise no regret. With suitable step sizes, Theorem 6.1 shows that the resulting behavior-policy path generated by this algorithm achieves exact AMCR asymptotically in every finite discounted stochastic game. After E episodes, the expected AMCR gap over all stages played in those episodes is O(E−1/3). Consequently, every accumulation point of the empirical distribution of realized states and contemporaneous policies is an MBCCE. Therefore, these two algorithms, despite both relying on standard features of reinforcement learning, differ substantially and still provide complementary implementations of the same regret–evaluation principle. We next connect MBCCE to stationary MCCE. Given an MBCCE, we form a stationary joint-action law by averaging, at each state, the action distributions prescribed by its policy profiles. We show that the MCCE gap of this averaged law is controlled by a continuation-value discrepancy that measures how the payoff effect of a unilateral action changes when continuation is evaluated under the 4sampled policy profile or under the averaged stationary law. This result implies, in particular, that an MBCCE induces an approximate MCCE whenever this continuation-value discrepancy has only a small effect on unilateral-deviation payoffs. We use this connection to derive MCCE guarantees for transition-aggregative stochastic games, in which the next-state distribution depends Lipschitz continuously on a weighted aggregate of players’ actions, while stage payoffs may have unrestricted strategic interactions. When players have equal weight each player’s influence on the next-state distribution is O(1 /N ) where N is the number of players. We show that the empirical joint-action law generated by our actor–critic algorithm is then an O(ε + 1 /N )-approximate MCCE, while the episodic policy-gradient algorithm yields an O(1 /N )-approximate MCCE. Dynamic routing and common-pool-resource games provide canonical examples of such settings, and hence, our algorithms achieve approximate MCCE for such games when the number of players is large. We also show in this setting we can provide welfare guarantees for MBCCE. The rest of the paper is organized as follows. Section 2 introduces the stochastic game and the feedback structure. Section 3 defines AMCR and establishes the regret–joint-policy-evaluation principle. Section 4 introduces MBCCE and establishes its connection to empirical learning paths. Section 5 studies the continuing actor–critic algorithm, and Section 6 studies episodic projected REINFORCE. Section 7 develops further properties of MBCCE, including its linearity of structure and welfare guarantees under smoothness. Section 8 relates MBCCE to stationary MCCE and applies the connection to transition-aggregative stochastic games. 

1.1 Motivating example 

Before introducing our equilibrium notions, we compare a normal-form game with a stochastic game that has a similar best-response pattern. The example provides a stylized setting in which stationary MCCE does not enlarge MPE, while MBCCE produces an enlargement analogous to that of CCE over Nash equilibrium—the type of enlargement one expects from adaptive learning in such settings. 

Example 1.1. Consider first the common-interest anti-coordination game 0 10 (0 , 0) (1 , 1) 1 (1 , 1) (0 , 0) .

The Nash marginal profiles are (1 , 0), (0 , 1), and (1 /2, 1/2). The possible action marginals under CCE form the shaded quadrilateral in Figure 1(a). As usual in such games, the CCE set generated by no-regret learning is larger than the Nash equilibrium set, since such dynamics generally do not converge to an exact best response. Now consider a two-player stochastic investment game with states S = {0, 1} and actions 

Ai = {0, 1}, where action 1 means investment. The following particular primitives simplify the calculations, but the economic structure is a standard public-investment problem with strategic 5substitutes. 

ui(s, a ) = s − 12 ai, P (1 | 0, a ) = a1 + a2

3 , P (1 | 1, a ) = 34 , γ = 45 .

The complementary transition probability leads to state 0. State 1 yields positive payoff but investment is costly. Aggregate investment at state 0 raises the probability of reaching state 1. At state 1, investment lowers the current payoff by 1 /2 without affecting transitions, so action 0 is strictly optimal. Under all three equilibrium notions considered here (MPE, MCCE, and MBCCE whenever state 1 occurs with positive probability), players therefore choose action 0 at state 1. Given this behavior, a direct calculation provided in the appendix shows that at state 0 a player prefers to invest exactly when the other player invests with probability below 1 /2. Intuitively, the other player’s investment already raises the probability of reaching state 1, reducing the benefit from investing oneself. Thus, the best-response correspondence at state 0 is exactly the same as in the normal-form anti-coordination game. The MPE investment probabilities at state 0 are consequently the same three points shown in Figure 1(b). Stationary MCCE does not enlarge this set. In this example, under any stationary correlated action law, the value of every stationary deviation depends only on the players’ statewise investment probabilities. Hence, replacing the correlated law by independent Markov policies with the same marginals does not change the equilibrium values, so a stationary joint-action law is an MCCE exactly when the corresponding product policy is an MPE. 3 The MCCE investment probabilities at state 0 are therefore again (1 , 0), (0 , 1), and (1 /2, 1/2). MBCCE produces the analogue of the CCE enlargement discussed above. As shown in Figure 1(b), MBCCE permits a two-dimensional set of investment probabilities at state 0, whereas MPE and MCCE are restricted to the three exact stationary best-response profiles described above. 4

In multi-agent reinforcement learning, one would not generally expect the policy path to converge to one of these stationary best-response profiles, much as no-regret dynamics in normal-form games need not converge to a Nash equilibrium. In this sense, MBCCE extends CCE from normal-form games as the distributional equilibrium generated by the empirical state–policy distributions of reinforcement learning algorithms, at least as we show for the actor–critic and policy-gradient algorithms studied in this paper. 

1.2 Related Literature 

Our paper is related to two strands of literature.    

> 3MCCE can still correlate the players’ actions. In particular, at the symmetric marginal profile (1 /2,1/2), MCCE allows correlated joint-action laws with these marginals, whereas MPE uses independent mixing, but this additional correlation does not affect payoffs, transitions, or deviation values.
> 4To obtain the shaded region, we consider the simple case in which the distribution over state-0 continuation policies is supported on the four pure action profiles ( a, b )∈ { 0,1}2. This is the only restriction imposed here: action 0 at state 1 is strictly optimal and therefore is chosen under every MBCCE in this game. Even this simple finite-support case already generates the two-dimensional shaded region. The formal construction and its exact characterization are given in the Appendix.

60.0 0.5 1.0 Player 1 action-1 probability 0.0 0.5 1.0 Player 2 action-1 probability   

> 2/3 1/3
> (a) Normal-form anti-coordination
> CCE Nash equilibrium
> 0.0 0.5 1.0 Player 1 action-1 probability 0.0 0.5 1.0
> 12/17 5/13
> (b) Stochastic investment
> MBCCE MPE and MCCE

Figure 1: Each axis gives a player’s probability of action 1; in panel (b), action 1 is investment at state 0. The points show Nash equilibrium marginals in panel (a) and the common MPE–MCCE marginals in panel (b). The shaded regions show CCE marginals in panel (a) and MBCCE marginals in panel (b). The MBCCE region is exact within the four pure stationary profiles described in Example 1.1. 

Learning in stochastic games. A growing literature develops algorithms that produce equilibrium policies in general-sum Markov games. Jin et al. (2024) introduce V-learning for episodic Markov games, obtaining correlated or coarse correlated policies in multiplayer general-sum games without dependence on the size of the joint action space. In finite-horizon models, Cai et al. (2024), Mao et al. (2024) develop policy-optimization procedures built from statewise no-regret updates. Daskalakis et al. (2023) establish the computational hardness of stationary MCCE and construct an efficiently learnable nonstationary Markov CCE. Recently, Farina et al. (2026) extend the hardness result to single-controller stochastic games. A broader literature studies learning under additional stochastic-game structure. In two-player zero-sum stochastic games, Daskalakis et al. (2020) study independent policy-gradient methods, while Xie et al. (2023) and Yan et al. (2024) study learning with function approximation or offline data. Global policy-gradient guarantees are also available in Markov potential games (Leonardos et al., 2022; Ding et al., 2022). Mean-field equilibrium and learning, as well as networked multiagent reinforcement learning, are studied in (Adlakha and Johari, 2013; Qu et al., 2022; Guo et al., 2023; Gu et al., 2025; Light, 2025). In general stochastic games, Giannou et al. (2022) establish local convergence around second-order stationary Nash policies, while Maheshwari et al. (2024) characterize the asymptotic behavior of decentralized actor–critic dynamics using Markov near-potential functions. These contributions focus on computing an equilibrium policy or a prescribed policy sequence, or on convergence of learning dynamics, often under additional game structure. We instead characterize the equilibrium 7content of the adaptive path produced by decentralized tabular learning, and our AMCR–MBCCE framework is developed for general finite discounted stochastic games. A related literature studies regret against policy deviations in Markov games. Foster et al. (2023) establish computational and statistical hardness results for independent no-regret learning in finite-horizon episodic Markov games. Liu et al. (2022) and Zhan et al. (2023) obtain no-regret guarantees against fixed policy comparators in episodic policy-revealing settings, where an opponent’s past policies are observed; Zhan et al. (2023) also allows general function approximation. Wang et al. (2023) develop decentralized policy-space mirror descent and all-policy evaluation in episodic Markov games under function approximation. Erez et al. (2023) study regret against Markov-policy deviations in finite-horizon common-algorithm self-play and obtain sublinear swap regret under an additional endogenous condition on the resulting policy iterates. Nguyen-Tang and Arora (2024) study one learner against an adaptive opponent, obtaining positive results under bounded-memory, stationarity, and consistency conditions together with hardness results outside such structure. These results use episodic protocols or rely on substantially stronger information or structural assumptions. They do not cover the continuing, single-trajectory partial-feedback setting of our actor–critic result. 

Asynchronous two-timescale stochastic approximation. This literature is related specifi-cally to our continuing actor–critic algorithm. The differential-inclusion framework developed in Bena¨ ım et al. (2005) and its asynchronous extension in Perkins and Leslie (2013), which also treats coupled two-timescale recursions, provide general asymptotic tools for stochastic-approximation dynamics. More recently, Borkar (2025) allows either timescale to remain nonconvergent and characterizes the possible asymptotic behavior. These frameworks characterize asymptotic behavior and do not provide the quantitative moving-target policy-evaluation bound used in our finite-time AMCR result. In addition, asynchronous analyses typically assume sufficient state–action visitation, whereas in our setting we derive this coverage from a primitive communication condition on the transition dynamics together with the exploration built into the algorithm. Hakami and Dehghan (2016) analyze a specific three-timescale no-regret Q-learning scheme for constrained general-sum stochastic games and establish convergence to the set of stationary correlated equilibria. In contrast, our critic tracks any no-regret actor satisfying mild regret and movement guarantees, even when the actor path does not converge. Recent finite-time analyses study more structured two-timescale fixed-point recursions. Chandak et al. (2025) assume that both the fast map and the reduced slow map are contractive. Chandak (2026) permits a nonexpansive reduced slow map and obtains a residual-rate exponent arbitrarily close to one quarter. In these papers, the fast recursion tracks the fixed point associated with a prescribed slow fixed-point iteration whose reduced map is contractive or nonexpansive, and the analysis targets convergence or residual of the slow recursion. Our objective is different: without requiring the slow policy update to be a fixed-point iteration or its reduced map to be contractive or nonexpansive, we establish quantitative policy-evaluation tracking along a possibly nonconvergent policy path and use this tracking to obtain finite-time AMCR guarantees. 82 Model 

2.1 Stochastic game 

We consider an N -player discounted stochastic game with finite state space S, finite action sets Ai,and joint action space A := QNi=1 Ai. Write a = ( a1, . . . , a N ) and A−i := Q  

> j̸=i

Aj . At stage t, the state is st, each player i chooses an action ai,t , and the resulting joint action is at = ( a1,t , . . . , a N,t ). Player i then receives ui(st, a t) ∈ [0 , 1], and the next state is drawn from P (· | st, a t). The discount factor is γ ∈ (0 , 1), and H := (1 − γ)−1.A stationary Markov policy of player i is a map πi : S → ∆( Ai), where πi(ai | s) is the probability of action ai at state s. A stationary product-policy profile is denoted by Π = ( πi)Ni=1 , with joint action law Π( · | s) := NNi=1 πi(· | s). We write Π −i(· | s) := N  

> j̸=i

πj (· | s) for the marginal of all players other than i.

2.2 Information and feedback 

At date t, the learning rules maintain a target profile Π t ∈ C and select a behavior profile pt ∈ C 

where 

C := Y

> s∈S
> N

Y

> i=1

∆( Ai)is the compact set of stationary product-policy profiles. The target profile is the policy being updated and evaluated, whereas the behavior profile generates the actions used to collect data. The behavior profile introduces the additional randomization required for learning. All random variables are defined on a probability space (Ω , F, P). Let Ht be the σ-field generated by all data available in the analysis immediately before the date-t action. 5 Conditional on Ht, the players draw their actions independently according to the behavior profile: 

P(at = a | H t) = pt(a | st) := 

> N

Y

> i=1

pi,t (ai | st), a ∈ A . (1) We assume that player i observes the current state, her own payoff, and the next state; she does not observe the other players’ policies, payoff functions, or learning rules, and the transition kernel is unknown. In particular, players do not have all the information contained in Ht. The actor–critic algorithm developed in Section 5 also assumes that player i observes the realized joint                          

> 5Formally, let H1be the σ-field generated by the initial state and the initial variables of the learning rules. Recursively, Π t,pt, and all variables maintained before the date-taction are Ht-measurable. Conditional on Htand the realized action, the state moves according to the transition kernel: P(st+1 =s′| H t∨σ(at)) = P(s′|st, a t). After the transition, define the next global history by
> Ht+1 := σ
> 
> Ht, a t,(ui(st, a t)) Ni=1 , s t+1
> 
> .
> This definition makes Htthe information available immediately before the date-taction, where σ(·) denotes the
> σ-algebra generated by its arguments.

9action. Importantly, throughout the analysis, player i does not observe the policies or counterfactual actions of the other players. 

2.3 Notation 

For finite-dimensional vectors x and y, write ⟨x, y ⟩ := P 

> j

xj yj , ∥x∥1 := P 

> j

|xj |, and ∥x∥∞ := max j |xj |. The notation 1{E} denotes the indicator of an event E. For a scalar x, let ( x)+ := max {x, 0}. For probability vectors α and β, let TV( α, β ) := 12 ∥α − β∥1. For stationary product profiles, use 

∥Π − Π′∥1 := X

> s∈S
> N

X

> i=1

∥πi(· | s) − π′

> i

(· | s)∥1.

For a horizon T , define Ts(T ) := {t ≤ T : st = s}, and Ns(T ) := |Ts(T )|. When no ambiguity arises, write Ts and Ns. Let t1(s) < t 2(s) < · · · denote the successive visits to state s, and define the local state and state–action clocks nt(s) := Ptu=1 1{su = s}, and nt(s, a ) := Ptu=1 1{su = s, a u = a}.Thus ntq (s)(s) = q and P 

> s

Ns(T ) = T . These clocks index the asynchronous updates below: a state block changes only when that state is visited, and a state–action coordinate changes only when the corresponding joint action is observed. 

# 3 Adaptive Markov coarse regret 

In this section we introduce Adaptive Markov coarse regret (AMCR). For each action at a state, AMCR evaluates the gain from using that action at every realized visit, with the profile of all players at that date as the continuation benchmark. For each player–state pair, AMCR selects the best fixed action in hindsight and requires its long-run time-average gain to vanish. Intuitively, AMCR requires that no fixed action yield a persistent average improvement relative to the contemporaneous profiles. This notion avoids specifying the unobserved future learning path under a counterfactual deviation which is a key challenge in a general stochastic game setting as we discussed in the introduction. To define AMCR we first define the value of a current action under a stationary continuation law. A stationary statewise joint-action law ρ = ( ρs)s∈S ∈ Q 

> s

∆( A) determines the canonical Bellman pair for ( i, s, a ): 

Qρi (s, a ) = ui(s, a ) + γ X

> s′

P (s′ | s, a )V ρi (s′),V ρi (s) = X

> a

ρs(a)Qρi (s, a ).

(2) The following Lemma follows from standard dynamic programming arguments. All proofs in this paper are in the appendix. 

Lemma 3.1 (Canonical Bellman pair) . For every ρ ∈ Q 

> s

∆( A), system (2) has a unique solution, and Qρ, V ρ ∈ [0 , H ] componentwise. 

For a product profile Π, we denote by ( QΠ, V Π) the canonical pair generated by Π( · | s) at every state. Intuitively, at date t, this pair treats Π t as a stationary continuation benchmark for 10 evaluating the current decision. With this notation, for a product profile Π, we define the deviation advantage 

Ai(Π; s, a ′

> i

) := X

> a−i

Π−i(a−i | s)QΠ 

> i

(s, (a′

> i

, a −i)) − V Π 

> i

(s). (3) as the expected gain from choosing a′ 

> i

at the current visit to s compared to drawing an action from 

πi(· | s), when the other players draw from Π −i(· | s) and all players follow Π from the next state onward. 

Definition 3.2 (Adaptive Markov coarse regret) . For a path Γ 1: T = (Γ t)Tt=1 of stationary product profiles, define 6

GAMCR  

> T

(Γ 1: T ) := 1

T

X

> s∈S
> N

X

> i=1

 max 

> a′
> i∈Ai

X 

> t∈Ts(T)

Ai(Γ t; s, a ′

> i

)



> +

. (4) We say that the path is a δ-AMCR at horizon T when GAMCR  

> T

(Γ 1: T ) ≤ δ, and it is asymptotically 

δ-AMCR when lim sup  

> T→∞

GAMCR  

> T

(Γ 1: T ) ≤ δ. 

For the behavior-policy path actually played we write GAMCR  

> T

:= GAMCR  

> T

(p1: T ). The maximization in (4) selects the best fixed action in hindsight for each player–state pair and evaluates it across all realized visits to that state. A key feature is the time average: the profile and its Bellman values may change from one visit to the next, but a small gap means that no fixed statewise action would have produced a systematic continuation-adjusted improvement along the realized path. The deviation is coarse because it does not condition on a recommendation, and adaptive because each date is evaluated under its own current profile. As explained above, AMCR requires neither a forecast of future learning nor a model of how the other players would react to an alternative history. 

Remark 3.3 (AMCR and MPE) . The one-deviation principle characterizes a Markov perfect equilib-rium: at every state, no player can gain by changing only her current action and then returning to the equilibrium profile. Formally, we have the following definition: 

Definition 3.4 (Stationary Markov perfect equilibrium) . For a stationary product profile Π, define Gap MPE (Π) := (1 − γ) max 

> i, s ∈S
> βi:S→ Ai



V βi,Π−i 

> i

(s) − V Π 

> i

(s)

 

> +

.

The profile Π is a δ-MPE if Gap MPE (Π) ≤ δ.         

> 6Throughout the paper, all AMCR gaps are evaluated along the realized state path ( st)t≥1generated by the behavior policies. To make this dependence explicit, one may write
> GAMCR
> T(Γ 1: T;s1: T) := 1
> T
> X
> s∈S
> NX
> i=1
> max
> a′
> i∈Ai
> X
> t≤Tst=s
> Ai(Γ t;s, a ′
> i)
> 
> +
> .
> We suppress s1: Tfrom the notation.

11 AMCR on the other hand aggregates deviation advantages along the realized visits to each state and requires that no fixed action yield a positive long-run average gain. If the sequence of policies converges, the continuation values stabilize, and under state space coverage condition, AMCR reduces to MPE. 

Proposition 3.5 . Suppose Γt → Π⋆, GAMCR  

> T

(Γ 1: T ) → 0, and lim inf T →∞ Ns(T )/T > 0 for every 

s ∈ S . Then Gap MPE (Π ⋆) = 0 .

We next show in Theorem 3.6 that two properties naturally desired of a learning algorithm imply AMCR: the actor should have small statewise regret relative to the scores it uses, and those scores should evaluate the true current joint profile accurately. We first define the statewise regret induced by a given sequence of scores. Suppose an algorithm maintains value tables ( Qt, V t). At a visit t to state st, define the target-law gain vector 

gtar  

> i,t

(ai) := X

> a−i

Π−i,t (a−i | st)Qi,t (st, (ai, a −i)) (5) and the target-law regret over the first n visits to state s,

Rtar  

> i,s

(n) := 

 max 

> a′
> i∈Ai
> n

X

> q=1

h

gtar  

> i,t q(s)

(a′

> i

) −

D

πi,t q (s)(· | s), g tar  

> i,t q(s)

Ei 

> +

. (6) Set Rtar  

> i,s

(0) := 0. This is the standard external-regret criterion applied statewise: it compares the cumulative score obtained by the actor at visits to state s with the cumulative score of the best fixed action in hindsight. 

Theorem 3.6 (Regret–joint-policy-evaluation principle) . Let et := ∥(Qt, V t) − (QΠt , V Πt )∥∞. For every sample path and every T ,

GAMCR  

> T

(Π 1: T ) ≤ 1

T

X

> s,i

Rtar  

> i,s

(Ns(T )) + 2NT

> T

X

> t=1

et. (7) Theorem 3.6 separates the two sources of error. The first term in (7) measures how well the actor responds to its estimated statewise scores. The second measures the error in estimating the canonical Bellman pair of the current joint profile. Because the profile Π t may change with t, et is an online joint-policy-evaluation error: at each date, the maintained tables by the learning algorithm are compared with the Bellman values of the contemporaneous profile. Consequently, sublinear aggregate regret and vanishing average joint-policy-evaluation error imply AMCR. We note that from the argument in the proof of Theorem 3.6 we have 

GAMCR  

> T

(Π 1: T ) − 1

T

X

> s∈S
> N

X

> i=1

Rtar  

> i,s

(Ns(T )) ≤ 2NT

> T

X

> t=1

et.

12 Consequently, if the average joint-policy-evaluation error vanishes, then GAMCR  

> T

(Π 1: T ) → 0 if and only if we have sub-linear regret, i.e., P

> s∈S

PNi=1 Rtar  

> i,s

(Ns(T )) = o(T ). In particular, with exact policy evaluation, namely et = 0 for every t, the AMCR gap equals regret at every horizon. This leads to a natural question of how exactly AMCR extends the no-regret interpretation of equilibrium in normal-form games to stochastic games. We discuss this connection in detail in the next section. 

# 4 Markov Bayes coarse correlated equilibrium 

We next relate AMCR to a static equilibrium notion that plays for Markov perfect equilibrium a role analogous to that of coarse correlated equilibrium for Nash equilibrium. The relevant static equilibrium notion preserves the timing of a stochastic game: the current state is publicly observed before actions are chosen. Thus, if one considers a static game preceded by a public state, the natural coarse equilibrium notion is a Bayes coarse correlated equilibrium (BCCE), in which deviations may depend on the observed state but must be chosen before observing the assigned action; see Hartline et al. (2015). Let μ be a Borel probability measure on S × C . Ex ante, its marginal on C is the common prior over stationary product-policy profiles. Nature draws ( s, Π) according to μ. Players observe the public state s but not Π, so whenever μ({s} × C ) > 0 their common posterior over the continuation policy is μ(dΠ | s). After observing the state, a mediator who also observes Π draws a ∼ Π( · | s). Before observing this assigned action, player i may follow the mediator’s recommendation or replace it by a fixed action bi ∈ Ai. Continuation play from the next state onward remains governed by Π. Thus QΠ 

> i

(s, a ) is the payoff from following the recommendation and QΠ 

> i

(s, (bi, a −i)) is the payoff from the deviation. 

Definition 4.1 (Markov Bayes coarse correlated equilibrium) . A probability measure μ ∈ ∆( S × C )is a Markov Bayes coarse correlated equilibrium (MBCCE) if, for every player i, every state s with 

μ({s} × C ) > 0, and every bi ∈ Ai,

EΠ∼μ(·| s)

> a∼Π( ·| s)

QΠ 

> i

(s, a ) ≥ EΠ∼μ(·| s)

> a∼Π( ·| s)

QΠ 

> i

(s, (bi, a −i))  . (8) The timing in Definition 4.1 is the usual coarse-obedience timing adapted to the public state of a stochastic game. A player may condition her deviation on the observed state, and hence on the posterior that the state induces over continuation policies, but not on the action drawn by the mediator. The next result gives a stochastic game analogue of the familiar connection between no regret and coarse correlated equilibrium in normal form games. Vanishing AMCR implies that every accumulation point of the states-policies empirical distribution is an MBCCE. 

Proposition 4.2 (AMCR and empirical MBCCE) . For a path Γ1: T , let bμT := T −1 PTt=1 δ(st,Γt).

13 Then 

X 

> s∈S
> Ns(T)>0

Ns(T )

T

> N

X

> i=1

max 

> bi∈Ai

EΠ∼bμT (·| s)

> a∼Π( ·| s)

QΠ 

> i

(s, (bi, a −i)) − QΠ 

> i

(s, a )!

> +

= GAMCR  

> T

(Γ 1: T ).

Consequently, if GAMCR  

> T

(Γ 1: T ) → 0, every weak accumulation point of (bμT ) is an MBCCE. 

MBCCE also relates to MPE in a similar way to the relation between CCE and Nash equilibrium. If there is no uncertainty over the policy profile, so that the policy marginal of μ is concentrated on some Π, and every state has positive probability, then the posterior after every state is concentrated on the same Π. Condition (8) then reduces to P 

> a−i

Π−i(a−i | s)QΠ 

> i

(s, (bi, a −i)) ≤ V Π 

> i

(s) for every 

i, s, b i. These are exactly the statewise one-deviation conditions for a stationary MPE (see Remark 3.3). Thus, an MBCCE concentrated on a single stationary profile is an MPE in the stochastic game, paralleling the familiar normal-form result that a CCE whose action distribution is a product distribution is a Nash equilibrium. More generally, we can think of MBCCE as another way to generalize CCE to stochastic games, as explained in the following remark. 

Remark 4.3 (Extensions of CCE to stochastic games) . There are two equivalent representations of a CCE in a finite normal-form game. First, a CCE is a joint-action distribution λ ∈ ∆( A) satisfying the usual coarse-obedience inequalities. Alternatively, let ν ∈ ∆ ( Q 

> i

∆( Ai)) be a distribution over mixed-strategy profiles x = ( xi)Ni=1 , draw x ∼ ν, and then draw a ∼ N 

> i

xi. The induced joint-action distribution is λν (a) := Ex∼ν

hQNi=1 xi(ai)

i

. Then, for every player i and fixed deviation bi ∈ Ai,

E x∼νa∼N 

> ixi

[ui(a) − ui(bi, a −i)] = Ea∼λν [ui(a) − ui(bi, a −i)] .

Hence, coarse obedience under ν is equivalent to the CCE inequalities for λν . Conversely, every joint-action distribution λ admits such a representation by assigning probability λ(a) to the pure mixed-strategy profile ( eai )Ni=1 for each a ∈ A .These two equivalent representations lead to distinct extensions in a stochastic game. Stationary MCCE extends the joint-action representation: it specifies a stationary statewise joint-action law 

σ = ( σs)s∈S and evaluates continuation under σ. MBCCE instead extends the distribution-over-mixed-strategy-profiles representation: conditional on the observed state s, it assigns a distribution 

μ(dΠ | s) over stationary product-policy profiles. In a normal-form game, passing from ν to its induced joint-action distribution λν does not change any coarse-deviation payoff. In a stochastic game, however, the analogous statewise averaging generally changes the continuation values used to evaluate deviations. Thus, the two representations of CCE that are equivalent in a normal-form game give rise to distinct solution concepts in a stochastic game: stationary MCCE and MBCCE. Section 8 makes the connection between MCCE and MBCCE precise. 14 5 Actor–critic algorithm and AMCR guarantees 

Theorem 3.6 reduces the target-path AMCR gap to two terms: statewise target-law regret and online joint-policy-evaluation error. We now develop an online actor–critic algorithm that controls both terms up to a fixed-exploration error. For each state s, let Ui(· | s) be the uniform distribution on Ai, so Ui(ai | s) = 1 /|Ai|, and let U

denote the corresponding product profile. Fix ε ∈ (0 , 1) and define the behavior profile by 

pi,t (· | s) = (1 − ε)πi,t (· | s) + εUi(· | s), i = 1 , . . . , N, s ∈ S . (9) The target profile Π t is updated by the actors whereas pt is the profile actually played. The fixed mixing in (9) is used to guarantee state-space coverage by the algorithm. In par-ticular, it provides full support over joint actions whenever a state is visited and combined with the communication condition imposed below, it yields the state–action coverage required by the asynchronous critic that is typically needed for stochastic approximation analysis. Importantly, the analysis derives coverage directly from the mild communication assumption given below and the algorithm’s exploration and not from exogenous state–action visitation frequencies assumption. The price of fixed exploration is that Algorithm 1 achieves an O(ε)-approximate AMCR guarantee rather than exact AMCR. Alternatively, one could set pt = Π t and impose directly the state–action visitation frequencies required by the critic. This approach would yield exact AMCR guarantees and simplify the asynchronous stochastic-approximation analysis, but at the cost of an exogenous coverage assumption. We now define the primitive communication property needed to prove our results. 7

Assumption 5.1. The finite-state chain with transition matrix under uniform joint actions P U

given by 

P U(s′ | s) := 1

|A| 

X

> a∈A

P (s′ | s, a ). (10) 

is irreducible. 

In the actor–critic algorithm, each player maintains Q and V critic tables. Under the model of Section 2, player i observes a−i,t but not the opponents’ policies. After this observation, she forms the vector Qi,t (st, (·, a −i,t )). Thus, one realization of the opponents’ action supplies a score for every action of player i through her current Q table. The Q table update is a standard tabular one-step temporal-difference update for the realized joint action. The V update averages the current Q

values under player i’s target policy, while the opponents’ realized action provides the corresponding averaging over their behavior without requiring their mixed policies to be observed.         

> 7Note that a stronger condition is P(s′|s, a )≥P > 0 for all s, s ′, a . This condition can be enforced by a small full-support perturbation of the transition kernel, at the cost of an additional approximation error relative to the original game. We expect the AMCR guarantee to transfer to the original game with an additional approximation error that vanishes with the size of the perturbation so in this sense we believe that we can remove the communication assumption. We do not pursue this extension here.

15 The actor uses the same gain vector as feedback for a statewise no-regret algorithm. We allow for general no-regret actors that belong to the family we now define. 

Definition 5.2 (Admissible statewise actor) . Fix a finite action set A and a positive nonincreasing sequence ( βn)n≥1. On round n, a statewise actor chooses πn ∈ ∆( A) before observing gn ∈ [0 , H ]A,using past gains and any predictable internal state. It is admissible if there are finite nonnegative constants DA, CA, and LA such that, for every gain sequence and every n ≥ 1, max 

> a∈An

X

> q=1

[gq(a) − ⟨ πq, g q⟩] ≤ DA

βn

+ CA

> n

X

> q=1

βq, (11) and 

∥πn+1 − πn∥1 ≤ LAβn. (12) Condition (11) is a pathwise external-regret bound. Condition (12) limits how rapidly the policy can change and is typically satisfied by standard no regret algorithms such as follow the regularized leader (FTRL) and online mirror ascent as we now show. Fix a norm ∥ · ∥ on RA and let ∥ · ∥ ∗ denote its dual. For the cumulative gain Sn−1 := P 

> q<n

gq,FTRL with regularizer h chooses 

πn = arg max  

> π∈∆( A)

{βn⟨Sn−1, π ⟩ − h(π)} .

We next define online mirror ascent. Suppose that h is differentiable, and let Dh(π, z ) := 

h(π) − h(z) − ⟨∇ h(z), π − z⟩ be its Bregman divergence. Starting from π1 ∈ ∆( A), ordinary online mirror ascent updates the policy after observing gn according to 

πn+1 ∈ arg max  

> π∈∆( A)

{βn⟨π, g n⟩ − Dh(π, π n)} .

Proposition 5.3 (Standard admissible actors) . Let βn = β0n−c with c ∈ (0 , 1) . FTRL is admissible when h is lower semicontinuous, strongly convex with respect to some norm, and has finite oscillation 

sup π∈∆( A) h(π) − inf π∈∆( A) h(π) < ∞. Online mirror ascent is admissible when h is differentiable, strongly convex, and max a∈A sup π∈∆( A) Dh(ea, π ) < ∞, where ea is the unit mass on a. The corresponding optimistic variants are also admissible when their predictable gain forecasts are uniformly bounded in the associated dual norm. 

The proposition covers several familiar actors. For h(π) = P 

> a

π(a) log π(a), FTRL is Hedge. For h(π) = ∥π∥22/2, FTRL is Euclidean dual averaging and mirror ascent is projected gradient ascent. In addition, we show that the corresponding optimistic variants, including optimistic Hedge and the Euclidean optimistic mirror update, are also admissible when based on uniformly bounded predictable gain forecasts; their updates and verification are given in the proof of Proposition 5.3. 

Remark 5.4 (Heterogeneous actor choices) . Each player–state pair ( i, s ) may use a different admissible actor, with its own regularizer, forecast, and admissibility constants. The analysis depends only 16 Algorithm 1 Asynchronous no-regret actor–critic with fixed exploration  

> 1:

Fix b ∈ ( 12 , 1), c ∈ (b, 1), α0, η 0 ∈ (0 , 1], β0 > 0, and ε ∈ (0 , 1). For every s ∈ S , a ∈ A , and 

n ≥ 1, set 

ηs,n = η0n−b, αs,a,n = α0n−b, βs,n = β0n−c. 

> 2:

For every i, s, and a, initialize Qi, 1(s, a ) ∈ [0 , H ]. For every i and s, initialize Vi, 1(s) ∈ [0 , H ], choose an admissible actor Ai,s with schedule ( βs,n )n≥1, and set πi, 1(· | s) equal to the decision it produces before its first local gain.  

> 3:

for t = 1 , 2, . . . do  

> 4:

Observe s = st and set n = nt(s).  

> 5:

For every player i and state r, set 

pi,t (· | r) = (1 − ε)πi,t (· | r) + εUi(· | r).

Independently across players, draw ai,t ∼ pi,t (· | s).  

> 6:

Each player i observes at, ui(s, a t), and st+1 , and forms Qi,t (s, (·, a −i,t )).  

> 7:

Let k = nt(s, a t). For every player i, simultaneously update 

Qi,t +1 (s, a t) = (1 − αs,a t,k )Qi,t (s, a t) + αs,a t,k 

 ui(s, a t) + γV i,t (st+1 ),Vi,t +1 (s) = (1 − ηs,n )Vi,t (s) + ηs,n ⟨πi,t (· | s), Q i,t (s, (·, a −i,t )) ⟩ .

All other critic coordinates are unchanged.  

> 8:

For every player i, supply Qi,t (s, (·, a −i,t )) as the nth gain to Ai,s , advance this actor to local round n + 1, and set πi,t +1 (· | s) equal to the resulting decision. For every r̸ = s, set 

πi,t +1 (· | r) = πi,t (· | r).  

> 9:

end for 

on the resulting finite collection of constants in (11)–(12). Adaptive or time-varying regularized learners are therefore also covered whenever they satisfy these two inequalities. For each ( i, s ), local round n of actor Ai,s occurs at date tn(s). The policy πi,t n(s)(· | s) is the actor’s nth decision, and the gain supplied on that round is Qi,t n(s)(s, (·, a −i,t n(s))). After receiving this gain, the actor produces its ( n + 1)st decision. The algorithm uses the local clocks defined in Section 2. At the nth visit to state s, the coordinate 

Vi(s) uses the state clock n, whereas Qi(s, a ) uses the number of observations of the particular state–action pair ( s, a ). Thus, unvisited coordinates do not change and different coordinates evolve on endogenous clocks. On their respective local clocks, the critic steps are of order n−b, while (12) makes the target-policy movement of order n−c. Since c > b , the actor movement is asymptotically negligible on the critic timescale. Together with the communication condition and the coverage generated by (9), this separation allows us to show that the critic tracks its moving fixed point even when the target policy does not converge and satisfies only the mild conditions in Definition 5.2. 17 To state the main results of this section, freeze a target profile Π, let X = ( Q, V ) and define 

GQε (Π , X )( i, s, a ) := ui(s, a ) + γ X

> s′

P (s′ | s, a )Vi(s′), (13) 

GVε (Π , X )( i, s ) := Ea−i∼pε 

> −i(Π)( ·| s)

[⟨πi(· | s), Q i(s, (·, a −i)) ⟩] , (14) where pεi (Π)( · | s) := (1 − ε)πi(· | s) + εUi(· | s) and pε

> −i

(Π) := N  

> j̸=i

pεj (Π). Write Gε := ( GQε , G Vε ). These two maps are the conditional means of the Q and V update targets when Π is held fixed. Let Xt := ( Qt, V t) ∈ X := [0 , H ]m, where m is the number of critic coordinates. Proposition D.1 shows that Gε(Π , ·) is a contraction in a weighted sup norm. Denote its unique fixed point by Λε(Π) = ( QΠ 

> ε

, V Π 

> ε

). At ε = 0, behavior and target policies coincide, so the fixed-point equations reduce to (2) and Λ 0(Π) = Λ(Π) := ( QΠ, V Π). We can now state the main learning guarantee. 

Theorem 5.5 (No-regret actor–critic AMCR guarantee) . Under Assumption 5.1, choose any admissible actor for each player–state pair (i, s ). There are finite constants Creg , κ AM , independent of ε, such that Algorithm 1 satisfies, almost surely, 

∥Xt − Λε(Π t)∥∞ −→ 0, (15) lim sup  

> T→∞

1

T

X

> s,i

Rtar  

> i,s

(Ns(T )) ≤ Creg ε, (16) lim sup  

> T→∞

GAMCR  

> T

(p1: T ) ≤ κAM ε. (17) The first conclusion is our main two-timescale result. To prove this, we show that the moving critic recursion is an asymptotically vanishing perturbation of an asynchronous contraction toward Λε(Π t). The admissible-actor regret bound is then used to give (16); combining this estimate with critic tracking in Theorem 3.6 yields the AMCR guarantee. 

Remark 5.6 . Recall that the behavior path p1: T describes the implemented play, whereas Π 1: T is the target-policy path used in the regret–evaluation argument. We note that Lemma D.2 implies that every AMCR bound for either path transfers to the other at an additional O(ε) cost. We therefore state the equilibrium guarantees for the behavior path that is actually played by the algorithms. The same discrete-time analysis used to prove Theorem 5.5 also yields a finite-horizon rate. 

Theorem 5.7 (Finite-time AMCR rate) . Suppose Assumption 5.1 holds, fix ε ∈ (0 , 1) , and choose any admissible actor for each player–state pair (i, s ). For every r ∈ (0 , 1/4) , run Algorithm 1 with 

b = 1 − 2r and c = 1 − r. There is a finite constant Cε,r , independent of T , such that, for every 

T ≥ 2,

EGAMCR  

> T

(p1: T ) ≤ κAM ε + Cε,r T −r. (18) Since r can be chosen arbitrarily close to 1 /4, the largest limiting exponent permitted in the last theorem has supremum 1 /4. 18 Challenges and technical overview of the analysis. The proof of Theorems 5.5 and 5.7 combines three features that are usually treated separately in stochastic-approximation analyses if at all. The critic is asynchronous and uses endogenous local clocks: a value coordinate is updated only when its state is visited, and an action-value coordinate only when the corresponding state–action pair is observed. At the same time, its evaluation target moves continuously because the statewise actors keep updating, and the theorems only assume mild conditions on the actors in order to capture a wide range of no-regret algorithms. Finally, the observations are generated by an exploratory behavior policy that creates a systematic bias. We are not aware of a standard two-timescale stochastic-approximation theorem that directly covers this combination while providing both the almost-sure moving-target tracking and the finite-time estimates. The analysis proceeds through a direct blockwise tracking argument. We first freeze the target-policy profile. For every fixed profile, the conditional-mean critic map is a contraction and has a unique fixed point. This fixed point is the evaluation that the critic would approach if the actors stopped moving. We then show that it varies Lipschitz continuously with the target profile and differs from the canonical evaluation used in AMCR only by the exploration-induced discrepancy between the behavior and target policies. The next issue is to ensure that every critic coordinate is updated sufficiently often. This cannot be inferred from action exploration alone because the state process is generated by the game and the evolving policies. The communication assumption and fixed exploration together imply that, after every possible history, each state–action pair has a uniformly positive probability of being observed within a bounded number of stages. A concentration argument converts this hitting property into uniform visitation guarantees over moving time intervals. Consequently, on every sufficiently late block of the appropriate length, each critic coordinate receives a cumulative update bounded away from zero. The block length is chosen to match the critic timescale. Over one block, each critic coordinate receives sufficient cumulative update to ensure a uniform contraction toward the fixed point associated with the target policy at the beginning of the block. At the same time, the block remains short enough that the target policy changes only by a vanishing amount. Thus, over each block, the critic makes substantial progress toward the current evaluation target while that target is nearly frozen. The accumulated critic noise is also small. Its treatment requires some care: the realized action both determines which action-value coordinate is updated and generates the value-critic innovation, while the subsequent state transition generates the action-value innovation. We separate these two sources by splitting each stage into two half-steps and constructing a valid martingale decomposition that allows maximal concentration inequalities to control the noise accumulated over the block. Combining these ingredients gives the central recursion: the tracking error at the end of a block is at most a fixed fraction of the error at its beginning, plus the movement of the target and the accumulated martingale noise. Iterating this recursion proves almost-sure tracking, while retaining the expected sizes of the two perturbations gives the finite-time rate. The actor argument is then carried out separately on the visit clock of each state and is more 19 standard. Each observed score is decomposed into its conditional mean and a martingale difference. The pathwise regret guarantee of each admissible actor, together with a standard martingale analysis yields sublinear target-policy regret up to this exploration error. Once critic tracking and target-policy regret have been established, Theorem 3.6 gives the AMCR guarantee. 

# 6 Policy gradient algorithm and AMCR guarantees 

In this section we obtain exact AMCR with an episodic projected stochastic policy gradient method. We use the stochastic-game primitives from Section 2 with an episodic timing protocol. Fix an episode-start distribution ρ0 ∈ ∆( S). At the beginning of each episode, the environment draws the initial state from ρ0. At every stage of the episode, actions and payoffs are generated as in Section 2. After the payoff is realized, the episode ends with probability 1 − γ, independently of the past. If it continues, the next state is drawn from P (· | s, a ). The behavior profile remains fixed throughout the episode. After termination, all players update, and the next episode begins from a new draw of the initial state. Because continuation occurs independently with probability γ after each payoff, for every fixed behavior profile and initial state, the expected undiscounted episode return equals the corresponding γ-discounted value. At each played stage, player i observes the current state, her own action, her own payoff, and the next state when the episode continues. Such episodic policy-update protocols are standard in theoretical multi-agent policy-gradient analyses; see Daskalakis et al. (2020); Leonardos et al. (2022); Ding et al. (2022). We note two main differences between Algorithm 1 and Algorithm 2 in terms of feedback and updates. Algorithm 2 uses only each player’s own payoff feedback, whereas Algorithm 1 also observes the realized joint action. In addition, Algorithm 2 updates policies only at common episode boundaries, while Algorithm 1 learns continuously along a single trajectory. Despite their differences, Algorithm 1 and Algorithm 2 obtain AMCR through the same underlying principle: they combine evaluation of the current joint policy with statewise no-regret learning. Algorithm 1 performs the evaluation explicitly through its critic and feeds the resulting scores to a no-regret actor. In contrast, Algorithm 2 evaluates the current joint policy implicitly from frozen-policy episode returns and uses these returns to form policy-gradient estimates; the projected-gradient update then provides the corresponding statewise regret control. Algorithm 2 follows the standard REINFORCE approach to estimating policy gradients and uses the same direct parameterization, likelihood-ratio estimator, and exploratory policy considered in Leonardos et al. (2022). Index all played stages consecutively across episodes by t = 1 , 2, . . . . If stage t belongs to episode 

e, set pt := pe, where pe is the behavior profile held fixed throughout that episode, and let st and 

at denote the corresponding state and joint action. Thus ( pt, s t, a t)t≥1 describes the sequence of played stages across episodes. Throughout this section, GAMCR  

> T

is evaluated on the first T stages of this sequence. Let KE := PEe=1 (Le + 1) be the total number of played stages in the first E episodes where Le is the terminal stage of episode e defined in Algorithm 2. Set Amax := max i |Ai|.20 Algorithm 2 Projected REINFORCE under own-payoff feedback  

> 1:

Choose positive nonincreasing sequences ( εe)e≥1 ⊂ (0 , 1) and ( ηe)e≥1. For every player i,initialize πi, 1 ∈ Q 

> s∈S

∆( Ai).  

> 2:

for e = 1 , 2, . . . do  

> 3:

Every player i sets 

pi,e (· | s) := (1 − εe)πi,e (· | s) + εeUi(· | s), s ∈ S , (19) and let pe = ( pi,e )Ni=1 . 

> 4:

Independently of the past, the environment draws se, 0 ∼ ρ0 and, independently, an unobserved terminal index Le satisfying 

P(Le = ℓ) = (1 − γ)γℓ, ℓ = 0 , 1, . . . . (20)  

> 5:

for k = 0 , . . . , L e do  

> 6:

Every player independently draws ai,e,k ∼ pi,e (· | se,k ).  

> 7:

Each player i observes se,k , ai,e,k , and ui(se,k , a e,k ). If k < L e, the environment draws 

se,k +1 ∼ P (· | se,k , a e,k ).  

> 8:

end for  

> 9:

Every player i forms 

bGi,e := 

> Le

X

> k=0

ui(se,k , a e,k )

! LeX

> k=0

∇πi log pi,e (ai,e,k | se,k )

!

, (21) where ∇πi denotes the ambient Euclidean gradient with respect to the coordinates ( πi(ai |

s)) s∈S ,a i∈Ai , evaluated at πi,e . 

> 10:

Every player updates before the next episode by Euclidean projection: 

πi,e +1 := Proj Q 

> s∈S ∆( Ai)



πi,e + ηe bGi,e 



. (22)  

> 11:

end for 

We now show that for general stochastic games, Algorithm 2 generates AMCR. 

Theorem 6.1. For any ρ0 ∈ ∆( S), run Algorithm 2 with εe = ( e + 1) −1/3 and ηe = p|S| (e +1) −2/3/(Amax H2). There is a universal constant C < ∞ such that, for every E ≥ 1,

EGAMCR 

> KE

 ≤ CN HA 3/2max 

p|S| E−1/3. (23) 

Moreover, GAMCR  

> T

→ 0 almost surely. 

Theorem 6.1 and Proposition 4.2 imply that Algorithm 2 asymptotically generates MBCCE, i.e., that every weak accumulation point of the empirical distribution of the realized state and the behavior profile used at that stage, 1

> T

PTt=1 δ(st,p t) is an MBCCE a.s. 21 7 Further properties of MBCCE 

Beyond the comparison in Example 1.1, we next discuss two further properties of MBCCE that illustrate its parallels with coarse correlated equilibrium in finite normal-form games: its linear structure and its welfare guarantees under smoothness. Let Π 1, . . . , ΠK be fixed candidate profiles, obtained, for example, from a discretization of the policy space or an observed policy path, and fix a state distribution d ∈ ∆( S). For each Π k, first evaluate the policy by solving (2). Once these evaluations are fixed, the probabilities assigned to the candidate profiles can be chosen by the following linear program: min 

> x,z

X

> s∈S
> N

X

> i=1

zi,s 

subject to 

> K

X

> k=1

xs,k Ea∼Πk (·| s)

h

QΠk 

> i

(s, (bi, a −i)) − QΠk 

> i

(s, a )

i

≤ zi,s ∀i, s, b i,

> K

X

> k=1

xs,k = d(s) ∀s, xs,k ≥ 0, zi,s ≥ 0 ∀i, s, k. 

(24) Here xs,k is the joint probability assigned to state s and policy profile Π k. Thus, conditional on a state with d(s) > 0, the ratios xs,k /d (s) give the posterior probabilities over the candidate continuation policies. The left-hand side of the first constraint is the deviation gain in (8) multiplied by the probability of the state, while zi,s bounds its positive violation for player i at state s. Hence, the value of (24) is the smallest sum, over player–state pairs, of the largest state-probability-weighted positive obedience violation attainable by a distribution supported on {Π1, . . . , ΠK } with state marginal d.8

MBCCE also preserves the welfare implications of smoothness developed in Roughgarden (2009): a pointwise deviation inequality transfers to equilibrium outcomes through coarse obedience. Here the deviation is chosen after observing the public state, and its payoff is evaluated by the corresponding Q-value. The difference from the static setting is that a current action can also change the next-state distribution and therefore continuation payoffs. The following definition separates the usual smoothness term from a nonnegative residual ξ that bounds this dynamic effect. For every state s, fix a⋆(s) ∈ arg max a′∈A 

PNi=1 ui(s, a ′).                           

> 8Proposition 4.2 is the empirical counterpart of this linear program. At horizon T, let Π 1, . . . , ΠKbe the distinct profiles appearing in Γ 1: T, set d(s) = Ns(T)/T , and fix
> xs,k := {t≤T:st=s, Γt= Π k}
> T.
> With these empirical probabilities fixed, minimizing (24) only over zgives exactly GAMCR
> T(Γ 1: T). Thus AMCR uses the empirical weights generated by the learning path, whereas the linear program is free to reweight the same candidate profiles to find the distribution with the smallest Markov-BCCE obedience violation.

22 Definition 7.1 (Markov smoothness) . Fix λ > 0, ν ≥ 0, and a nonnegative bounded measurable function ξ : S × C × A → R+. The stochastic game is ( λ, ν, ξ )-Markov smooth if, for every s ∈ S ,Π ∈ C , and a ∈ A ,

> N

X

> i=1

QΠ 

> i

(s, (a⋆i (s), a −i)) − QΠ 

> i

(s, a ) ≥ λ

> N

X

> i=1

ui(s, a ⋆(s)) 

− (1 + ν)

> N

X

> i=1

ui(s, a ) − ξ(s, Π, a ).

(25) When γ = 0 and ξ = 0, condition (25) is equivalent to the usual utility-smoothness condition 

> N

X

> i=1

ui(s, (a⋆i (s), a −i)) ≥ λ

> N

X

> i=1

ui(s, a ⋆(s)) − ν

> N

X

> i=1

ui(s, a ).

Thus Proposition 7.2 below recovers the familiar welfare factor λ/ (1 + ν). Although (25) involves continuation values, Section 8.1 shows that it follows from ordinary statewise smoothness and a bound on the effect of unilateral actions on transitions. 

Proposition 7.2. Suppose (25) holds. Then every MBCCE μ satisfies, for each state in the support of its state marginal, 

EΠ∼μ(·| s)

> a∼Π( ·| s)

" NX

> i=1

ui(s, a )

#

≥ 11 + ν λ

> N

X

> i=1

ui(s, a ⋆(s)) − EΠ∼μ(·| s)

> a∼Π( ·| s)

[ξ(s, Π, a )] 

!

. (26) 

# 8 Markov coarse correlated equilibrium 

In this section we study the connection between MBCCE and MCCE. MCCE allows a player to replace her entire stationary policy and evaluates the deviation against one stationary Markov policy. For a stationary statewise joint-action law σ = ( σs)s∈S ∈ Q 

> s

∆( A), let V σi denote the canonical value defined by (2). For a deterministic Markov policy βi : S → Ai, let V βi,σ −i 

> i

(s) be player i’s value from state s when she uses βi and the opponents use the marginals σ−i. By standard dynamic programming arguments a deterministic stationary best response to the opponents exists in our setting. 

Definition 8.1 (Stationary MCCE) . For a stationary joint-action law σ, define Gap MCCE (σ) := (1 − γ) max 

> i, s ∈S
> βi:S→ Ai



V βi,σ −i 

> i

(s) − V σi (s)

 

> +

. (27) The law σ is a δ-MCCE if Gap MCCE (σ) ≤ δ.Definition 8.1 uses the perfect criterion, which tests every initial state and agrees with Definition 1 in Daskalakis et al. (2023). We now give the exact connection of MCCE with MBCCE. Let 23 μ ∈ ∆( S × C ) assign positive probability to every state and define its statewise average action law by 

σsμ(a) := EΠ∼μ(·| s)[Π( a | s)] , s ∈ S , a ∈ A . (28) Let ωβi,σ −i 

> i,s 0

(s) denote the normalized discounted probability of visiting state s when play starts from 

s0, player i uses βi, and the opponents use σ−i:

ωβi,σ −i 

> i,s 0

(s) := (1 − γ)

> ∞

X

> m=0

γm Pr 

> βi,σ −i

(Sm = s | S0 = s0). (29) Thus ωβi,σ −i 

> i,s 0

is a probability distribution on S and let the one-step deviation gain under σμ be 

gμi (s, b i) := X

> a−i

σsμ, −i(a−i)Qσμ 

> i

(s, (bi, a −i)) − V σμ 

> i

(s). (30) Note that the following decomposition holds 

gμi (s, b i) = EΠ∼μ(·| s)

> a∼Π( ·| s)

h

QΠ 

> i

(s, (bi, a −i)) − QΠ 

> i

(s, a )+ γ⟨P (· | s, (bi, a −i)) − P (· | s, a ), V σμ 

> i

− V Π 

> i

⟩

i

. (31) The Q-difference in (31) is exactly the deviation gain in (8). The second term is the difference between the continuation benchmarks: MBCCE retains V Π 

> i

, whereas MCCE uses V σμ 

> i

after the hidden policies are averaged state by state. 

Lemma 8.2. For every μ with full-support state marginal, 

Gap MCCE (σμ) = max 

> i, s 0∈S
> βi:S→ Ai

X

> s∈S

ωβi,σ μ, −i 

> i,s 0

(s)gμi (s, β i(s)) 

!

> +

. (32) Lemma 8.2 writes the gain from a stationary policy deviation as an occupancy-weighted sum of one-stage gains. MBCCE controls the Q-difference in (31) at every state. A stationary MCCE deviation selects one action at every state, and (32) weights these gains by the states reached under that deviation. If μ is an MBCCE, only the continuation term in (31) can increase the MCCE gap. To isolate this remaining term, define 

C(μ) := max 

> i, s 0∈S
> βi:S→ Ai

X

> s∈S

ωβi,σ μ, −i 

> i,s 0

(s)EΠ∼μ(·| s)

> a∼Π( ·| s)

h

P (· | s, (βi(s), a −i)) − P (· | s, a ), V σμ 

> i

− V Π

> i

i!

> +

. (33) 24 Because every discounted occupancy is a probability distribution, (31) and Lemma 8.2 imply Gap MCCE (σμ) ≤ max 

> i,s,b i

EΠ∼μ(·| s)

> a∼Π( ·| s)

[QΠ 

> i

(s, (bi, a −i)) − QΠ 

> i

(s, a )] 

!

> +

+ γC(μ). (34) Thus, a full-support MBCCE satisfies Gap MCCE (σμ) ≤ γC(μ). Markov BCCE evaluates a unilateral deviation using the continuation value associated with the policy Π, whereas MCCE uses the continuation value associated with the statewise average law σμ. Hence, an MBCCE induces an approximate MCCE whenever replacing V Π 

> i

by V σμ 

> i

changes the payoff from a unilateral deviation by only a small amount. For the empirical distributions generated by our algorithms, this compares the continuation value under the policy used at each visit to a state with the continuation value under the statewise average of the policies used at visits to that state. Therefore, our algorithms achieve approximate MCCE whenever this continuation-value replacement has a small effect on unilateral-deviation payoffs. We next make this connection precise and then provide a class of games where this effect is small. Take Γ t = pt in Proposition 4.2 and let bμT := T −1 PTt=1 δ(st,p t). At horizons for which every state has been visited, define 

σsT (a) := 1

Ns(T )

X 

> t∈Ts(T)

pt(a | s), bσsT (a) = 1

Ns(T )

X 

> t∈Ts(T)

1{at = a}.

By construction, σT = σbμT . Thus σT is the stationary action law induced by the empirical state–policy distribution, whereas bσT is constructed from realized actions. 

Corollary 8.3. Consider either of the following cases. (i) Assumption 5.1 holds and Algorithm 1 is run with fixed exploration ε and any admissible actor for each player–state pair. (ii) Algorithm 2 is run under the schedules in Theorem 6.1, and the episode-start distribution has full support: ρ0(s) > 0 for every s ∈ S .There is a finite constant κM, independent of ε, such that, almost surely, 

lim sup  

> T→∞

Gap MCCE (bσT ) ≤



κMε + γ lim sup T →∞ C(bμT ), in case (i) ,γ lim sup T →∞ C(bμT ), in case (ii) .

(35) 

8.1 Applications: transition-aggregative games 

Many economic and operations models have transitions that depend on a weighted aggregate of individual actions. In large games, a single player’s action can then have only a small effect on the next-state distribution, even when stage payoffs exhibit unrestricted strategic interactions. The previous section shows that this is precisely the type of setting in which our algorithms can achieve approximate MCCE. We now formalize this idea through transition-aggregative stochastic games. 25 Definition 8.4 (Transition-aggregative stochastic game) . The game is transition-aggregative if there are weights w1, . . . , w N ≥ 0 with P 

> j

wj = 1 and the following property. For every state s,there exist ds ∈ N, maps ϕj,s : Aj → Rds , a map Ps from feasible aggregate vectors into ∆( S), and 

Ls < ∞ such that 

P (· | s, a ) = Ps



> N

X

> j=1

wj ϕj,s (aj )

 , ∀a ∈ A , (36) TV( Ps(x), Ps(y)) ≤ Ls∥x − y∥, for all feasible x, y. (37) For each i and s, let ∆ i,s := max ai,b i∈Ai ∥ϕi,s (bi) − ϕi,s (ai)∥. Then Lswi∆i,s bounds the change in the next-state law caused by a unilateral action at s. For the sharper bound below, we also use the common minorization condition 

P (· | s, a ) ≥ ηq (·) ∀s, a, (38) componentwise, for some q ∈ ∆( S) and η ∈ (0 , 1]. 

Proposition 8.5 (Transition influence and approximate MCCE) . Under either case of Corollary 8.3, suppose the game is transition-aggregative. In case (i), almost surely, 

lim sup  

> T→∞

Gap MCCE (bσT ) ≤ κMε + 2γ

1 − γ max  

> i,s

Lswi∆i,s .

In case (ii), the same bound holds with κMε deleted. Under (38) , the denominator 1 − γ can be replaced by 1 − γ(1 − η).

Thus, for a sequence of games with wi = 1 /N , if max i,s Ls∆i,s is bounded independently of N ,an O(ε)-AMCR guarantee yields an O(ε + 1 /N )-approximate MCCE. In particular, exact AMCR yields an O(1 /N )-approximate MCCE. We now provide natural examples of transition-aggregative games. The examples below display case (i). For Algorithm 2, delete κMε from the displayed bounds. 

Example 8.6 (Dynamic routing) . Let Es be the set of network edges available at state s. For a route ai ∈ Ai, define ϕi,s (ai) ∈ { 0, 1}|Es| by [ ϕi,s (ai)] e = 1{e is used by route ai}. If wi is player i’s share of total demand, then the eth coordinate of P 

> i

wiϕi,s (ai) is the fraction of demand using edge 

e. The transition law can therefore describe how current traffic affects future congestion. Use the ℓ1 norm and suppose every feasible route uses at most ℓ edges. Changing routes removes at most ℓ edges and adds at most ℓ edges, so ∆ i,s ≤ 2ℓ. Proposition 8.5 gives lim sup  

> T→∞

Gap MCCE (bσT ) ≤ κMε + 4γℓ 

1 − γ max  

> i,s

Lswi.

Hence, if max i wi = O(1 /N ) and max s Ls = O(1) as N → ∞ we get O(ε + 1 /N )-approximate MCCE. We note that stage payoffs may include nonlinear congestion costs, heterogeneous tolls, and 26 other player-specific parameters. 

Example 8.7 (Dynamic common-pool resource) . Let S = {g, d} denote good and degraded resource conditions. Each Ai is a finite menu of resource-use decisions, and action ai induces a depletion intensity xi(ai) ∈ [0 , 1]. Let m(a) := N −1 P 

> j

xj (aj ) and suppose 

P (d | g, a ) = αg + bgm(a), P (g | d, a ) = αd − bdm(a),

where bg, b d ≥ 0. Greater aggregate depletion makes degradation more likely and recovery less likely. Assume every transition probability belongs to [ p, 1 − p] for every feasible aggregate, where 

p ∈ (0 , 1/2]. Take wi = 1 /N , ϕi,s (ai) = xi(ai), L = max {bg, b d}, and ∆ i,s ≤ 1. Moreover, P (· | s, a ) ≥ 2p q 

for q = (1 /2, 1/2), so (38) holds with η = 2 p. Proposition 8.5 therefore gives lim sup  

> T→∞

Gap MCCE (bσT ) ≤ κMε + 2γL N [1 − γ(1 − 2p)] almost surely .

Thus, we get O(ε + 1 /N )-approximate MCCE in this example too. Rewards may include nonlinear revenue, state-dependent operating costs, and asymmetric strategic interactions. The same argument applies to any finite collection of resource-quality states when P (· | s, a ) = Ps(m(a)) is Lipschitz. The final result in this section shows that, when each statewise stage game is smooth, the transition-aggregative condition also implies the Markov smoothness condition in Proposition 7.2, and therefore yields welfare guarantees for MBCCE. Routing games of the type considered above are examples in which the stage game is smooth under suitable utility specifications. 

Corollary 8.8 (Welfare under transition aggregation) . Suppose the game is transition-aggregative and, for some λ > 0 and ν ≥ 0, every statewise stage game satisfies 

> N

X

> i=1

ui(s, (a⋆i (s), a −i)) ≥ λ

> N

X

> i=1

ui(s, a ⋆(s)) − ν

> N

X

> i=1

ui(s, a ) ∀s, a. (39) 

Then every MBCCE μ satisfies, at each state in the support of its state marginal, 

EΠ∼μ(·| s)

> a∼Π( ·| s)

" NX

> i=1

ui(s, a )

#

≥ λ

1 + ν

> N

X

> i=1

ui(s, a ⋆(s)) − γHL s

1 + ν

> N

X

> i=1

wi∆i,s . (40) 

Under (38) , H can be replaced by [1 − γ(1 − η)] −1.

# 9 Conclusions 

In this paper, we introduce adaptive Markov coarse regret (AMCR), a pathwise regret criterion for sequences of stationary policy profiles, and Markov Bayes coarse correlated equilibrium (MBCCE), 27 a static distributional solution concept over states and stationary policy profiles. We show that vanishing AMCR implies that every accumulation point of the empirical state–policy distribution is an MBCCE, and that achieving AMCR reduces to statewise external regret and accurate evaluation of the current joint policy. We establish these properties for a decentralized asynchronous actor–critic algorithm and an episodic multi-agent projected policy-gradient method. Finally, we relate MBCCE to stationary MCCE and use this connection to obtain approximate MCCE guarantees for classes of stochastic games in which individual actions have limited effects on state transitions. Several research questions remain open; we discuss two here. First, when a vanishing-AMCR path does converge, its limit is an MPE under state-space coverage, raising the question of when standard multi-agent reinforcement-learning algorithms possess such last-iterate guarantees. Existing results obtain convergence guarantees under additional game structure such as for policy-gradient methods in Markov potential games Leonardos et al. (2022), but last-iterate results remain limited. Second, our analysis is tabular. Extending the AMCR–MBCCE framework to large state spaces, where reinforcement-learning algorithms rely on function approximation for policy evaluation and optimization, is an important direction for bringing these equilibrium guarantees closer to modern reinforcement learning. 28 A Example 1.1 calculations 

Proof of the claims in Example 1.1. We first verify the normal-form comparison. Let λab denote the probability of action profile ( a, b ) ∈ { 0, 1}2, where the first coordinate is player 1’s action. Following the draw gives each player expected payoff λ01 + λ10 . If player 1 deviates to action 1, her expected payoff is λ00 + λ10 ; if she deviates to action 0, it is λ01 + λ11 . Hence her CCE inequalities are 

λ01 ≥ λ00 and λ10 ≥ λ11 . The corresponding inequalities for player 2 are λ10 ≥ λ00 and λ01 ≥ λ11 .Thus, the distribution is a CCE if and only if 

λ01 ≥ λ00 , λ10 ≥ λ00 , λ10 ≥ λ11 , λ01 ≥ λ11 .

Let q1 := λ10 + λ11 and q2 := λ01 + λ11 be the induced probabilities of action 1. Using 

P 

> a,b

λab = 1, 2 − (2 q1 + q2) = 2 λ00 + λ01 − λ11 , 2 − (q1 + 2 q2) = 2 λ00 + λ10 − λ11 ,

(2 q1 + q2) − 1 = λ10 + 2 λ11 − λ00 , (q1 + 2 q2) − 1 = λ01 + 2 λ11 − λ00 .

The CCE inequalities therefore imply 2q1 + q2 ≤ 2, q1 + 2 q2 ≤ 2, 2q1 + q2 ≥ 1, q1 + 2 q2 ≥ 1. (41) Conversely, suppose ( q1, q 2) ∈ [0 , 1] 2 satisfies (41). If q1 + q2 ≤ 1, take ( λ00 , λ 10 , λ 01 , λ 11 ) = (1 −

q1 −q2, q 1, q 2, 0). The two lower inequalities in (41) are exactly λ10 ≥ λ00 and λ01 ≥ λ00 ; the other two CCE inequalities are automatic. If q1 + q2 ≥ 1, take ( λ00 , λ 10 , λ 01 , λ 11 ) = (0 , 1 − q2, 1 − q1, q 1 + q2 − 1). The two upper inequalities in (41) are exactly λ10 ≥ λ11 and λ01 ≥ λ11 ; the other two CCE inequalities are automatic. Hence (41) characterizes the projected CCE set. Its vertices are (1 , 0), (2 /3, 2/3), (0 , 1), and (1 /3, 1/3). The Nash marginal profiles are (1 , 0), (0 , 1), and (1 /2, 1/2) by the best-response threshold 1 /2. We next consider the stochastic investment game. Fix a stationary product profile Π. Its Bellman equations are 

V Π 

> i

(0) = − 12 πi(1 | 0) + 45

 π1(1 | 0) + π2(1 | 0) 3 V Π 

> i

(1) + 



1 − π1(1 | 0) + π2(1 | 0) 3



V Π 

> i

(0) 



,V Π 

> i

(1) = 1 − 12 πi(1 | 1) + 45

 34 V Π 

> i

(1) + 14 V Π 

> i

(0) 



.

Solving these two equations for the value difference gives 

V Π 

> i

(1) − V Π 

> i

(0) = 1 + 12 [πi(1 | 0) − πi(1 | 1)]  

> 25

+ 415 [π1(1 | 0) + π2(1 | 0)] .

For j̸ = i, changing the current state-0 action from 0 to 1 lowers the current payoff by 1 /2 and raises the probability of state 1 by 1 /3, independently of player j’s current action. Hence, for every 29 aj ∈ { 0, 1},

QΠ 

> i

(0 , (1 , a j )) − QΠ 

> i

(0 , (0 , a j )) = 1 − 2πj (1 | 0) − 2πi(1 | 1) 6 + 4 π1(1 | 0) + 4 π2(1 | 0) . (42) At state 1, changing action 0 to action 1 has gain −1/2, because the action does not affect the transition law. 

MPE. The one-deviation principle implies that every MPE satisfies πi(1 | 1) = 0 for both players. The denominator in (42) is positive, so at state 0 

πi(1 | 0) ∈



{1}, πj (1 | 0) < 12 ,

{0}, πj (1 | 0) > 12 ,

[0 , 1] , πj (1 | 0) = 12 .

Applying these conditions to both players yields exactly the state-0 marginal profiles (1 , 0), (0 , 1), and (1 /2, 1/2). Conversely, each of these profiles, together with πi(1 | 1) = 0 for both players, satisfies the statewise one-deviation conditions and is therefore an MPE. 

MCCE. Let σ be a stationary statewise joint-action law, and let ¯Πσ be the stationary product profile with the same statewise marginals, so ¯ πσi (1 | s) = σsi (1). Under σ, player i’s expected current payoff at state s is s − 12 σsi (1). Moreover, Pσ(1 | 0) = [ σ01 (1) + σ02 (1)] /3 and Pσ(1 | 1) = 3 /4. These are exactly the expected current payoffs and transition probabilities induced by ¯Πσ. Hence V σi = V ¯Πσ 

> i

.Now fix a deterministic stationary deviation βi : S → { 0, 1} and let j̸ = i. Against either 

σ−i or ¯Πσ

> −i

, the deviating player’s current payoff at state s is s − 12 βi(s). The induced transition probabilities are also the same: at state 0 they equal [ βi(0) + σ0 

> j

(1)] /3, and at state 1 they equal 3/4. Therefore V βi,σ −i 

> i

= V βi, ¯Πσ 

> −i
> i

for every deterministic stationary deviation βi.It follows that, for every player i, initial state s, and deterministic stationary deviation βi,

V βi,σ −i 

> i

(s) − V σi (s) = V βi, ¯Πσ 

> −i
> i

(s) − V ¯Πσ 

> i

(s).

Thus the MCCE inequalities for σ are exactly the MPE inequalities for ¯Πσ. A stationary joint-action law is therefore an MCCE if and only if the product profile with the same statewise marginals is an MPE. Consequently, its state-0 marginals are (1 , 0), (0 , 1), or (1 /2, 1/2), and its state-1 marginals are (0 , 0). 

MBCCE. We first record the implication of MBCCE obedience at state 1. For every stationary profile Π and every current action profile a, QΠ 

> i

(1 , (0 , a −i)) − QΠ 

> i

(1 , a ) = ai/2. Hence, whenever state 1 has positive probability, the MBCCE inequality for the fixed deviation to action 0 implies 

E[ai | s = 1] = 0. Since ai ≥ 0, action 0 is therefore chosen almost surely at state 1. Equivalently, 

πi(1 | 1) = 0 for μ(· | 1)-almost every Π. Notice that this conclusion concerns the distribution of policies conditional on the current state being 1; it does not restrict the off-state components of policies drawn conditional on state 0. We now characterize the shaded MBCCE region. For a, b ∈ { 0, 1}, let Π ab prescribe ( a, b ) at 30 state 0 and (0 , 0) at state 1. Fix any full-support state marginal and consider MBCCE distributions whose state-conditional policy distributions are supported on these four profiles. At state 1, every supported profile prescribes (0 , 0), so the obedience conditions hold for any distribution over these profiles. At state 0, write μab := μ(Π ab | 0). Substituting the four profiles into (42), the gain from changing the current action from 0 to 1 is Π00 Π10 Π01 Π11 

player 1 16110 − 110 − 114 

player 2 16 − 110 110 − 114 .

For player 1, deviation to action 1 is relevant only under Π 00 and Π 01 , whereas deviation to action 0 is relevant only under Π 10 and Π 11 . Her two obedience inequalities are therefore 16 μ00 − 110 μ01 ≤ 0and − 110 μ10 + 114 μ11 ≤ 0. The corresponding inequalities for player 2 are symmetric. Since there are only two actions, these are all the state-0 obedience conditions. Equivalently, 

μ01 ≥ 53 μ00 , μ10 ≥ 53 μ00 , μ10 ≥ 57 μ11 , μ01 ≥ 57 μ11 , (43) with μab ≥ 0 and P 

> a,b

μab = 1. Let q1 := μ10 + μ11 and q2 := μ01 + μ11 denote the induced state-0 investment probabilities. From (43), 12 − 12 q1 − 5q2 = 12 μ00 + 7 μ01 − 5μ11 ≥ 0,

12 − 5q1 − 12 q2 = 12 μ00 + 7 μ10 − 5μ11 ≥ 0,

8q1 + 5 q2 − 5 = 3 μ10 − 5μ00 + 8 μ11 ≥ 0,

5q1 + 8 q2 − 5 = 3 μ01 − 5μ00 + 8 μ11 ≥ 0.

Hence every induced marginal pair belongs to [0 , 1] 2 and satisfies 12 q1 + 5 q2 ≤ 12 , 5q1 + 12 q2 ≤ 12 , 8q1 + 5 q2 ≥ 5, 5q1 + 8 q2 ≥ 5. (44) Conversely, suppose ( q1, q 2) ∈ [0 , 1] 2 satisfies (44). If q1 + q2 ≤ 1, take ( μ00 , μ 10 , μ 01 , μ 11 ) = (1 − q1 − q2, q 1, q 2, 0). The two lower inequalities in (44) are exactly μ10 ≥ 53 μ00 and μ01 ≥ 53 μ00 ,while the other two inequalities in (43) are automatic. If q1 +q2 ≥ 1, take ( μ00 , μ 10 , μ 01 , μ 11 ) = (0 , 1−q2, 1−q1, q 1 +q2 −1). The two upper inequalities in (44) are exactly μ01 ≥ 57 μ11 and μ10 ≥ 57 μ11 , while the other two inequalities in (43) are automatic. Therefore, (44) characterizes the projected state-0 MBCCE marginal set within the four-profile family. Its vertices are (1 , 0), (12 /17 , 12 /17), (0 , 1), and (5 /13 , 5/13), which proves the claimed description of the shaded region. 31 B Proofs for Section 3 

This appendix establishes the two ingredients introduced in Section 3. We first solve the frozen-policy Bellman system. We then compare each true deviation gain with the gain computed from the current critic; summing this pointwise comparison proves the regret–tracking principle. 

Proof of Lemma 3.1. Fix a player i. For a statewise joint-action law ρ, define 

rρi (s) := X

> a

ρs(a)ui(s, a ), Pρ(s′ | s) := X

> a

ρs(a)P (s′ | s, a ).

Averaging the first equation in (2) under ρs and using the second gives 

V ρi = rρi + γP ρV ρi . (45) Since Pρ is stochastic, the right-hand side of (45) is a γ-contraction in the sup norm. Hence 

V ρi = ( I − γP ρ)−1rρi =

> ∞

X

> k=0

γkP kρ rρi

is the unique solution of (45). The series representation and 0 ≤ rρi ≤ 1 imply 0 ≤ V ρi ≤ H

componentwise. Given V ρi , the first equation in (2) uniquely determines Qρi . Averaging that equation under ρs

recovers (45), so the tying equation also holds. Moreover, 0 ≤ Qρi (s, a ) ≤ 1 + γH = H. The con-struction is independent across players, which proves existence, uniqueness, and the componentwise bounds. 

Proof of Proposition 3.5. By Proposition 4.2, every weak accumulation point of the empirical state– policy distributions is an MBCCE. Since Γ t → Π⋆, its policy marginal is concentrated on Π ⋆, while the state-coverage condition gives positive probability to every state. Hence its Markov-BCCE obedience conditions reduce to the statewise one-deviation conditions for Π ⋆, so Gap MPE (Π ⋆) = 0. 

Proof of Theorem 3.6. Fix a state s, player i, visit t ∈ Ts(T ), and deviation a′

> i

. The tying equation in (2) gives V Πt 

> i

(s) = Ea∼Πt(·| s)QΠt 

> i

(s, a ). Therefore, 

Ai(Π t; s, a ′

> i

) − gtar  

> i,t

(a′

> i

) − πi,t (· | s), g tar 

> i,t



= Ea−i∼Π−i,t (·| s)

h

QΠt 

> i

(s, (a′

> i

, a −i)) − Qi,t (s, (a′

> i

, a −i)) 

i

+ Ea∼Πt(·| s)

h

Qi,t (s, a ) − QΠt 

> i

(s, a )

i

. (46) Each expectation on the right-hand side of (46) is at most et in absolute value. Thus 

Ai(Π t; s, a ′

> i

) ≤ gtar  

> i,t

(a′

> i

) − πi,t (· | s), g tar  

> i,t

+ 2 et. (47) 32 Sum (47) over t ∈ Ts(T ), maximize over a′

> i

, and use ( x + y)+ ≤ x+ + y for y ≥ 0. By (6), 

max 

> a′
> i

X 

> t∈Ts(T)

Ai(Π t; s, a ′

> i

)



> +

≤ Rtar  

> i,s

(Ns(T )) + 2 X 

> t∈Ts(T)

et.

Summing over ( i, s ) and using that the sets Ts(T ) partition {1, . . . , T } proves (7). 

# C Proofs for Section 4 

Proof of Proposition 4.2. Fix s with Ns(T ) > 0. Conditional on s under bμT , the posterior over policies is bμT (dΠ | s) = Ns(T )−1 P  

> t∈Ts(T)

δΓt (dΠ). Hence, for every i and bi,

Ns(T )

T EΠ∼bμT (·| s)

> a∼Π( ·| s)

QΠ 

> i

(s, (bi, a −i)) − QΠ 

> i

(s, a )

= 1

T

X 

> t∈Ts(T)

Ai(Γ t; s, b i),

where Ea∼Γt(·| s)QΓt 

> i

(s, a ) = V Γt 

> i

(s). Maximizing over bi, taking positive parts, and summing over players and states proves the identity in the proposition. Now suppose GAMCR  

> T

(Γ 1: T ) → 0, and consider any subsequence such that bμTk converges weakly to μ. For every fixed i, s, b i,1

Tk

X 

> t∈Ts(Tk)

Ai(Γ t; s, b i) ≤ G AMCR  

> Tk

(Γ 1: Tk ),

and therefore its upper limit is at most zero. By Lemma D.2, Π 7 → A i(Π; s, b i) is continuous. Since S is finite, weak convergence implies 

Z

> C

Ai(Π; s, b i) μ({s}, d Π) ≤ 0.

For any s with μ({s} × C ) > 0, dividing by the probability of s gives EΠ∼μ(·| s)[Ai(Π; s, b i)] ≤ 0. Using the definition of Ai and V Π 

> i

(s) = Ea∼Π( ·| s)[QΠ 

> i

(s, a )], this is precisely (8). Hence μ is an MBCCE. 

Proof of Proposition 7.2. Fix a state s in the support of the state marginal. Applying (8) with 

bi = a⋆i (s) gives 

EΠ∼μ(·| s)

> a∼Π( ·| s)

QΠ 

> i

(s, (a⋆i (s), a −i)) − QΠ 

> i

(s, a ) ≤ 033 for every player i. Summing over players and taking expectations in (25) therefore yields 0 ≥ λ

> N

X

> i=1

ui(s, a ⋆(s)) − (1 + ν)EΠ∼μ(·| s)

> a∼Π( ·| s)

" NX

> i=1

ui(s, a )

#

− EΠ∼μ(·| s)

> a∼Π( ·| s)

[ξ(s, Π, a )] .

Rearranging proves (26). 

# D Proofs for Section 5 

D.1 Critic recursion and frozen-policy regularity 

The stochastic-approximation representation used in the proof is 

Xt+1 = Xt + At

 Gε(Π t, X t) − Xt + ξt+1 

.

The diagonal matrix At has entry αst,a t,n t(st,a t) on each coordinate Qi(st, a t), entry ηst,n t(st) on each coordinate Vi(st), and zero elsewhere. The nonzero innovation coordinates are 

ξQt+1 (i, s t, a t) := γ Vi,t (st+1 ) − X

> r

P (r | st, a t)Vi,t (r)

!

,ξVt+1 (i, s t) := ⟨πi,t (· | st), Q i,t (st, (·, a −i,t )) ⟩ − GVε (Π t, X t)( i, s t).

and all remaining coordinates are zero. For ω > 0, write 

∥(Q, V )∥ω := max {∥ Q∥∞, ω ∥V ∥∞}.

Proposition D.1 (Frozen critic fixed point) . Let ω = √γ. Uniformly over ε ∈ [0 , 1] :(i) Gε maps C × X into X and is jointly Lipschitz; (ii) the affine map Gε(Π , ·) : Rm → Rm is a √γ-contraction in ∥ · ∥ ω; its unique fixed point Λε(Π) 

belongs to X ;(iii) Π 7 → Λε(Π) is Lipschitz; (iv) Λ0(Π) = Λ(Π) and, for some C∞ 

> Λ

< ∞,

∥Λε(Π) − Λ(Π) ∥∞ ≤ C∞ 

> Λ

ε. (48) 

Proof. If X = ( Q, V ) ∈ X , then 0 ≤ GQε (Π , X ) ≤ 1 + γH = H and 0 ≤ GVε (Π , X ) ≤ H. Hence 

Gε(Π , X ) ∈ X .34 For fixed ( i, s ), let μεi,s (Π) denote the product measure πi(· | s) ⊗j̸ =i pεj (Π)( · | s) that appears in (14). A telescoping expansion of product measures gives 

∥μεi,s (Π) − μεi,s (Π ′)∥1 ≤

> N

X

> j=1

∥πj (· | s) − π′ 

> j

(· | s)∥1.

Linearity of the two blocks and Q ∈ [0 , H ] now imply joint Lipschitz continuity, uniformly in ε.For arbitrary X = ( Q, V ), X ′ = ( Q′, V ′) ∈ Rm,

∥GQε (Π , X ) − GQε (Π , X ′)∥∞ ≤ γ∥V − V ′∥∞ ≤ √γ∥X − X′∥ω,

while 

ω∥GVε (Π , X ) − GVε (Π , X ′)∥∞ ≤ ω∥Q − Q′∥∞ ≤ √γ∥X − X′∥ω.

The same affine formulas define Gε(Π , ·) on Rm, so Banach’s fixed-point theorem gives a unique fixed point there. Since Gε(Π , ·) maps X into itself, this fixed point belongs to X , proving (ii). Let X = Λ ε(Π) and X′ = Λ ε(Π ′). The fixed-point identities and the preceding contraction give (1 − √γ)∥X − X′∥ω ≤ ∥ Gε(Π , X ′) − Gε(Π ′, X ′)∥ω. (49) The product-measure estimate and ∥X′∥∞ ≤ H bound the right-hand side of (49) by a constant times ∥Π − Π′∥1, proving (iii). At ε = 0, the fixed-point equations are exactly (2), so Λ 0(Π) = Λ(Π). With X = Λ ε(Π) and 

Y = Λ(Π), contraction gives (1 − √γ)∥X − Y ∥ω ≤ ∥ Gε(Π , Y ) − G0(Π , Y )∥ω.

Only the opponents’ law in the V block changes. Total-variation tensorization and (9) give TV( πi ⊗ pε

> −i

, π i ⊗ π−i) ≤ (N − 1) ε, 

and hence 

∥Gε(Π , Y ) − G0(Π , Y )∥ω ≤ 2H√γ(N − 1) ε. 

Since ∥Z∥∞ ≤ ω−1∥Z∥ω, (iv) holds, for example, with C∞ 

> Λ

= 2 H(N − 1) /(1 − √γ). 

Lemma D.2 (Regularity of current-policy advantages) . There is LA < ∞ such that 

|A i(Π; s, a ′

> i

) − A i(Π ′; s, a ′

> i

)| ≤ LA∥Π − Π′∥1

for every i, s, a ′ 

> i

and Π, Π′ ∈ C .

35 Proof. By Proposition D.1 at ε = 0 and norm equivalence, there is Lev < ∞ such that max 

n

∥QΠ − QΠ′

∥∞, ∥V Π − V Π′

∥∞

o

≤ Lev ∥Π − Π′∥1.

Fix ( i, s, a ′

> i

). Adding and subtracting P 

> a−i

Π−i(a−i | s)QΠ′ 

> i

(s, (a′

> i

, a −i)) gives 

|A i(Π; s, a ′

> i

) − A i(Π ′; s, a ′

> i

)|≤ ∥ QΠ − QΠ′

∥∞ + H Π−i(· | s) − Π′−i(· | s) 1 + ∥V Π − V Π′

∥∞.

The product-measure telescoping inequality implies 

Π−i(· | s) − Π′−i(· | s) 1 ≤ X 

> j̸=i

πj (· | s) − π′ 

> j

(· | s) 1 ≤ ∥ Π − Π′∥1.

Hence the claim holds with LA = 2 Lev + H.

D.2 Admissible actor families 

Proof of Proposition 5.3. Fix a finite action set A. The cited regret results are stated for losses; we apply them to the negatives of the gains. Let ∥ · ∥ be a norm on RA, let ∥ · ∥ ∗ be its dual, and choose KA < ∞ such that ∥x∥1 ≤ KA∥x∥. Since g ∈ [0 , H ]A, GA := max g∈[0 ,H ]A ∥g∥∗ < ∞.First consider FTRL. Let h : ∆( A) → R be lower semicontinuous and μ-strongly convex with respect to ∥ · ∥ , and suppose its oscillation Ω h := sup π h(π) − inf π h(π) is finite. Subtracting the constant inf h, assume inf h = 0. Define 

Qh(y) := arg max 

> π∈∆( A)

{⟨ y, π ⟩ − h(π)}.

Compactness and lower semicontinuity give existence, and strong convexity gives uniqueness. The two optimality inequalities imply 

∥Qh(y) − Qh(y′)∥ ≤ 1

μ ∥y − y′∥∗. (50) Indeed, μ∥Qh(y) − Qh(y′)∥2 ≤ ⟨ y − y′, Q h(y) − Qh(y′)⟩.Let Sn−1 := P 

> q<n

gq and set πn = Qh(βnSn−1). For the constrained loss problem on ∆( A), let ℓq(π) := −⟨ gq, π ⟩ and define the centered incremental regularizers by r0 = h/β 1 and rq =(β−1 

> q+1

− β−1 

> q

)h for q ≥ 1, with the simplex constraint encoded by its indicator. Their cumulative sum before round n is h/β n. Because ( βn) is nonincreasing and h ≥ 0, every rq is nonnegative. The resulting adaptive FTRL update is exactly πn. Moreover, h/β q is μ/β q-strongly convex, so the dual-norm term in Theorem 1 in McMahan (2017) is βq∥gq∥2

> ∗

/μ . The theorem therefore gives, for every a ∈ A, nX

> q=1

gq(a) − ⟨ πq, g q⟩ ≤ h(ea)

βn

+ 12μ

> n

X

> q=1

βq∥gq∥2

> ∗

.

36 This is the simplex specialization of the regularized dual-averaging update in Algorithm 1 in Xiao (2010). Hence max 

> a∈An

X

> q=1

gq(a) − ⟨ πq, g q⟩ ≤ Ωh

βn

+ G2

> A

2μ

> n

X

> q=1

βq. (51) Moreover, n(βn − βn+1 ) ≤ cβ n and (50) give 

∥πn+1 − πn∥1 ≤ KA

μ ∥βn+1 Sn − βnSn−1∥∗

≤ KA(1 + c)GA

μ βn.

Thus FTRL is admissible. A bounded predictable forecast may be inserted by taking πn = Qh(βn(Sn−1 + mn)), where 

∥mn∥∗ ≤ MA. Let ¯ πn := Qh(βnSn−1). By (50), ∥πn − ¯πn∥ ≤ βnMA/μ . Comparing the regret of (πn) with that of (¯ πn) in (51) gives max 

> a∈An

X

> q=1

gq(a) − ⟨ πq, g q⟩ ≤ Ωh

βn

+

 G2

> A

2μ + GAMA

μ

 nX

> q=1

βq.

The same Lipschitz estimate yields 

∥πn+1 − πn∥1 ≤ KA[(1 + c)GA + 2 MA]

μ βn.

Hence the bounded-forecast FTRL actor is admissible. We next consider online mirror ascent. Let h be differentiable and μ-strongly convex, write Dh

for its Bregman divergence, and suppose 

Bh := max  

> a∈A

sup 

> π∈∆( A)

Dh(ea, π ) < ∞.

Starting from any z1 ∈ ∆( A), let ( mn) be predictable with ∥mn∥∗ ≤ MA, and consider the optimistic mirror update 

πn ∈ arg max 

> π∈∆( A)

{βn⟨π, m n⟩ − Dh(π, z n)},zn+1 ∈ arg max 

> z∈∆( A)

{βn⟨z, g n⟩ − Dh(z, z n)}.

The ordinary mirror-ascent update is obtained by setting mn = 0, in which case πn = zn. The one-step inequality in the proof of Lemma 3 in Rakhlin and Sridharan (2013) is roundwise and therefore applies with the learning rate βn:

⟨ea − πn, g n⟩ ≤ Dh(ea, z n) − Dh(ea, z n+1 )

βn

+ βn

2μ ∥gn − mn∥2

> ∗

.

37 For dq := Dh(ea, z q), monotonicity of 1 /β q gives 

> n

X

> q=1

dq − dq+1 

βq

= d1

β1

+

> n

X

> q=2

 1

βq

− 1

βq−1



dq − dn+1 

βn

≤ Bh

βn

.

Summing the one-step inequality therefore gives max 

> a∈An

X

> q=1

gq(a) − ⟨ πq, g q⟩ ≤ Bh

βn

+ (GA + MA)2

2μ

> n

X

> q=1

βq.

Strong convexity of the two proximal problems also gives ∥πn − zn∥ ≤ βnMA/μ and ∥zn+1 − zn∥ ≤ 

βnGA/μ . Hence 

∥πn+1 − πn∥1 ≤ KA(GA + 2 MA)

μ βn.

Thus optimistic mirror ascent, and ordinary mirror ascent as the case MA = 0, are admissible. For h(π) = P 

> a

π(a) log π(a), FTRL is Hedge and Ω h = log |A|. For h(π) = ∥π∥22/2, mirror ascent is Euclidean projected gradient ascent and Bh ≤ 1, while FTRL is Euclidean dual averaging. The bounded-forecast versions give optimistic Hedge and the Euclidean optimistic mirror update, often called optimistic projected gradient. For the actor Ai,s selected in Algorithm 1, fix admissibility constants DA

> i,s

, CA

> i,s

, and LA

> i,s

.

D.3 Moving-target tracking 

The proof is quantitative from the outset. Assumption 5.1 and fixed exploration imply that, on a block of ⌈tb⌉ stages, every critic coordinate receives a fixed cumulative step except on an event with exponentially small probability. Over the same block, the target profile moves by O(t−(c−b)), while the critic martingale fluctuation has expected size O(t−b/ 2) and an exponentially small tail. The same one-block contraction therefore yields almost-sure tracking and the finite-time tracking bound. 

Lemma D.3 (One-step movement of admissible actors) . There is LΠ < ∞ such that 

∥Πt+1 − Πt∥1 ≤ LΠβst,n t(st) ∀t. (52) 

Proof. At stage t, only the player blocks at state st receive a new gain. By (12), block ( i, s t) moves by at most LA 

> i,s t

βst,n t(st). Summing over players proves (52) with LΠ := P 

> i

max s LA

> i,s

.The next lemma converts a uniform probability of observing an event within a bounded number of stages into an exponential lower-tail bound for the number of successful blocks. 

Lemma D.4 (Concentration from uniform block hitting) . Let (Ft) be a filtration and let Et ∈ F t+1 .Suppose there are L ∈ N and θ > 0 such that 

P

> t+L−1

[

> r=t

Er Ft

!

≥ θ ∀t ≥ 1. (53) 38 For deterministic integers t, B ≥ 1, define 

Yr := 1



> t+rL −1

[

> u=t+( r−1) L

Eu

 , r = 1 , . . . , B. 

Then 

P

> B

X

> r=1

Yr < θB 

2 Ft

!

≤ exp 



− θ2B

8



almost surely . (54) 

Proof. For each r = 1 , . . . , B , Yr is Ft+rL -measurable. Moreover, (53) applied at time t + ( r − 1) L

gives E[Yr | F t+( r−1) L] ≥ θ. Thus 

Yr − E[Yr | F t+( r−1) L], r = 1 , . . . , B, 

is a martingale-difference sequence with respect to ( Ft+rL )Br=0 , with increments bounded in absolute value by one. If PBr=1 Yr < θB/ 2, then 

> B

X

> r=1

Yr − E[Yr | F t+( r−1) L] < − θB 

2 .

Conditional Azuma–Hoeffding therefore gives (54). For a critic coordinate j, let χt(j) denote its actual step at stage t: set χt(Qi(s, a )) := 1{(st, a t) = (s, a )}α0nt(s, a )−b and χt(Vi(s)) := 1{st = s}η0nt(s)−b. Also set ¯ χt := max j χt(j). Thus At =diag( χt(j)) j and ∥At∥∞ = ¯ χt. For the remainder of this subsection, set mt := ⌈tb⌉.Recall P U from (10), and set L := |S| and P U := min {P U(s′ | s) : P U(s′ | s) > 0}. Define 

θε := ( εN P U)|S|− 1εN /|A| > 0 and με := κε := θε/(8 L). For each t, let Vt := {nt−1(s, a ) ≥ μεt ∀(s, a ) ∈ S × A} and 

Et := Vt ∩

(t+mt−1X

> u=t

1{(su, a u) = ( s, a )} ≥ κεmt ∀(s, a ) ∈ S × A 

)

.

The first event controls the local clocks before the block; the second also requires every state–action pair to occur sufficiently often within the block. Note that nt−1(s, a ) is Ht-measurable for every (s, a ), so Vt ∈ H t but Et also depends on the observations in [ t, t + mt) and need not belong to Ht.

Lemma D.5 (Quantitative critic blocks) . Under Assumption 5.1, Vt ∈ H t for every t. Moreover, there are δε, B ε, C 0, c 0 > 0 and Dε < ∞ such that, for every sufficiently large t,

P(Vct ) ≤ C0e−c0t, P(Ect ) ≤ C0e−c0tb

. (55) 

On Et, t+mt−1X

> u=t

χu(j) ≥ δε ∀j, 

> t+mt−1

X

> u=t

¯χu ≤ Bε, (56) 39 and 

sup 

> t≤k≤t+mt

∥Πk − Πt∥1 ≤ Dεt−(c−b). (57) 

Moreover, almost surely, Et occurs for every sufficiently large t, and 

nt(s, a ) ≥ μεt, nt(s) ≥ μεt ∀s, a (58) 

holds for every sufficiently large t.Proof. The minimum P U is positive because the state space is finite. Fixed exploration gives 

pt(a | s) ≥ εN /|A| and P(st+1 = s′ | H t) ≥ εN P U(s′ | st). Fix ( s, a ). By Assumption 5.1, from the current state there is a path to s in the support graph of P U with at most |S| − 1 edges. Conditional on traversing the preceding edges, every next edge has probability at least εN P U, and upon reaching s, action a is sampled with probability at least 

εN /|A| . Multiplying these conditional lower bounds along the path and using εN P U ≤ 1 gives 

P (( sr, a r) = ( s, a ) for some r ∈ { t, . . . , t + L − 1} | H t) ≥ θε

for every history. Partition [1 , t ) into ⌊(t − 1) /L ⌋ complete intervals of length L. For every sufficiently large t,

⌊(t−1) /L ⌋ ≥ t/ (2 L), so ( θε/2) ⌊(t−1) /L ⌋ ≥ 2μεt. If nt−1(s, a ) < μ εt, then fewer than θε⌊(t−1) /L ⌋/2of these intervals can contain ( s, a ). Applying (54) and taking a union bound over S × A therefore gives the first inequality in (55), after changing C0, c 0.Apply (54) conditionally on Ht to the interval [ t, t + mt). For every sufficiently large t, ⌊mt/L ⌋ ≥ 

mt/(2 L), and hence ( θε/2) ⌊mt/L ⌋ ≥ 2κεmt. Thus, if a state–action pair occurs fewer than κεmt

times in the block, fewer than θε⌊mt/L ⌋/2 complete subintervals can contain that pair. A union bound gives 

P

(t+mt−1X

> u=t

1{(su, a u) = ( s, a )} ≥ κεmt ∀(s, a )

)c

Ht

!

≤ C0e−c0mt .

Because Vt ∈ H t, the tower property gives 

P(Ect ) ≤ P(Vct ) + E

"

1{V t}P

(t+mt−1X

> u=t

1{(su, a u) = ( s, a )} ≥ κεmt ∀(s, a )

)c

Ht

!# 

≤ C0e−c0t + C0e−c0mt .

Using mt ≥ tb and changing C0, c 0 proves the second inequality in (55). We next prove the deterministic conclusions on Et. For every sufficiently large t, t+mt ≤ 2t. Each 

Qi(s, a ) coordinate is activated at least κεmt times on [ t, t +mt). For a Vi(s) coordinate, fix any a ∈ A ;every occurrence of ( s, a ) also activates that coordinate. At each such activation, the corresponding local counter is at most 2 t. Therefore, χu(j) ≥ 2−b min {α0, η 0}t−b whenever coordinate j is activated. 40 Since mt ≥ tb, the first inequality in (56) holds with δε := κε2−b min {α0, η 0}.On Vt, every active state–action counter after time t is at least μεt. For each state s, fixing any a ∈ A also gives nu(s) ≥ nt−1(s, a ) ≥ μεt for u ≥ t. Thus ¯ χu ≤ μ−bε max {α0, η 0}t−b for 

t ≤ u < t + mt. Since mt ≤ 2tb for every sufficiently large t, the second inequality in (56) holds with 

Bε := 2 μ−bε max {α0, η 0}.By (52), for t ≤ k ≤ t + mt, ∥Πk − Πt∥1 ≤ LΠβ0mt(μεt)−c ≤ Dεt−(c−b), where Dε := 2 LΠβ0μ−cε .This proves (57). The probabilities in (55) are summable over t. The Borel–Cantelli lemma implies that Et occurs for every sufficiently large t almost surely. Since Et ⊆ V t, on this event nt(s, a ) ≥ nt−1(s, a ) ≥ μεt

for every state–action pair. Fixing any joint action at state s gives the state-frequency bound in (58). Because αs,a,n , η s,n ∈ (0 , 1], each critic update is a convex combination of its previous value and a target in [0 , H ]. Indeed, 0 ≤ ui(s, a ) + γV i,t (s′) ≤ 1 + γH = H and 0 ≤ ⟨ πi,t (· | s), Q i,t (s, (·, a −i,t )) ⟩ ≤ 

H. The initialization therefore implies Xt ∈ X = [0 , H ]m for every t.For t ≤ k ≤ t + mt, define Mt,k := Pk−1 

> u=t

Auξu+1 and Nt := max t≤k≤t+mt ∥Mt,k ∥ω.

Lemma D.6 (Blockwise critic noise) . There are C1, c 1 > 0 such that, for every sufficiently large t,

E[1{V t}N t] ≤ C1t−b/ 2, (59) 

P (Vt ∩ {N t > x }) ≤ C1 exp( −c1x2tb) ∀x > 0. (60) 

Proof. The action both selects the active coordinates and generates the V -critic innovation, while the subsequent state transition generates the Q-critic innovation. We therefore separate these two sources of randomness by splitting every stage into two half-steps. For u ≥ 1, set G2u := Hu,

G2u+1 := σ(Hu, a u), and G2u+2 := Hu+1 . Let ∆ 2u+1 have zero Q block and, for every ( i, s ), set ∆V

> 2u+1

(i, s ) := 1{su = s}ηs,n u(s)

 ⟨πi,u (· | s), Q i,u (s, (·, a −i,u )) ⟩ − GVε (Π u, X u)( i, s ).

Let ∆ 2u+2 have zero V block and, for every ( i, s, a ), set ∆Q

> 2u+2

(i, s, a ) := 1{(su, a u) = ( s, a )}αs,a,n u(s,a )γ Vi,u (su+1 ) − X

> s′

P (s′ | s, a )Vi,u (s′)

!

.

Equations (1), (14), and the transition law imply ∆ 2u+1 + ∆ 2u+2 = Auξu+1 , E[∆ 2u+1 | G 2u] = 0, and E[∆ 2u+2 | G 2u+1 ] = 0. For fixed t, set St, 2t := 0 and St,ℓ := Pℓ−1 

> j=2 t

1{V t}∆j+1 for 2 t < ℓ ≤ 2( t + mt). Since Vt ∈ G 2t,(St,ℓ )2( t+mt) 

> ℓ=2 t

is a finite-dimensional martingale with respect to ( Gℓ)2( t+mt) 

> ℓ=2 t

. At every complete stage, 

St, 2k = 1{V t}Mt,k for t ≤ k ≤ t + mt.On Vt, every nonzero critic step on the block is at most μ−bε max {α0, η 0}t−b. Since Xu ∈ X , the innovations are uniformly bounded. Hence there is a deterministic C < ∞ such that every scalar 41 coordinate of St,ℓ +1 − St,ℓ has absolute value at most Ct −b. The block contains 2 mt ≤ 4tb half-step increments for every sufficiently large t, so the sum of the squared bounds for each scalar coordinate is at most Ct −b, after increasing C. Finite dimensionality also gives 

> 2( t+mt)−1

X

> ℓ=2 t

E∥St,ℓ +1 − St,ℓ ∥22 Gℓ

 ≤ Ct −b almost surely .

Let Cω < ∞ satisfy ∥x∥ω ≤ Cω∥x∥2. At the complete stages, 1{V t}N t = max t≤k≤t+mt ∥St, 2k∥ω,which is bounded by the maximum over all half-steps. Applying Doob’s L2 maximal inequality to each scalar coordinate, followed by Cauchy–Schwarz and orthogonality of martingale differences, gives 

E[1{V t}N t] ≤ Cω

X

> h

E



max  

> 2t≤ℓ≤2( t+mt)

|[St,ℓ ]h|2

!1/2

≤ 2Cω

X

> h

E|[St, 2( t+mt)]h|2!1/2

= 2 Cω



> 2( t+mt)−1

X

> ℓ=2 t

E∥St,ℓ +1 − St,ℓ ∥22



> 1/2

≤ C1t−b/ 2.

This proves (59). For a scalar coordinate h, the increment bound Ct −b and the fact that the block has at most 4 tb half-steps give a deterministic total squared bound of at most 4 C2t−b. The maximal Azuma–Hoeffding inequality therefore gives P{max 2t≤ℓ≤2( t+mt) |[St,ℓ ]h| > x } ≤ 2 exp( −cx 2tb) for a constant c > 0. A union bound over the finitely many coordinates and norm equivalence yield 

P{max 2t≤ℓ≤2( t+mt) ∥St,ℓ ∥ω > x } ≤ C1 exp( −c1x2tb). On Vt, St, 2k = Mt,k at every complete stage, so 

Vt ∩ {N t > x } is contained in this event. This proves (60). The next deterministic estimate shows how contraction survives asynchronous updates. Each coordinate may be updated at different stages, but it must receive a fixed cumulative step during the block. 

Lemma D.7 (Asynchronous contraction on one block) . Let T : Rm → Rm be a θ-contraction in 

∥ · ∥ ω with fixed point x⋆, where θ < 1. Suppose 

yn+1 = yn + An

 T (yn) − yn + rn

, n = p, . . . , q − 1,

42 where An is diagonal with entries in [0 , 1] . If 

> q−1

X

> n=p

[An]jj ≥ δ ∀j, 

> q−1

X

> n=p

∥An∥∞ ≤ B, 

then, with ρ := max p≤n<q ∥rn∥ω,

∥yq − x⋆∥ω ≤

h

θ + (1 − θ)e−δi

∥yp − x⋆∥ω + (1 + θB )ρ, (61) max  

> p≤n≤q

∥yn − x⋆∥ω ≤ ∥ yp − x⋆∥ω + Bρ. (62) 

Proof. Let Wω be diagonal with ∥z∥ω = ∥Wωz∥∞. Since An and Wω commute, we may work in the ordinary sup norm after this scaling. Set En := ∥yn − x⋆∥ω and an,j := [ An]jj . Contraction gives 

|[Wω(yn+1 − x⋆)] j | ≤ (1 − an,j )|[Wω(yn − x⋆)] j | + an,j (θE n + ρ).

Hence En+1 ≤ En + ∥An∥∞ρ, which proves (62). Let Pj := Qq−1

> n=p

(1 − an,j ). Using (62) in the coordinate recursion and iterating gives 

|[Wω(yq − x⋆)] j | ≤ Pj Ep + (1 − Pj ){θ(Ep + Bρ ) + ρ}.

Moreover, Pj ≤ exp( − Pq−1 

> n=p

an,j ) ≤ e−δ. Taking the maximum over j proves (61). 

Lemma D.8 (One-block moving-target contraction) . There are qε ∈ (0 , 1) and Kε, ¯Kε < ∞ such that, for every sufficiently large t, on Et,

∥Xt+mt − Λε(Π t+mt )∥ω ≤ qε∥Xt − Λε(Π t)∥ω + Kε



t−(c−b) + Nt



. (63) 

Moreover, for every t ≤ k ≤ t + mt, on Et,

∥Xk − Λε(Π k)∥ω ≤ ∥ Xt − Λε(Π t)∥ω + ¯Kε



t−(c−b) + Nt



. (64) 

Proof. Fix a sufficiently large t and work on Et. We compare the critic trajectory over the block [t, t + mt] with the asynchronous iteration associated with the policy Π t frozen at the beginning of the block. For t ≤ k ≤ t + mt, set Yk := Xk − Mt,k . Since Mt,k +1 = Mt,k + Akξk+1 , using the critic recursion we have 

Yk+1 = Xk+1 − Mt,k +1 

= Yk + Ak (Gε(Π k, X k) − Xk)= Yk + Ak (Gε(Π t, Y k) − Yk + dt,k ) , (65) where dt,k := Gε(Π k, X k) − Gε(Π t, Y k) − (Xk − Yk). This perturbation accounts for two effects: the movement of the target policy from Π t to Π k, and the fact that Xk and Yk differ by the 43 accumulated martingale noise Mt,k . The joint Lipschitz property in Proposition D.1 gives LG < ∞

such that 

∥Gε(Π , X ) − Gε(Π ′, X )∥ω ≤ LG∥Π − Π′∥1 ∀X ∈ X .

Let LΛ be a Lipschitz constant of Λ ε in the weighted norm. Adding and subtracting Gε(Π t, X k), using the contraction of Gε(Π t, ·), and recalling that Xk − Yk = Mt,k give 

∥dt,k ∥ω ≤ ∥ Gε(Π k, X k) − Gε(Π t, X k)∥ω

+ ∥Gε(Π t, X k) − Gε(Π t, Y k)∥ω + ∥Xk − Yk∥ω

≤ LG∥Πk − Πt∥1 + (1 + √γ)∥Mt,k ∥ω.

Thus (57) implies max 

> t≤k<t +mt

∥dt,k ∥ω ≤ LGDεt−(c−b) + (1 + √γ)Nt. (66) Although Yk need not belong to X , Proposition D.1 shows that Gε(Π t, ·) is defined on all of Rm

and remains a √γ-contraction there. Apply Lemma D.7 to (65) with T = Gε(Π t, ·), x⋆ = Λ ε(Π t)and θ = √γ. On Et, (56) provides the required cumulative-update bounds δε and Bε. Hence 

qε := √γ + (1 − √γ)e−δε < 1. Since Yt = Xt, (61) and (66) give 

∥Yt+mt − Λε(Π t)∥ω ≤ qε∥Xt − Λε(Π t)∥ω

+ (1 + √γB ε)

h

LGDεt−(c−b) + (1 + √γ)Nt

i

.

Moreover, ∥Mt,t +mt ∥ω ≤ N t and ∥Λε(Π t+mt ) − Λε(Π t)∥ω ≤ LΛDεt−(c−b). Since Xt+mt = Yt+mt +

Mt,t +mt , increasing Kε proves (63). For t ≤ k ≤ t + mt, (62) and (66) give 

∥Yk − Λε(Π t)∥ω ≤ ∥ Xt − Λε(Π t)∥ω

+ BεLGDεt−(c−b) + Bε(1 + √γ)Nt.

Combining this inequality with Xk = Yk + Mt,k , ∥Mt,k ∥ω ≤ N t, and ∥Λε(Π k) − Λε(Π t)∥ω ≤

LΛDεt−(c−b), and then increasing ¯Kε, proves (64). 

Proposition D.9 (Moving-target joint online policy evaluation) . Under Assumption 5.1, Algorithm 1 satisfies 

∥Xt − Λε(Π t)∥∞ −→ 0 almost surely . (67) 

Moreover, there is Cε,b,c < ∞ such that, for every t ≥ 2,

E[∥Xt − Λε(Π t)∥∞] ≤ Cε,b,c 



t−(c−b) + t−b/ 2

. (68) 

Proof. Choose a sufficiently large deterministic t0 and define consecutive blocks by tr+1 := tr + ⌈tbr⌉.Write eet := ∥Xt − Λε(Π t)∥ω. Since Xt, Λε(Π t) ∈ X and √γ ≤ 1, we have 0 ≤ eet ≤ H.44 We first prove (67). By Lemma D.5, almost surely Etr occurs for every sufficiently large r.Moreover, (60) gives ∞X

> r=0

P



Vtr ∩ {N tr > t −b/ 4 

> r

}



< ∞,

because the summand is at most C1 exp( −c1tb/ 2 

> r

) and tr ≥ t0 + r. The Borel–Cantelli lemma and 

Etr ⊆ V tr therefore imply that, almost surely, for every sufficiently large r, Etr occurs and Ntr ≤ t−b/ 4 

> r

.Equation (63) then gives 

eetr+1 ≤ qεeetr + Kε



t−(c−b) 

> r

+ t−b/ 4

> r



for every sufficiently large r. The additive term converges to zero. Taking upper limits and using boundedness yields lim sup r eetr ≤ qε lim sup r eetr , so eetr → 0 almost surely. On the same probability-one event, (64) gives sup tr ≤k≤tr+1 eek ≤ eetr + ¯Kε(t−(c−b) 

> r

+ t−b/ 4 

> r

) for every sufficiently large r. The right-hand side converges to zero, and norm equivalence proves (67). We next prove (68). Let ur := E[eetr ]. On Etr , apply (63); on Ectr , use eetr+1 ≤ H. Since Etr ⊆ V tr ,(59) and (55) give, for every sufficiently large r,

ur+1 = E1{E tr }eetr+1 

 + E1{E ctr }eetr+1 



≤ qεE[1{E tr }eetr ] + Kεt−(c−b) 

> r

+ KεE[1{E tr }N tr ] + HP(Ectr )

≤ qεur + Kεt−(c−b) 

> r

+ KεC1t−b/ 2 

> r

+ HC 0e−c0tbr

≤ qεur + C2



t−(c−b) 

> r

+ t−b/ 2

> r



. (69) We solve (69). For every sufficiently large r, tr+1 ≤ 2tr. The mean-value theorem and tbr ≤

tr+1 − tr ≤ 2tbr therefore give (1 − b)2 −b ≤ t1−br+1 − t1−br ≤ 2(1 − b). After summation, t1−br is bounded above and below by positive multiples of r. In particular, there is d > 0 such that tj ≥ dt r whenever 

r/ 2 ≤ j ≤ r.Iterating (69) gives 

ur ≤ qr−r0 

> ε

ur0 + C2

> r−1

X

> j=r0

qr−1−jε



t−(c−b) 

> j

+ t−b/ 2

> j



.

For j ≥ r/ 2, the comparison between tj and tr bounds the corresponding sum by a constant times 

t−(c−b) 

> r

+ t−b/ 2 

> r

. For j < r/ 2, the geometric coefficient is at most qr/ 2−1 

> ε

, while the number of terms is at most r and the remaining factors are bounded. Since tr grows polynomially in r, rq r/ 2−1

> ε

decreases faster than either power of tr. Therefore, after increasing the constant, 

ur ≤ C3



t−(c−b) 

> r

+ t−b/ 2

> r



. (70) 45 Fix tr ≤ k ≤ tr+1 . On Etr , apply (64); on Ectr , use eek ≤ H. Since Etr ⊆ V tr ,

E[eek] ≤ ur + ¯Kεt−(c−b) 

> r

+ ¯KεE[1{E tr }N tr ] + HP(Ectr )

≤ ur + ¯Kεt−(c−b) 

> r

+ ¯KεC1t−b/ 2 

> r

+ HC 0e−c0tbr

≤ C4



t−(c−b) 

> r

+ t−b/ 2

> r



,

where the second inequality uses (59) and (55), and the last inequality uses (70). Since tr ≤ k ≤

tr + 2 tbr ≤ 2tr for every sufficiently large r, the same estimate holds with k in place of tr. Finally, 

∥z∥∞ ≤ γ−1/2∥z∥ω. Increasing the constant to cover the finitely many initial dates proves (68). 

D.4 Actor regret and the AMCR bound 

Under Assumption 5.1, (58) implies that tq(s) < ∞ for every state s and every q ≥ 1 almost surely. Each tq(s) is an ( Ht)t≥1-stopping time. For any almost surely finite, integer-valued stopping time τ ,let 

Hτ := {A ∈ F : A ∩ { τ = t} ∈ H t for every t ≥ 1}

denote the corresponding stopped sigma-field. 9

For a fixed state s, write bgsi,q := Qi,t q (s)

 s, (·, a −i,t q (s)) and Hs,q := Htq (s). Define 

gp,s i,q := E[bgsi,q | H s,q ], ∆si,q := bgsi,q − gp,s i,q . (71) Applying the stopped-time identity above coordinatewise and using (1) gives, for every ai ∈ Ai,

gp,s i,q (ai) = X

> a−i

p−i,t q (s)(a−i | s)Qi,t q (s)(s, (ai, a −i)) . (72) 

Proposition D.10 (Observed-feedback regret) . For every player i and state s, there is a nonnegative process Mi,s (n), n ≥ 0, with Mi,s (0) = 0 , such that Mi,s (n)/n → 0 almost surely, E[Mi,s (n)] ≤

KiH√n for every n ≥ 1, and 

Rtar  

> i,s

(n) ≤ DA

> i,s

βs,n 

+ CA

> i,s n

X

> q=1

βs,q + Mi,s (n) + 4 H(N − 1) εn (73) 

for every n ≥ 1.Proof. Apply (11) pathwise to the realized vectors bgsi,q . To analyze the sampling error on the local clock of state s, let Ks, 0 be the trivial sigma-field and set Ks,q := Htq (s)+1 for q ≥ 1. Thus,             

> 9We use the following elementary identity. If τis an almost surely finite ( Ht)t≥1-stopping time, Ytis bounded, and
> Ytis Ht+1 -measurable for every t, then
> E[Yτ| H τ] =
> ∞X
> t=1
> 1{τ=t}E[Yt| H t].

46 Hs,q = Htq (s) is the information available immediately before the action at the q-th visit to 

s, whereas Ks,q contains the information available after that visit. In particular, ∆ si,q is Ks,q -measurable. Moreover, for q ≥ 2, tq−1(s) + 1 ≤ tq(s), so monotonicity of stopped sigma-fields gives 

Ks,q −1 ⊆ H s,q . The same inclusion holds for q = 1 because Ks, 0 is trivial. Then, by definition of ∆ si,q ,we have E[∆ si,q | H s,q ] = 0. Therefore, E[∆ si,q (ai) | K s,q −1] = 0 for every action ai. Since πi,t q (s)(· | s)is Hs,q -measurable, the tower property also gives 

E πi,t q (s)(· | s), ∆si,q Ks,q −1

 = 0 .

Consequently, for every action ai,



> n

X

> q=1

∆si,q (ai)



> n≥0

and 



> n

X

> q=1

πi,t q (s)(· | s), ∆si,q 



> n≥0

are martingales with respect to ( Ks,n )n≥0. Define 

Mi,s (n) := max 

> 1≤m≤n

max 

> ai
> m

X

> q=1

∆si,q (ai) +

> m

X

> q=1

πi,t q (s)(· | s), ∆si,q 

 .

Thus Mi,s (n) simultaneously controls, uniformly over all prefixes m ≤ n, both the coordinatewise sampling errors for every fixed action and the sampling error of the actions actually mixed by the actor. All martingale increments are bounded. Hence, for each of the finitely many martingales 

Y , the series P 

> n≥1

n−2E[( Yn − Yn−1)2] is finite, and the martingale strong law gives Yn/n → 0. It follows that max m≤n |Ym|/n → 0: after a fixed initial segment, use |Ym| = o(m), while the initial segment divided by n vanishes. Therefore, Mi,s (n)/n → 0 almost surely. The terminal second moment of each martingale is at most nH 2. Applying Doob’s L2 maximal inequality to these finitely many martingales and summing the resulting bounds gives E[Mi,s (n)] ≤ KiH√n.It remains to compare the conditional mean in (71) with the target-law vector in (5). By (72), total-variation tensorization, and (9), TV( p−i,t q (s)(· | s), Π−i,t q (s)(· | s)) ≤ (N − 1) ε. Since every gain coordinate lies in [0 , H ], 

∥gp,s i,q − gtar  

> i,t q(s)

∥∞ ≤ 2H(N − 1) ε. (74) 47 For every action ai, (71) gives 

> n

X

> q=1

h

gtar  

> i,t q(s)

(ai) −

D

πi,t q (s)(· | s), g tar  

> i,t q(s)

Ei 

=

> n

X

> q=1

bgsi,q (ai) − πi,t q (s)(· | s), bgsi,q 



−

> n

X

> q=1

∆si,q (ai) − πi,t q (s)(· | s), ∆si,q 



+

> n

X

> q=1

h

(gtar   

> i,t q(s)

− gp,s i,q )( ai) −

D

πi,t q (s)(· | s), g tar   

> i,t q(s)

− gp,s i,q 

Ei 

. (75) The second sum on the right-hand side of (75) is bounded in absolute value by Mi,s (n). By (74), the third sum is at most 4 H(N − 1) εn . Maximizing over ai, taking the positive part, and applying (11) to the first sum proves (73). For reference, one admissible choice of constants in Theorem 5.5 is 

Creg := 4 HN (N − 1) , κAM := 2 N C ∞ 

> Λ

+ 4 HN (N − 1) + 2 N 2|S| LA. (76) 

Proof of Theorem 5.5. Proposition D.9 proves (15). For n ≥ 1, the deterministic actor terms in (73) satisfy β−1 

> s,n

= O(nc) and P 

> q≤n

βs,q = O(n1−c), so both are o(n). Equation (58) gives Ns(T ) → ∞ for every state. Hence, for each ( i, s ), the sum of the two deterministic terms and Mi,s (Ns(T )) is o(Ns(T )) almost surely. Since Ns(T )/T ≤ 1, each such contribution is o(T ). Summing over the finitely many player–state blocks and using 

P 

> s

Ns(T ) = T in the exploration term, Proposition D.10 yields lim sup  

> T→∞

1

T

X

> s,i

Rtar  

> i,s

(Ns(T )) ≤ 4HN (N − 1) ε almost surely . (77) This proves (16) with Creg in (76). Moreover, Proposition D.1 gives ∥Xt − Λ(Π t)∥∞ ≤ ∥ Xt − Λε(Π t)∥∞ + C∞ 

> Λ

ε. Equation (15) and Ces` aro’s lemma yield lim sup  

> T→∞

2NT

> T

X

> t=1

∥Xt − Λ(Π t)∥∞ ≤ 2N C ∞ 

> Λ

ε. (78) Substituting (77) and (78) into Theorem 3.6 gives lim sup  

> T→∞

GAMCR  

> T

(Π 1: T ) ≤ [2 N C ∞ 

> Λ

+ 4 HN (N − 1)] ε. 

48 For the behavior path, Lemma D.2 implies 

GAMCR  

> T

(p1: T ) ≤ G AMCR  

> T

(Π 1: T ) + N L A

T

> T

X

> t=1

∥pt − Πt∥1.

Under fixed mixing, ∥pt − Πt∥1 ≤ 2N |S| ε. Hence the behavior path satisfies (17) with the constant in (76). 

D.5 Finite-time AMCR rate 

The quantitative conclusion of Proposition D.9 controls the evaluation term in Theorem 3.6. It remains to retain the finite-horizon terms in the actor-regret estimate. 

Proof of Theorem 5.7. For n ≥ 1, Proposition D.10, βs,n = β0n−c, and Pnq=1 q−c ≤ n1−c/(1 − c)give, pathwise, 

Rtar  

> i,s

(n) ≤ DA

> i,s

β0

nc + CA

> i,s

β0

1 − c n1−c + Mi,s (n) + 4 H(N − 1) εn. 

Together with Rtar  

> i,s

(0) = Mi,s (0) = 0, this bound holds at n = 0 and may be evaluated at n = Ns(T ). Monotonicity of Mi,s and Ns(T ) ≤ T give E[Mi,s (Ns(T ))] ≤ E[Mi,s (T )] ≤ KiH√T . Concavity and P 

> s

Ns(T ) = T imply P 

> s

Ns(T )c ≤ |S| 1−cT c and P 

> s

Ns(T )1−c ≤ |S| cT 1−c. Consequently, 1

T

X

> s,i

ERtar  

> i,s

(Ns(T ))  ≤ 4HN (N − 1) ε + CA,1T −(1 −c)

+ CA,2T −c + CA,3T −1/2, (79) where CA,1 := |S| 1−cβ−10

PNi=1 max s∈S DA

> i,s

, CA,2 := β0|S| c(1 − c)−1 PNi=1 max s∈S CA

> i,s

, and CA,3 := 

H|S| PNi=1 Ki.Let e0 

> t

:= ∥Xt − Λ(Π t)∥∞. Proposition D.1 and (68) give, for t ≥ 2, E[e0 

> t

] ≤ C∞ 

> Λ

ε + Cε,b,c (t−(c−b) +

t−b/ 2), while E[e01] ≤ H. Since 0 < c − b < 1 and 0 < b/ 2 < 1, applying the integral comparison to the two sums yields 2NT

> T

X

> t=1

E[e0 

> t

] ≤ 2N C ∞ 

> Λ

ε + Ctr 

> ε,b,c



T −(c−b) + T −b/ 2

(80) for a finite constant Ctr 

> ε,b,c

, after absorbing 2 N H/T .Theorem 3.6 is pathwise. Taking expectations, substituting (79) and (80), and increasing Cε,b,c 

to absorb the finite actor constants gives 

EGAMCR  

> T

(Π 1: T ) ≤ [4 HN (N − 1) + 2 N C ∞ 

> Λ

] ε

+ Cε,b,c 



T −(1 −c) + T −(c−b) + T −b/ 2

.

The terms T −c and T −1/2 in (79) are absorbed into T −(1 −c) because c > 1/2. 49 For the behavior path, Lemma D.2 and fixed mixing give, pathwise, 

GAMCR  

> T

(p1: T ) ≤ G AMCR  

> T

(Π 1: T ) + 2 N 2|S| LAε. 

Consequently, 

EGAMCR  

> T

(p1: T ) ≤ κAM ε + Cε,b,c 



T −(1 −c) + T −(c−b) + T −b/ 2

, (81) where κAM is defined in (76). For arbitrary admissible b, c , the polynomial exponent in (81) is min {1 − c, c − b, b/ 2}. Moreover, min {1 − c, c − b} ≤ [(1 − c) + ( c − b)] /2 = (1 − b)/2 < 1/4, where the strict inequality follows from 

b > 1/2. Thus 1 /4 is the supremum of the obtainable exponents under the power restrictions of Algorithm 1. Fix r ∈ (0 , 1/4) and choose b = 1 − 2r and c = 1 − r. Then 1 /2 < b < c < 1, 1 − c = c − b = r,and b/ 2 = 1 /2 − r > r . Substitution into (81), followed by an increase of the constant to cover the finitely many initial horizons, proves (18). 

# E Proofs for Section 6 

Proof of Theorem 6.1. Let Fe be the σ-field available immediately before episode e. In particular, the target and behavior profiles used during episode e are Fe-measurable. Conditional on Fe, the initial state se, 0 is drawn from ρ0, and independently the terminal index Le is drawn according to (20). The episode is then generated using the fixed behavior profile pe and the transition kernel P .The value of Le is not revealed to the players; at each stage they only observe whether the episode has terminated. Let Fe+1 contain Fe, the complete record of episode e, and the update in (22). The draws ( se, 0, L e) are independent across episodes, and the random draws used for initialization and termination are independent of those used for actions and state transitions. We next introduce a within-episode filtration that is indexed by deterministic stage numbers even though the episode length is random. For this purpose, enlarge the state, action, and payoff spaces by a dummy value and set all corresponding variables after termination equal to that value. For each deterministic k ≥ 0, let He,k be the σ-field generated by Fe, the extended episode record before stage k, the extended state at stage k, and the survival indicators 1Le ≥ r for 0 ≤ r ≤ k. Thus, on the event Le ≥ k, He,k represents exactly the information available upon arrival at se,k and before the stage-k action. In particular, it reveals that the episode has survived through stage k, but not the value of Le or any additional information about the remaining horizon. This distinction will allow us to use the memoryless property of (20) conditional on He,k . Write Ne,s := PLe 

> k=0

1{se,k = s}.Finally, bGi,e is square-integrable. Indeed, pi,e (ai | s) ≥ εe/|Ai|, so for fixed e every score term in (21) is bounded by a constant times ε−1 

> e

, while the return and the number of score terms are both bounded by Le + 1. Since the geometric random variable Le has finite moments of every order, 

E[| bGi,e |22] < ∞.50 Fix player i, state s, and action bi ∈ Ai. In this proof, let ds,b i 

> i,e

:= ebi − πi,e (· | s) and 

qsi,e (ai) := X

> a−i∈A−i

p−i,e (a−i | s)Qpe 

> i

(s, (ai, a −i)) .

The coordinates of ds,b i 

> i,e

sum to zero. Under the direct parameterization, 

D

ds,b i 

> i,e

, ∇πi(·| s) log pi,e (ai,e,k | se,k )

E

= (1 − εe)1{se,k = s} ds,b i 

> i,e

(ai,e,k )

pi,e (ai,e,k | s) .

Therefore, on {Le ≥ k},

E

hD 

ds,b i 

> i,e

, ∇πi(·| s) log pi,e (ai,e,k | se,k )

E

He,k 

i

= (1 − εe)1{se,k = s} X

> ai∈Ai

ds,b i 

> i,e

(ai) = 0 .

Every payoff realized before stage k is He,k -measurable, so its product with this directional score has conditional expectation zero. Conditional on He,k and ae,k = a, the memoryless property of (20) gives, on {Le ≥ k},

E

" LeX

> ℓ=k

ui(se,ℓ , a e,ℓ ) He,k , a e,k = a

#

= Qpe 

> i

(se,k , a ).

To make the resulting directional-unbiasedness identity explicit, write 

Ze,k := 

D

ds,b i 

> i,e

, ∇πi(·| s) log pi,e (ai,e,k | se,k )

E

.

Expanding (21) over the score times gives 

D

ds,b i 

> i,e

, bGsi,e 

E

=

> Le

X

> k=0

Ze,k 

X

> ℓ<k

ui(se,ℓ , a e,ℓ ) + 

> Le

X

> ℓ=k

ui(se,ℓ , a e,ℓ )

!

.

Conditional on He,k , the first sum is measurable and E[Ze,k | He,k ] = 0, so the contribution of rewards realized before stage k vanishes. For the remaining rewards, conditioning additionally on 

ae,k = a and using the memoryless identity above gives 

E

"

Ze,k LeX

> ℓ=k

ui(se,ℓ , a e,ℓ ) He,k 

#

= (1 − εe)1{Le ≥ k, s e,k = s}

D

ds,b i 

> i,e

, q si,e 

E

.

51 Indeed, conditional on He,k and se,k = s, averaging the stage-k action under pe yields 

X

> ai,a −i

pi,e (ai | s)p−i,e (a−i | s) (1 − εe)ds,b i 

> i,e

(ai)

pi,e (ai | s) Qpe 

> i

(s, (ai, a −i)) = (1 − εe)

D

ds,b i 

> i,e

, q si,e 

E

.

Summing over k and conditioning on Fe, while noting that ds,b i 

> i,e

and qsi,e are Fe-measurable, gives 

E

hD 

ds,b i 

> i,e

, bG si,e 

E

Fe

i

= E[Ne,s | Fe](1 − εe)

D

ds,b i 

> i,e

, q si,e 

E

, (82) where bG si,e is the state-s block of bGi,e .The second equation in (2) gives V pe 

> i

(s) = ⟨pi,e (· | s), q si,e ⟩. Hence 

Ai(pe; s, b i) = qsi,e (bi) − pi,e (· | s), q si,e 

≤ (1 − εe)

D

ds,b i 

> i,e

, q si,e 

E

+ εeH. (83) Indeed, the difference between the two sides before the term εeH is εe[qsi,e (bi) − ⟨ Ui, q si,e ⟩] ≤ εeH.Projection in (22) separates across states. The standard projected-gradient inequality with nonincreasing steps gives 

> E

X

> e=1

D

ds,b i 

> i,e

, bG si,e 

E

≤ 1

ηE

+ 12

> E

X

> e=1

ηe∥ bG si,e ∥22;see Zinkevich (2003). The first term is at most 1 /η E because the squared Euclidean diameter of a simplex is at most two. Define ∆Mi,s,b i,e := Ne,s (1 − εe)

D

ds,b i 

> i,e

, q si,e 

E

−

D

ds,b i 

> i,e

, bG si,e 

E

,Mi,s,b i (E) := 

> E

X

> e=1

∆Mi,s,b i,e .

Each increment is Fe+1 -measurable, and equation (82) gives E[∆ Mi,s,b i,e | Fe] = 0. Thus (Mi,s,b i (E), FE+1 )E≥0 is a martingale. Combining the projected-gradient inequality with (83) gives 

> E

X

> e=1

Ne,s Ai(pe; s, b i) ≤ 1

ηE

+ 12

> E

X

> e=1

ηe∥ bG si,e ∥22 + Mi,s,b i (E) + H

> E

X

> e=1

εeNe,s .

52 Maximizing over bi, taking positive parts, and summing over players and states yields 

GAMCR  

> KE

≤ N |S| 

KE ηE

+ 12KEEX

> e=1

ηeNX

> i=1

∥ bGi,e ∥22

+ 1

KENX

> i=1

X

> s∈S

max 

> bi∈Ai

|Mi,s,b i (E)| + N H KEEX

> e=1

εe(Le + 1) . (84) Conditional on Fe, the profile and exploration level are fixed. Lemma D.4 in Leonardos et al. (2022) applies to (21) with its exploration parameter α = εe. Using H = (1 − γ)−1, it gives 

E

h

∥ bGi,e ∥22 Fe

i

≤ 24 A2max H4

εe

. (85) We next bound the martingale increments. Since |(1 − εe)⟨ds,b i 

> i,e

, q si,e ⟩| ≤ H,

X

> s∈S

X

> bi∈Ai

N 2

> e,s

(1 − εe)2 D

ds,b i 

> i,e

, q si,e 

E2

≤ Amax H2(Le + 1) 2.

Also, Cauchy–Schwarz and P 

> bi∈Ai

∥ebi − πi,e (· | s)∥22 ≤ 2|Ai| give, for every state block, 

X

> bi∈Ai

D

ebi − πi,e (· | s), bG si,e 

E2

≤ 2|Ai|∥ bG si,e ∥22.

Using ( x − y)2 ≤ 2x2 + 2 y2, E[( Le + 1) 2] = (1 + γ)H2 ≤ 2H2, and (85), we obtain 

X

> s∈S

X

> bi∈Ai

E(∆ Mi,s,b i,e )2 Fe

 ≤ 100 A3max H4

εe

. (86) Moreover, 

> N

X

> i=1

X

> s∈S

max 

> bi∈Ai

|Mi,s,b i (E)|

!2

≤ N |S| 

> N

X

> i=1

X

> s∈S

X

> bi∈Ai

Mi,s,b i (E)2.

Since martingale increments from different episodes are orthogonal, (86) implies 

E



> N

X

> i=1

X

> s∈S

max 

> bi∈Ai

|Mi,s,b i (E)|

!2 ≤ 100 N 2|S| A3max H4

> E

X

> e=1

1

εe

. (87) Let Ye := Le + 1. The variables ( Ye) are independent geometric random variables on {1, 2, . . . }

with mean H, and KE = PEe=1 Ye. The variable KE has probability mass P(KE = k) =  k−1

> E−1

(1 −

γ)E γk−E for k ≥ E. For E ≥ 3, the identities 1

k − 1

 k − 1

E − 1



= 1

E − 1

 k − 2

E − 2



, 1(k − 1)( k − 2) 

 k − 1

E − 1



= 1(E − 1)( E − 2) 

 k − 3

E − 3



53 and the binomial series give E[( KE − 1) −1] = [ H(E − 1)] −1 and E[(( KE − 1)( KE − 2)) −1] = [H2(E − 1)( E − 2)] −1. Therefore, for E ≥ 3, 

E

 1

KE



≤ 1

H(E − 1) , E

 1

K2

> E



≤ 1

H2(E − 1)( E − 2) . (88) The estimator from episode e depends on Ye, so it cannot be separated directly from K−1 

> E

. Let 

m := ⌊E/ 2⌋. If e ≤ m, the sum PEj=m+1 Yj is independent of bGi,e ; hence 

E

"

∥ bGi,e ∥22

KE

#

≤ E[∥ bGi,e ∥22]E

"

1

PEj=m+1 Yj

#

.

If e > m , the sum Pmj=1 Yj is Fe-measurable, so 

E

"

∥ bGi,e ∥22

KE

#

≤ E

"

1

Pmj=1 Yj

E[∥ bGi,e ∥22 | Fe]

#

.

Each half contains at least three episodes when E ≥ 6. Applying (85) and (88) to the relevant half gives 

E

"

∥ bGi,e ∥22

KE

#

≤ 96 A2max H3

Eε e

, E ≥ 6. (89) For the martingale term, Cauchy–Schwarz, (87), and (88) give 

E

"

1

KENX

> i=1

X

> s∈S

max 

> bi∈Ai

|Mi,s,b i (E)|

#

≤ 20 N A 3/2max Hp|S| 

E

> E

X

> e=1

1

εe

!1/2

.

Finally, exchangeability and PEe=1 Ye/K E = 1 give E[Ye/K E ] = 1 /E for every e.Taking expectations in (84) and using (89) gives, for E ≥ 6, 

EGAMCR 

> KE

 ≤ 2N |S| 

HEη E

+ 48 N A 2max H3

E

> E

X

> e=1

ηe

εe

+ 20 N A 3/2max Hp|S| 

E

> E

X

> e=1

1

εe

!1/2

+ N H E

> E

X

> e=1

εe. (90) 54 Under the schedules in the theorem, 1

Eη E

= O



Amax H2|S| −1/2E−1/3

,

> E

X

> e=1

ηe

εe

= O |S| 1/2

Amax H2 E2/3

!

,

> E

X

> e=1

εe = O(E2/3),

> E

X

> e=1

1

εe

= O(E4/3).

Substitution into (90) proves (23) for E ≥ 6. Increasing C covers E < 6 because GAMCR  

> KE

≤ N H .We next prove almost-sure convergence. The strong law gives KE /E → H. Equation (85) and the schedules imply 

E

"

1

E

> E

X

> e=1

ηeNX

> i=1

∥ bGi,e ∥22

#

= O(E−1/3),

E

"

1

E

> E

X

> e=1

εeYe

#

= O(E−1/3).

The two numerators are nonnegative and nondecreasing. For either numerator, write it as SE .Markov’s inequality at E = 2 m gives P(S2m /2m > 2−m/ 6) = O(2 −m/ 6). The Borel–Cantelli lemma gives S2m /2m → 0, and monotonicity gives SE /E → 0 between consecutive dyadic horizons. Therefore, almost surely, 1

E

> E

X

> e=1

ηeNX

> i=1

∥ bGi,e ∥22 −→ 0, 1

E

> E

X

> e=1

εeYe −→ 0.

For each fixed ( i, s, b i), (86) and ε−1 

> e

= O(e1/3) imply 

> ∞

X

> e=1

E[(∆ Mi,s,b i,e )2]

e2 < ∞.

Hence the martingale PEe=1 ∆Mi,s,b i,e /e has uniformly bounded second moments and converges almost surely. Kronecker’s lemma then gives Mi,s,b i (E)/E → 0 almost surely. There are finitely many player–state–action triples. Since 1 /(Eη E ) → 0 and KE /E → H, (84) now gives 

GAMCR  

> KE

−→ 0 almost surely . (91) It remains to consider a horizon inside an episode. For sufficiently large T , let ET := max {E :

KE ≤ T }. Since each one-stage advantage belongs to [ −H, H ], 

T GAMCR  

> T

≤ KET GAMCR 

> KET

+ N H (T − KET )

≤ KET GAMCR 

> KET

+ N HY ET +1 .

55 Thus 

GAMCR  

> T

≤ G AMCR 

> KET

+ N H YET +1 

KET

.

The geometric tail and the Borel–Cantelli lemma imply YE+1 /E → 0 almost surely. Together with 

KE /E → H, this gives YE+1 /K E → 0. Equation (91) completes the proof. 

# F Proofs for Section 8 

Proof of Lemma 8.2. Fix i, s , and bi. By (28), (30), and the tying equation, gμi (s, b i) is the expec-tation under Π ∼ μ(· | s) and a ∼ Π( · | s) of Qσμ 

> i

(s, (bi, a −i)) − Qσμ 

> i

(s, a ). For every (Π , a ), the Bellman equations give 

Qσμ 

> i

(s, (bi, a −i)) − Qσμ 

> i

(s, a )= QΠ 

> i

(s, (bi, a −i)) − QΠ 

> i

(s, a ) + γ P (· | s, (bi, a −i)) − P (· | s, a ), V σμ 

> i

− V Π 

> i

.

Averaging proves (31). Fix a deterministic stationary deviation βi, and let P βi,σ μ, −i 

> i

be its transition matrix against 

σμ, −i. Its Bellman equation and (30) imply 

V βi,σ μ, −i 

> i

− V σμ 

> i

= gμi (·, β i(·)) + γP βi,σ μ, −i

> i



V βi,σ μ, −i 

> i

− V σμ

> i



.

Iterating this identity and using (29) gives (1 − γ)

h

V βi,σ μ, −i 

> i

(s0) − V σμ 

> i

(s0)

i

= X

> s

ωβi,σ μ, −i 

> i,s 0

(s)gμi (s, β i(s)) .

Maximizing over i, s 0, β i proves (32). 

Proof of Corollary 8.3. We first bound the MCCE gap of the average behavior law σT .Consider case (i). Equation (58) implies that every state is visited infinitely often almost surely. Fix i and s, and write e0 

> t

:= ∥Xt − Λ(Π t)∥∞. Summing (47) over the first n visits to s gives 1

n

max 

> bi
> n

X

> q=1

Ai(Π tq (s); s, b i)



> +

≤ Rtar  

> i,s

(n)

n + 2

n

> n

X

> q=1

e0 

> tq(s)

.

Proposition D.10 gives lim sup n Rtar  

> i,s

(n)/n ≤ 4H(N − 1) ε almost surely. Also, (67) and (48) give 

e0 

> t

≤ ∥ Xt − Λε(Π t)∥∞ + C∞ 

> Λ

ε. Ces` aro’s lemma therefore yields lim sup 

> n→∞

1

n

max 

> bi
> n

X

> q=1

Ai(Π tq (s); s, b i)



> +

≤ [4 H(N − 1) + 2 C∞ 

> Λ

]ε. 

By Lemma D.2 and fixed exploration, |A i(pt; s, b i) − A i(Π t; s, b i)| ≤ 2N |S| LAε. Since there are 56 finitely many player–state–action triples, lim sup  

> T→∞

max 

> i,s,b i

 1

Ns(T )

X 

> t∈Ts(T)

Ai(pt; s, b i)



> +

≤ κMε, 

where one may take κM := 4 H(N − 1) + 2 C∞ 

> Λ

+ 2 N |S| LA. Applying (34) to bμT proves the case (i) bound in (35) with bσT replaced by σT .Consider case (ii). For s ∈ S , let Cs(E) := PEe=1 1{se, 0 = s}. The strong law gives Cs(E)/E →

ρ0(s). The proof of Theorem 6.1 gives KE /E → H and ( LE+1 + 1) /E → 0. Hence, for ET := max {E : KE ≤ T }, T /E T → H. Since the first ET episode starts occur among the first T played stages, lim inf  

> T→∞

Ns(T )

T ≥ ρ0(s)

H > 0 ∀s ∈ S (92) almost surely. For every i, s, b i,

 1

Ns(T )

X 

> t∈Ts(T)

Ai(pt; s, b i)



> +

≤ TNs(T ) GAMCR  

> T

.

Theorem 6.1 and (92) make the right-hand side converge to zero uniformly over the finitely many triples ( i, s, b i). Applying (34) to bμT proves the case (ii) bound in (35) with bσT replaced by σT .It remains to replace the average behavior law by realized empirical play. Let Jt be the information available immediately before the tth action in the relevant record. For Algorithm 1, 

Jt = Ht; for Algorithm 2, it is the corresponding episodic pre-action history. In either case, pt is 

Jt-measurable and P(at = a | J t) = pt(a | st). Fix s and a, and let Ks,q be the history immediately after the action at the qth visit to s, with 

Ks, 0 trivial. Then Ks,q −1 ⊆ J tq (s), and 

E1{atq (s) = a} − ptq (s)(a | s) Ks,q −1

 = 0 .

The martingale strong law and infinite visitation imply max s ∥bσsT − σsT ∥1 → 0 almost surely. Let δT := max s ∥bσsT − σsT ∥1. For every v ∈ [0 , H ]|S| , the Bellman operators induced by bσT and 

σT differ by at most Hδ T in sup norm; the same bound holds for the operators induced by any fixed deviation βi against their opponents’ marginals. Since these operators are γ-contractions, max 

> i

(

∥V bσT 

> i

− V σT 

> i

∥∞, sup 

> βi

∥V βi,bσT, −i 

> i

− V βi,σ T, −i 

> i

∥∞

)

≤ H2δT .

Therefore, | Gap MCCE (bσT ) − Gap MCCE (σT )| ≤ 2Hδ T → 0 almost surely. This proves (35). For v ∈ R|S| , write sp( v) := max r∈S v(r) − min r∈S v(r). 57 Proof of Proposition 8.5. Fix i, s, b i, and Π, and draw a ∼ Π( · | s). By (36) and (37), TV( P (· | s, (bi, a −i)) , P (· | s, a )) ≤ Lswi∥ϕi,s (bi) − ϕi,s (ai)∥ ≤ Lswi∆i,s .

For probability vectors α, α ′ and any vector v, |⟨ α−α′, v ⟩| ≤ TV( α, α ′) sp( v). In C(bμT ), the averaged law is σT and each sampled policy is one of the behavior profiles pt. Since their canonical values belong to [0 , H ]|S| , sp( V σT 

> i

− V pt 

> i

) ≤ 2H. Substitution into (33) gives 

C(bμT ) ≤ 21 − γ max 

> i, s 0∈S
> βi:S→ Ai

X

> s

ωβi,σ T, −i 

> i,s 0

(s)Lswi∆i,s .

Because ωβi,σ T, −i 

> i,s 0

is a probability distribution over S,

C(bμT ) ≤ 21 − γ max  

> i,s

Lswi∆i,s .

Corollary 8.3 proves the claimed bounds. Under (38), any stationary transition matrix satisfies Pσ = η1q⊤ + (1 − η) ePσ for some stochastic matrix ePσ. Hence sp( Pσv) ≤ (1 − η) sp( v) for every vector v. Since V σi = rσi + γP σV σi and 

rσi ∈ [0 , 1] |S| ,sp( V σi ) ≤ 1 + γ(1 − η) sp( V σi ),

so sp( V σi ) ≤ [1 − γ(1 − η)] −1. Therefore, sp( V σT 

> i

− V pt 

> i

) ≤ 2[1 − γ(1 − η)] −1, which gives the sharper bound. 

Proof of Corollary 8.8. Fix s, Π, and a. The Bellman equations give 

> N

X

> i=1

QΠ 

> i

(s, (a⋆i (s), a −i)) − QΠ 

> i

(s, a )

=

> N

X

> i=1

[ui(s, (a⋆i (s), a −i)) − ui(s, a )] + γ

> N

X

> i=1

P (· | s, (a⋆i (s), a −i)) − P (· | s, a ), V Π 

> i

. (93) By (39), the first term on the right-hand side of (93) is at least λ P 

> i

ui(s, a ⋆(s)) − (1 + ν) P 

> i

ui(s, a ). Moreover, Definition 8.4 gives TV( P (· | s, (a⋆i (s), a −i)) , P (· | s, a )) ≤ Lswi∆i,s .

Since V Π 

> i

∈ [0 , H ]|S| , the continuation term in (93) is therefore bounded below by −γHL s

P 

> i

wi∆i,s .Proposition 7.2 now proves (40). Under (38), the range of every V Π 

> i

is at most [1 − γ(1 − η)] −1 by the argument above, so the 58 same proof gives the sharper bound. 

# References 

S. Adlakha and R. Johari. Mean field equilibrium in dynamic games with strategic complementarities. 

Operations Research , 61(4):971–989, 2013. doi: 10.1287/opre.2013.1192. M. Bena¨ ım, J. Hofbauer, and S. Sorin. Stochastic approximations and differential inclusions. SIAM Journal on Control and Optimization , 44(1):328–348, 2005. doi: 10.1137/S0363012904439301. V. S. Borkar. Stochastic approximation with two time scales: The general case. Stochastic Processes and their Applications , 190:104759, 2025. doi: 10.1016/j.spa.2025.104759. Y. Cai, H. Luo, C.-Y. Wei, and W. Zheng. Near-optimal policy optimization for correlated equilibrium in general-sum Markov games. In Proceedings of the 27th International Conference on Artificial Intelligence and Statistics , volume 238 of Proceedings of Machine Learning Research ,pages 3889–3897. PMLR, 2024. S. Chandak. Nonexpansive mappings in two-time-scale stochastic approximation: Finite-time analysis. SIAM Journal on Control and Optimization , 64(4), 2026. doi: 10.1137/25M1725000. S. Chandak, S. U. Haque, and N. Bambos. Finite-time bounds for two-time-scale stochastic approximation with arbitrary norm contractions and Markovian noise. In 2025 IEEE 64th Conference on Decision and Control (CDC) , pages 6095–6101. IEEE, 2025. doi: 10.1109/ CDC57313.2025.11312393. C. Daskalakis, D. J. Foster, and N. Golowich. Independent policy gradient methods for competitive reinforcement learning. In Advances in Neural Information Processing Systems , volume 33, pages 5527–5540. Curran Associates, Inc., 2020. URL https://proceedings.neurips.cc/paper/ 2020/hash/3b2acfe2e38102074656ed938abf4ac3-Abstract.html .C. Daskalakis, N. Golowich, and K. Zhang. The complexity of Markov equilibrium in stochastic games. In G. Neu and L. Rosasco, editors, Proceedings of Thirty Sixth Conference on Learning Theory , volume 195 of Proceedings of Machine Learning Research , pages 4180–4234. PMLR, 2023. URL https://proceedings.mlr.press/v195/daskalakis23a.html .D. Ding, C.-Y. Wei, K. Zhang, and M. Jovanovic. Independent policy gradient for large-scale Markov potential games: Sharper rates, function approximation, and game-agnostic convergence. In K. Chaudhuri, S. Jegelka, L. Song, C. Szepesvari, G. Niu, and S. Sabato, editors, Proceedings of the 39th International Conference on Machine Learning , volume 162 of Proceedings of Machine Learning Research , pages 5166–5220. PMLR, 2022. URL https://proceedings.mlr.press/ v162/ding22b.html .59 L. Erez, T. Lancewicki, U. Sherman, T. Koren, and Y. Mansour. Regret minimization and convergence to equilibria in general-sum Markov games. In Proceedings of the 40th International Conference on Machine Learning , volume 202 of Proceedings of Machine Learning Research , pages 9343–9373. PMLR, 2023. URL https://arxiv.org/abs/2207.14211 . Updated arXiv version v3, June 2025, includes a correction. G. Farina, A. Kontogiannis, I. Panageas, and V. Pollatos. The complexity of computing coarse correlated equilibria in Markov games with a single controller, 2026. D. J. Foster, N. Golowich, and S. M. Kakade. Hardness of independent learning and sparse equilibrium computation in Markov games. In Proceedings of the 40th International Conference on Machine Learning , volume 202 of Proceedings of Machine Learning Research , pages 10188–10221. PMLR, 2023. URL https://proceedings.mlr.press/v202/foster23a.html .A. Giannou, K. Lotidis, P. Mertikopoulos, and E.-V. Vlatakis-Gkaragkounis. On the conver-gence of policy gradient methods to Nash equilibria in general stochastic games. In Ad-vances in Neural Information Processing Systems , volume 35, pages 7128–7141. Curran As-sociates, Inc., 2022. URL https://proceedings.neurips.cc/paper_files/paper/2022/hash/ 2f060912eacace9ce61ef339205ec54c-Abstract-Conference.html .H. Gu, X. Guo, X. Wei, and R. Xu. Mean-field multiagent reinforcement learning: A decentralized network approach. Mathematics of Operations Research , 50(1):506–536, 2025. doi: 10.1287/moor. 2022.0055. X. Guo, A. Hu, R. Xu, and J. Zhang. A general framework for learning mean-field games. Mathematics of Operations Research , 48(2):656–686, 2023. doi: 10.1287/moor.2022.1274. V. Hakami and M. Dehghan. Learning stationary correlated equilibria in constrained general-sum stochastic games. IEEE Transactions on Cybernetics , 46(7):1640–1654, 2016. doi: 10.1109/TCYB. 2015.2453165. J. Hartline, V. Syrgkanis, and E. Tardos. No-regret learning in bayesian games. In Advances in Neural Information Processing Systems , volume 28. Curran As-sociates, Inc., 2015. URL https://proceedings.neurips.cc/paper/2015/hash/ 3e7e0224018ab3cf51abb96464d518cd-Abstract.html .C. Jin, Q. Liu, Y. Wang, and T. Yu. V-learning—a simple, efficient, decentralized algorithm for multiagent reinforcement learning. Mathematics of Operations Research , 49(4):2295–2322, 2024. doi: 10.1287/moor.2021.0317. S. Leonardos, W. Overman, I. Panageas, and G. Piliouras. Global convergence of multi-agent policy gradient in Markov potential games. In International Conference on Learning Representations .OpenReview.net, 2022. URL https://openreview.net/forum?id=gfwON7rAm4 .60 B. Light. Computing and learning stationary mean field equilibria with scalar interactions: Algo-rithms and applications. arXiv preprint arXiv:2502.12024 , 2025. Q. Liu, Y. Wang, and C. Jin. Learning Markov games with adversarial opponents: Efficient algorithms and fundamental limits. In Proceedings of the 39th International Conference on Machine Learning , volume 162 of Proceedings of Machine Learning Research , pages 14036–14053. PMLR, 2022. C. Maheshwari, M. Wu, and S. Sastry. Convergence of decentralized actor–critic algorithm in general-sum Markov games. IEEE Control Systems Letters , 8:2643–2648, 2024. doi: 10.1109/ LCSYS.2024.3510193. W. Mao, H. Qiu, C. Wang, H. Franke, Z. Kalbarczyk, and T. Ba¸ sar. eO(t−1) convergence to (coarse) correlated equilibria in full-information general-sum Markov games. In Proceedings of the 6th Annual Learning for Dynamics and Control Conference , volume 242 of Proceedings of Machine Learning Research , pages 361–374. PMLR, 2024. H. B. McMahan. A survey of algorithms and analysis for adaptive online learning. Journal of Machine Learning Research , 18(90):1–50, 2017. URL https://jmlr.org/papers/v18/14-428.html .T. Nguyen-Tang and R. Arora. Learning in Markov games with adaptive adversaries: Policy regret, fundamental barriers, and efficient algorithms. In Advances in Neural Information Processing Systems , volume 37, pages 56268–56303, 2024. S. Perkins and D. S. Leslie. Asynchronous stochastic approximation with differential inclusions. 

Stochastic Systems , 2(2):409–446, 2013. doi: 10.1287/11-SSY056. G. Qu, A. Wierman, and N. Li. Scalable reinforcement learning for multiagent networked systems. 

Operations Research , 70(6):3601–3628, 2022. doi: 10.1287/opre.2021.2226. A. Rakhlin and K. Sridharan. Online learning with predictable sequences. In S. Shalev-Shwartz and I. Steinwart, editors, Proceedings of the 26th Annual Conference on Learning Theory , volume 30 of Proceedings of Machine Learning Research , pages 993–1019. PMLR, 2013. URL https: //proceedings.mlr.press/v30/Rakhlin13.html .T. Roughgarden. Intrinsic robustness of the price of anarchy. In Proceedings of the Forty-First Annual ACM Symposium on Theory of Computing , pages 513–522. Association for Computing Machinery, 2009. ISBN 9781605585062. doi: 10.1145/1536414.1536485. Y. Wang, Q. Liu, Y. Bai, and C. Jin. Breaking the curse of multiagency: Provably efficient decentralized multi-agent rl with function approximation. In Proceedings of the 36th Conference on Learning Theory , volume 195 of Proceedings of Machine Learning Research , pages 2793–2848, 2023. 61 L. Xiao. Dual averaging methods for regularized stochastic learning and online optimization. Journal of Machine Learning Research , 11(88):2543–2596, 2010. URL https://jmlr.org/papers/v11/ xiao10a.html .Q. Xie, Y. Chen, Z. Wang, and Z. Yang. Learning zero-sum simultaneous-move Markov games using function approximation and correlated equilibrium. Mathematics of Operations Research , 48(1): 433–462, 2023. doi: 10.1287/moor.2022.1268. Y. Yan, G. Li, Y. Chen, and J. Fan. Model-based reinforcement learning for offline zero-sum Markov games. Operations Research , 72(6):2430–2445, 2024. doi: 10.1287/opre.2022.0342. W. Zhan, J. D. Lee, and Z. Yang. Decentralized optimistic hyperpolicy mirror descent: Provably no-regret learning in Markov games. In International Conference on Learning Representations ,2023. M. Zinkevich. Online convex programming and generalized infinitesimal gradient ascent. In 

Proceedings of the Twentieth International Conference on Machine Learning , pages 928–936. AAAI Press, 2003. 62