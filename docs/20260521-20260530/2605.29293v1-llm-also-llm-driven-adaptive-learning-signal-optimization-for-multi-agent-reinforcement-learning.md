---
title: "LLM-ALSO: LLM-Driven Adaptive Learning-Signal Optimization for Multi-Agent Reinforcement Learning"
title_zh: LLM-ALSO：面向多智能体强化学习的LLM驱动的自适应学习信号优化
authors: "Xiaoguang Wu, Zhi Zheng, Hui Xiong"
date: 2026-05-28
pdf: "https://arxiv.org/pdf/2605.29293v1"
tags: ["query:marl"]
score: 9.0
evidence: 大语言模型驱动的自适应学习信号优化用于合作型多智能体强化学习
tldr: 多智能体强化学习在稀疏奖励下训练引导困难，现有LLM方法多面向单智能体且缺乏适应性。本文提出LLM-ALSO，通过迭代诊断、提议和分支验证实现自适应学习信号优化。Critic LLM诊断失败，Generator LLM生成候选奖励塑形，验证后更新。实验表明该方法显著提升稀疏奖励任务性能和学习效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.29293v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 796, \"height\": 559, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.29293v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1639, \"height\": 905, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.29293v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 796, \"height\": 408, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.29293v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 803, \"height\": 493, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.29293v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 809, \"height\": 502, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.29293v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 795, \"height\": 590, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.29293v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 801, \"height\": 758, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.29293v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 702, \"height\": 178, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.29293v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 826, \"height\": 643, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.29293v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 797, \"height\": 454, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.29293v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1185, \"height\": 543, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.29293v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 686, \"height\": 735, \"label\": \"Table\"}]"
motivation: 解决稀疏奖励下MARL训练引导难，现有方法需大量人工设计，LLM方法缺乏多智能体适应性和迭代验证。
method: 提出LLM-ALSO框架：Critic LLM诊断失败，Generator LLM生成候选奖励塑形，短视验证后更新主训练轨迹。
result: 在稀疏奖励合作MARL任务中，LLM-ALSO相比基线提升稀疏评估性能和样本效率。
conclusion: 迭代LLM驱动与分支验证机制有效利用LLM灵活性，可靠优化MARL学习信号，减少不可靠修改风险。
---

## 摘要
有效的训练时引导是多智能体强化学习（MARL）的核心，但在稀疏奖励环境中仍然困难，因为弱监督限制了协调和策略改进，而现有方法通常需要大量领域专业知识或手动设计工作。大型语言模型（LLM）为灵活的学习信号设计提供了有前景的替代方案，然而现有基于LLM的方法大多仍面向单智能体、一次性或对合作MARL的演化训练动态验证不足。为克服这些局限，我们提出LLM-ALSO，一种针对MARL的迭代式LLM驱动自适应学习信号优化框架。LLM-ALSO并非直接部署LLM生成的奖励，而是将自适应分解为迭代诊断、提议和验证：批判者LLM根据稀疏回报指标和紧凑行为证据诊断特定阶段的训练和协调失败，生成者LLM根据诊断提出候选奖励塑形配置，分支验证反馈在候选配置影响主训练轨迹之前对其进行优化。通过短期验证和阶段感知自适应，LLM-ALSO仅将验证过的更新纳入训练，降低了不可靠的LLM生成修改带来的风险。在稀疏奖励合作MARL任务上的实验表明，LLM-ALSO提升了稀疏评估表现和学习效率。

## Abstract
Effective training-time guidance is central to multi-agent reinforcement learning (MARL), yet remains difficult in sparse-reward settings where weak supervision limits coordination and policy improvement, and existing methods often require substantial domain expertise or manual design effort. Large language models (LLMs) provide a promising alternative for flexible learning-signal design, yet existing LLM-based methods remain largely single-agent-oriented, one-shot, or weakly validated for the evolving training dynamics of cooperative MARL. To address these limitations, we propose LLM-ALSO, an iterative LLM-driven adaptive learning-signal optimization framework for MARL. Rather than directly deploying LLM-generated rewards, LLM-ALSO decomposes adaptation into iterative diagnosis, proposal, and validation: a Critic LLM diagnoses stage-specific learning and coordination failures from sparse-return metrics and compact behavior evidence, a Generator LLM proposes candidate reward-shaping configurations conditioned on the diagnosis, and branch-validation feedback refines candidates before they affect the main training trajectory. Through short-horizon validation and stage-aware adaptation, LLM-ALSO promotes only validated updates into training, reducing the risk of unreliable LLM-generated modifications. Experiments on sparse-reward cooperative MARL tasks show that LLM-ALSO improves sparse-evaluation performance and learning efficiency.

---

## 论文详细总结（自动生成）

# 论文总结：LLM-ALSO：面向多智能体强化学习的LLM驱动的自适应学习信号优化

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机：** 多智能体强化学习（MARL）在稀疏奖励环境中面临训练引导困难，由于监督信号稀疏，智能体难以发现协调行为。现有方法（如手工设计奖励塑形、内在动机、基于学习的内在奖励生成）通常需要大量领域专业知识或依赖预定义信号形式，难以适应训练过程中协调需求的动态变化。同时，近年来大型语言模型（LLM）为奖励设计提供了灵活方案，但已有LLM方法多为单智能体设计、一次性生成或缺乏对合作MARL演化训练动态的迭代验证。
- **整体含义：** 论文提出一种将LLM用于MARL训练时学习信号自适应的新范式，通过迭代诊断、提议和验证，实现阶段感知的奖励塑形优化，旨在提升稀疏奖励场景下的协调能力和学习效率。

## 2. 方法论

### 核心思想
- **LLM-ALSO** 是一个迭代式LLM驱动的自适应学习信号优化框架，用于合作MARL。它不直接部署LLM生成的奖励，而是将自适应分解为三个阶段：诊断（Critic LLM）、提议（Generator LLM）和验证（短视分支验证）。只有经过验证的更新才会被纳入主训练轨迹，从而降低不可靠LLM修改的风险。

### 关键技术细节
1. **结构化PBRS配置空间：** 使用基于势能的奖励塑形（PBRS）：  
   `r_shape_t = β(γΦθ(s̃_{t+1}) - Φθ(s̃_t))`  
   其中势能函数 `Φθ(s̃)` 是多个可解释任务组件的加权组合（如采集进度、接近行为、覆盖与发现、目标分配、联合采集就绪、后期稳定性），权重满足非负且和为1，模式m约束候选方向。该空间保持可解释性并限制奖励破解。
2. **双LLM引擎：**
   - **Critic LLM：** 根据稀疏回报指标和紧凑行为证据（如覆盖率、接近目标移动、失败采集尝试等）诊断阶段特定的学习和协调失败，输出集成指导卡（指定故障模式、证据、约束和风险）。
   - **Generator LLM：** 基于指导卡，在结构化PBRS配置空间内提出少量候选配置，包括定向更新、探索性替代和保守调整。LLM不能引入任意奖励项或策略干预。
3. **初始搜索阶段：** 从稀疏诊断运行开始，通过两轮搜索（每轮3个候选，每候选训练85万步）选择初始奖励塑形配置和终点检查点，用于引导稀疏奖励学习。
4. **检查点验证阶段：** 在固定的干预检查点（如1.0M、1.5M步）上，构建当前配置的“无变化”对照分支（从同一检查点继续，不修改塑形），同时评估LLM提出的更新分支（短视训练，如30万步）。分支结果返回给双LLM进行第二轮细化。最终由确定性验证门基于稀疏回报和稳定性标准决定是否采纳更新（正边际且无不稳定才采纳）。若采纳，则将该分支的终点和配置提升为主轨迹；否则保留当前配置。

### 公式或算法流程（文字说明）
- 训练时智能体接收 `r_train = r_env + r_shape`，评估时仅使用原始稀疏奖励 `r_env`。
- 初始阶段：稀疏诊断→Critic分析→Generator提议→两轮搜索→选择 (θ0, c0)。
- 验证阶段：在检查点Cj处，构建上下文（当前配置、指标、行为证据、分支历史等）→Critic诊断→Generator提议候选集→每个候选从cj开始短视训练→比较各分支与无变化对照的得分（综合最后k均值、AUC、最终值、最佳值，并惩罚不稳定性）→若某候选边际明显为正且通过稳定性检查，则提升该分支；否则保留。

## 3. 实验设计

### 数据集/场景
- 使用 **Level-Based Foraging (LBF)** 环境，五种配置，覆盖不同大小、智能体数量和食物数量：
  - 8×8-2p-1f, 8×8-2p-2f, 2s-8×8-2p-2f, 10×10-2p-1f, 15×15-3p-4f。
- 这些任务要求智能体探索、接近可行食物、分配目标并协调联合采集，在稀疏任务反馈下学习。

### Benchmark
- 以原始稀疏环境奖励作为评估目标（训练中使用的塑形奖励在评估时禁用）。
- 底层MARL算法：**QMIX**（值基）和 **MAPPO**（策略梯度），共10个（5配置×2算法）实验组合。

### 对比方法
- **Sparse:** 仅使用原始稀疏奖励。
- **Fixed-RS:** 使用预定义的静态结构化PBRS信号（从三个非LLM静态方向中选一个，全程固定）。
- **Single-LLM-RG (S-LLM-RG):** 使用单个LLM在初始搜索阶段生成并选择一组PBRS配置，然后从头训练并固定该配置。
- **LLM-ALSO:** 提出的方法，使用双LLM引擎和检查点验证自适应更新。

## 4. 资源与算力

- 论文中**未明确说明**使用的GPU型号、数量或具体训练硬件配置。只提到训练步数的预算设置（大多数实验使用2.05M步的短预算，MAPPO 15×15-3p-4f使用10M步的中预算）。初始搜索每候选85万步，分支验证每分支30万步（短预算）或50万步（中预算）。所有比较方法共享相同的最终训练步数。
- 需要指出：文中没有提供GPU类型、数量、运行时间等算力细节。

## 5. 实验数量与充分性

- **主要结果表格（Table 1）** 覆盖了5个LBF配置×2种算法=10个设置，每个设置报告最大测试稀疏回报和平均测试稀疏回报（均值±标准差，基于多个随机种子）。整体上LLM-ALSO在大多数设置中取得最高平均回报，尤其在困难任务（QMIX 10×10-2p-1f, MAPPO 15×15-3p-4f）上提升显著。
- **消融实验（Ablation）：** 在QMIX 10×10-2p-1f上比较LLM-ALSO与Single-LLM Adapt（将双LLM替换为单LLM，但保持相同验证流程），结果显示双LLM交互在中后期提升了平均回报（0.32 vs 0.26），最大回报相近（0.95 vs 0.94）。
- **阶段分析：** 在QMIX 10×10-2p-1f案例中详细展示了两个检查点（C1、C2）的分支验证曲线和得分边际，说明验证门如何决定采纳或保留。
- **充分性：** 实验覆盖了基础方法和主要消融，但仅限于LBF环境，缺乏更广泛MARL环境（如SMAC、MAMuJoCo）的验证。此外，随机种子数未明确说明（但从标准差看可能为3-5）。总体实验设计较为客观公平，所有方法使用相同底层算法和超参数，仅学习信号不同，且评估基于原始稀疏奖励避免了奖励破解。

## 6. 主要结论与发现

- LLM-ALSO在大多数LBF设置下**提升了稀疏回报的平均值和峰值表现**，尤其在较难的协调任务（如QMIX 10×10-2p-1f, MAPPO 15×15-3p-4f）中优势明显。
- **阶段感知的自适应**优于固定奖励塑形或单LLM一次性生成。初始搜索提供了基础引导，而检查点验证能在适当时候替换配置（如C1案例），同时避免在收益不足时错误更新（如C2案例保留无变化）。
- **双LLM分离角色**（诊断+提议）比单LLM更有效地产生阶段合适的候选，从而改善后续主轨迹。
- 短视分支验证**有效分离了LLM提议与实证采纳**，减少了不可靠LLM输出直接应用于训练的风险。

## 7. 优点

- **创新性：** 将LLM用于MARL学习信号优化，并引入基于分支验证的迭代自适应机制，区别于一次性或直接部署的LLM方法。
- **可靠性：** 通过结构化PBRS空间约束、诊断-提议分离、短视分支验证和无变化对照，显著降低了LLM不可靠修改的风险，提升了方法的鲁棒性。
- **可解释性：** 势能函数基于可解释任务组件（采集、接近、覆盖等），LLM操作限于配置选择而非任意代码生成，便于分析和调试。
- **实验设计严谨：** 所有对比方法使用相同底层算法和超参数，评估基于原始稀疏奖励，避免奖励破解；消融实验直接隔离了双LLM交互的影响。

## 8. 不足与局限

- **环境覆盖不足：** 实验仅基于Level-Based Foraging环境，未在更广泛的MARL基准（如SMAC、MAMuJoCo、多机器人模拟）上验证，泛化性有待确认。
- **依赖预定义组件空间：** 方法的性能受限于预定义的PBRS组件（六个LBF专用组件）的表述能力，若组件不够丰富或不适配目标任务，自适应效果可能受限。
- **额外计算开销：** 检查点验证需要额外短视分支训练（每次检查点需训练7个分支×30万步），虽然计入工作流预算，但相比固定基线增加了训练成本。
- **局部验证的局限性：** 短视分支结果不能完美预测长期行为，验证门基于有限时间窗口的分数决定是否采纳，可能错过长期效用更好的候选（或采纳短期好但长期差的候选）。
- **资源信息缺失：** 未报告GPU型号、数量等硬件资源消耗，不利于复现和资源评估。
- **随机种子数未明确：** 虽然报告了标准差，但未说明具体种子数，可能影响对结果显著性的判断。

（完）
