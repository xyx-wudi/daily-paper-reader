---
title: Provably Efficient Federated Reinforcement Learning with Linear Function Approximation and Logarithmic Communication Cost
title_zh: 具有线性函数逼近与对数通信成本的可证明高效联邦强化学习
authors: "Zihang Liang, Haochen Zhang, Lingzhou Xue"
date: 2026-08-31
pdf: "https://arxiv.org/pdf/2609.00193v1"
tags: ["query:marl"]
score: 8.0
evidence: 面向分布式智能体的联邦强化学习，降低通信成本；契合去中心化多智能体强化学习方法
tldr: 针对联邦在线强化学习中现有方法需共享原始轨迹、通信成本随回合数线性增长且违反隐私约束的问题，提出首个可证明高效的联邦线性函数近似算法Fed-LSVI。该算法通过基于行列式的事件触发同步与逐步后向更新机制，仅交换压缩的充分统计量，实现协作学习最优策略。其遗憾界为~O(sqrt(M d^3 H^4 T))，匹配已知最优；通信成本仅为T的对数依赖，显著优于先前方法。
source: arxiv
selection_source: fresh_fetch
motivation: 现有联邦强化学习算法共享完整轨迹，导致通信成本随回合数线性增长并泄露隐私，缺乏高效在线线性函数近似方案。
method: 提出Fed-LSVI，采用行列式事件触发同步与逐步后向更新，仅交换压缩充分统计量实现协作学习。
result: 达到~O(sqrt(Md^3H^4T))遗憾界，匹配最优；通信成本降至对数依赖T，兼顾隐私与效率。
conclusion: 首个可证明高效的联邦在线线性强化学习算法，为联邦环境下的多智能体学习提供通信与隐私双重保障。
---

## 摘要
我们研究具有线性函数逼近的联邦在线强化学习。尽管最近的多智能体强化学习算法实现了强遗憾保证，但它们通常需要共享原始轨迹。这种依赖导致通信成本随回合数线性增长，并违反联邦设置的隐私约束。为解决这些局限性，我们提出Fed-LSVI，这是针对情节马尔可夫决策过程中具有线性函数逼近的在线强化学习的首个可证明高效的联邦算法。通过将基于行列式的事件触发同步与逐步反向更新机制相结合，Fed-LSVI使智能体能够仅交换压缩的充分统计量来协作学习最优策略。我们证明Fed-LSVI实现了$\widetilde{\mathcal O}(\sqrt{Md^3H^4T})$的遗憾界，其中$d$是特征维度，$H$是时间跨度长度，$M$是智能体数量，$T$是每个智能体的情节数，与多智能体在线强化学习中最著名的线性函数逼近遗憾界相匹配。此外，通过遵循联邦设置的严格通信和隐私约束，Fed-LSVI将通信成本降低到仅对$T$的对数依赖，这相较于先前方法代表了显著改进。

## Abstract
We study federated online reinforcement learning with linear function approximation. While recent multi-agent reinforcement learning algorithms achieve strong regret guarantees, they typically require sharing raw trajectories. This reliance incurs a communication cost that scales linearly with the number of episodes and violates the privacy constraints of federated settings. To address these limitations, we propose Fed-LSVI, the first provably efficient federated algorithm for online reinforcement learning with linear function approximation in episodic Markov decision processes. By integrating a determinant-based event-triggered synchronization with a stepwise backward update mechanism, Fed-LSVI enables agents to collaboratively learn an optimal policy by exchanging only compressed sufficient statistics. We prove that Fed-LSVI achieves a regret bound of $\widetilde{\mathcal O}(\sqrt{Md^3H^4T})$, where $d$ is the feature dimension, $H$ is the horizon length, $M$ is the number of agents, and $T$ is the number of episodes per agent, matching the best-known regret for multi-agent online reinforcement learning with linear function approximation. Moreover, by following the stringent communication and privacy constraints of the federated setting, Fed-LSVI reduces the communication cost to only logarithmic dependence on $T$, representing a significant improvement over prior methods.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- 论文关注**联邦在线强化学习 + 线性函数逼近**场景。现有具备理论保证的多智能体强化学习（MARL）算法（如 Dubey & Pentland 2021、Min et al. 2023、Hsu et al. 2024）通常要求各智能体将**原始轨迹**（raw trajectories）上传到中心服务器，才能进行协作策略学习。
- 这种机制带来两个关键问题：
  1. **通信成本高**：传输量与回合数 T 呈**线性增长**（O(MHT)）；
  2. **隐私风险大**：原始轨迹可能包含敏感的个体级数据（如医疗记录、推荐系统中的用户行为），违反了联邦学习（Federated Learning）中“数据不出本地”的核心约束。
- 论文由此提出了一个核心研究问题：**能否设计一种联邦在线 RL 算法，在共享线性 MDP 设定的基础上，同时实现样本效率（sublinear regret）和高通信效率（logarithmic communication），且无需共享原始轨迹？**
- 整体意义在于：这是**首个**在联邦约束下、可证明高效（provably efficient）的线性函数逼近在线 RL 算法，填补了该方向的空白；并证明了联邦隐私约束与统计效率**可以共存**。

## 2. 论文提出的方法论

- **核心算法：Fed-LSVI**（Federated Least-Squares Value Iteration）。它是在单智能体 LSVI-UCB 基础上的联邦化扩展，但包含两大核心技术组件：

### （1）基于行列式的事件触发同步（Determinant-based Event-triggered Synchronization）
- 每轮（round）中，每个智能体局部收集经验并累加协方差矩阵 Λ 后，若某个智能体的局部协方差增量满足触发条件：
  \[
  \frac{\det(\Lambda_h^{\rm ser} + \Lambda_h^{m,\rm loc})}{\det(\Lambda_h^{\rm ser})}\ge \gamma^{\Delta t}
  \]
  则该智能体向服务器发送同步信号，服务器广播后各智能体结束当前轮次。
- 该机制保证：**通信轮数 K = O(log T)**（Theorem 3），且不牺牲统计效率。

### （2）逐步后向同步协议（Stepwise Backward Synchronization）
- 针对 LSVI 类算法中固有的“后向更新结构”（step-h 回归目标依赖于 step-h+1 的更新后的值函数 V），提出**反向逐步同步**：
  - 从 h=H 开始，智能体计算基于当前最新全局 V_{h+1} 的统计量并上传；
  - 服务器聚合（公式 6）并更新 w_h = (Λ)^{-1} b（公式 7），再广播回各智能体；
  - 智能体更新对应 layer 的乐观 Q/V 估计（公式 2）后，再进行 step h-1 的统计量计算。
- 该方法避免了一次性批量同步导致的**陈旧响应（stale-response）偏差**问题，是整篇论文理论分析中的关键创新。

### （3）理论保证
| 设定 | 遗憾界（Regret） | 通信成本 |
|---|---|---|
| 同质设定（Homogeneous） | \(\widetilde{\mathcal{O}}(\sqrt{Md^3H^4T})\)（Theorem 1） | \(O(M^2d^3H + Md^3H^2\log(1+MT/d))\) |
| 错误设定（Misspecified，ξ-近似线性 MDP，各智能体异质） | 包含 ξ·M dH²T 等异质性惩罚项；当 ξ=\(\widetilde{\mathcal{O}}\left(\sqrt{d/(MT)}\right)\) 时退化到同质设定界（Theorem 2） | 同上（对数依赖 T） |

- 当 \(M=1\) 时，Fed-LSVI 退化为一种**低切换成本（low-switching-cost）的 LSVI-UCB 变体**，保持单智能体最优遗憾界的同时策略切换次数仅为对数级。

## 3. 实验设计

- 该论文是**纯理论性（theory-only）论文**，**不包含任何实验验证**。
- 没有使用数据集、基准（benchmark）场景，也没有与其他方法进行实验对比。
- 其主要“验证方式”为：
  1. **数学证明**（附录 B–E 中给出完整理论推导）；
  2. 与**已有理论结果**在界限上作比较（与 Dubey & Pentland 2021、Min et al. 2023、Hsu et al. 2024 的遗憾界与通信界进行理论对比）。

## 4. 资源与算力

- 论文中**未提及**任何算力相关的内容，包括 GPU 型号、数量、训练时长、优化器等。
- 原因在于该论文为**纯数学理论工作**，不依赖任何数值实验、仿真环境或大规模训练设备。因此，无需计算资源说明。

## 5. 实验数量与充分性

- 论文**没有实验**，因此不存在实验数量、消融实验、基准比较或复现实验。
- 从“实验充分性”角度评估：
  - 若以**理论分析的充分性**来衡量，论文提供了遗憾界（Theorem 1）、鲁棒性界（Theorem 2）和通信界（Theorem 3）三层完整证明，并有详尽的辅助引理（附录 B–E），整体理论论证是严谨且充分的。
  - 若以**实证验证**作为标准，则论文**缺乏**数值仿真，无法观察算法在有限样本下的实际表现、边界行为或可扩展性，属于明显的空白。

## 6. 论文的主要结论与发现

1. **隐私与效率可兼得**：满足联邦学习严格的数据本地化约束下（不传输原始轨迹），仍可实现与最先进 MARL 线性函数逼近算法**匹配的遗憾界**，即 \(\widetilde{\mathcal{O}}(\sqrt{Md^3H^4T})\)。
2. **通信成本呈指数级改善**：通信量从 O(MHT)（线性于 T）降低至 **O(log T)** — 依赖 log T，且每轮仅传输 O(M d²H) 个标量。这一改进源于确定的触发条件限制了有效通信轮数。
3. **多智能体加速保持**：平均每个智能体的遗憾随 M 增加以 \(\sqrt{M}\) 速度下降，即“线性加速”式改进在联邦设置下仍成立。
4. **对异质性具备鲁棒性**：在 ξ-近似线性 MDP 的轻度异质设定下，只要 ξ 足够小（ξ=\(\widetilde{\mathcal{O}}(\sqrt{d/(MT))}\)），遗憾界不退化；否则异质性会产生显式惩罚项。
5. **后向同步协议的价值**：证明了通过逐步后向的模型广播 + 局部充分统计量上传，可以完全规避陈旧响应偏差，这是未来联邦 RL 算法设计的重要方法论参考。

## 7. 优点

- **理论贡献鲜明**：首篇针对线性函数逼近联邦在线 RL 的可证明效率工作，填补了 tabular FRL 与线性函数逼近之间的空白。
- **机制设计巧妙**：两个核心设计（事件触发同步 + 后向逐步更新）互为支撑，直接回应了联邦约束下无法共享原始轨迹的两大难点（通信量 + stale-response）。
- **通信界强且可解释**：通信上界清晰刻画了 K（通信轮数）与 γ、M、d、T 的关系；且结果表明通信开销不依赖 H 的次数反复，只有 d³H² 对数项，实际可行性较高。
- **统一框架**：M=1 特化到低切换 LSVI-UCB；同质 vs. 异质性中的退化分析提供了统一视角。
- **分析深度充分**：自归一化集中不等式、覆盖数、椭球势能引理等工具使用得当，证明结构完整（约 35 页）。

## 8. 不足与局限

- **无实验验证**：
  - 没有任何数值实验或仿真支持，导致算法在实际任务中（如联邦医疗、移动边缘计算等）的可部署性、数值稳定性未经验证。
- **假设较强**：
  - 基于“线性 MDP”（Definition 1），要求转移与奖励可被已知特征 ϕ 精确线性表示；
  - 异质性设定（Definition 2）要求各智能体共享同一特征映射且偏离程度 ξ 受限，对较强分布漂移或特征不一致的情况覆盖有限；
  - 同步通信假设：所有智能体以同一 episode 索引同步推进，现实中部分智能体掉线、延时或异步参与的情况未考虑。
- **通信模型较理想化**：
  - 论文以“传输标量数量”衡量通信成本，假设传输无噪声、无压缩误差；实际中浮点精度、编码开销与 channel capacity 未被建模。
- **仅考虑回合内固定策略**：
  - 同步轮内所有智能体执行同一策略，不能像完全异步算法那样即时适应局部数据差异。
- **常数没有精细化**：理论界中的常数依赖于 cβ、cβ′，是宽松的绝对常数，未做数值紧化。

（完）
