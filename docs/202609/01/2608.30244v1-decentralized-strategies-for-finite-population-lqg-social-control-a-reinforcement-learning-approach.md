---
title: "Decentralized Strategies for Finite Population LQG Social Control: A Reinforcement Learning Approach"
title_zh: 有限群体LQG社会控制的去中心化策略：一种强化学习方法
authors: "Liangyuan Guo, Bing-Chang Wang, Guangchen Wang"
date: 2026-08-31
pdf: "https://arxiv.org/pdf/2608.30244v1"
tags: ["query:marl"]
score: 8.0
evidence: 面向去中心化有限总体LQG社会控制的无模型强化学习算法
tldr: 针对有限人数线性二次高斯社会控制问题，提出一种无模型强化学习算法。该算法无需系统矩阵先验知识，通过求解两个代数Riccati方程获得社会最优策略，适用于有限时域与无限时域，且代价权重可为非正定。无限时域收敛性基于Lyapunov型算子谱性质分析，数值算例验证了有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.30244v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 852, \"height\": 821, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.30244v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 856, \"height\": 304, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.30244v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 678, \"height\": 586, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.30244v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 872, \"height\": 1232, \"label\": \"Figure\"}]"
motivation: 现有社会控制方法依赖模型知识且要求权重半正定，需发展无模型算法以应对实际系统参数未知的挑战。
method: 设计无模型强化学习算法，分别求解有限与无限时域的两个代数Riccati方程，并分析Lyapunov算子谱性质保证收敛。
result: 算法在无需系统矩阵下达到社会最优，数值仿真验证了所提方法的有效性。
conclusion: 提出的RL方法统一了有限与无限时域求解框架，扩展了可解问题范围并降低模型依赖。
---

## 摘要
本文针对具有乘性噪声的有限群体线性二次高斯（LQG）去中心化社会控制问题，提出了一种新颖的无模型算法。成本函数中的状态和控制权重不限于半正定。对于有限时域和无限时域两种情况，目标是通过求解两个代数Riccati方程（AREs）获得社会最优，而无需系统矩阵的先验知识。然后，我们完成了求解去中心化社会控制问题的无模型算法的设计。特别是在无限时域情况下，算法的收敛性基于对Lyapunov型算子谱性质的分析。比较了有限时域和无限时域情况下强化学习（RL）解的差异。最后，通过数值算例验证了所提出算法的有效性。

## Abstract
This paper presents a novel model-free algorithm for the finite-population linear quadratic Gaussian (LQG) decentralized social control problem with multiplicative noise. The state and control weights in the cost functional are not limited to be positive semidefinite. For both finite-horizon and infinite-horizon cases, the goal is to obtain a social optimum by solving two algebraic Riccati equations (AREs), without requiring prior knowledge of the system matrices. Then, we complete the design of a model-free algorithm for solving the decentralized social control problem. Especially, in the infinite-horizon case, the algorithm's convergence is based on analyzing the spectral property of the Lyapunov-type operator. The differences of reinforcement learning (RL) solutions between the finite-horizon and infinite-horizon cases are compared. Finally, the effectiveness of the proposed algorithm is demonstrated by a numerical example.

---

## 论文详细总结（自动生成）

## 有限群体LQG社会控制的去中心化策略：一种强化学习方法——论文详细总结

### 一、核心问题与整体含义（研究动机与背景）

- **研究背景**：去中心化社会控制是大规模群体协同差分博弈中的重要方法，广泛应用于系统控制、金融数学、人群动力学和工程领域。其核心特征是多个智能体共同优化一个社会成本函数。
- **现有研究不足**（本文识别的三个关键空白）：
  1. **有限群体系统性缺失**：经典平均场博弈/控制理论主要面向大规模或无限群体系统，但实际中的小规模或中等规模群体（如电动汽车充电调度、中小型人群系统）中，基于大群体极限推导的渐近最优策略会造成显著性能损失，且现有数据驱动方法无法直接推广到有限群体场景。
  2. **有限时域无模型算法稀缺**：现有无模型/强化学习算法绝大多数聚焦无限时域问题，有限时域问题的数据驱动方法研究近乎空白，且两种情形下RL解之间的关系尚不清晰。
  3. **非正定权重矩阵收敛分析不足**：已有策略迭代算法收敛性普遍依赖于权重矩阵Q和R的（半）正定性假设，对不定权重矩阵情形的收敛性分析十分匮乏。

### 二、方法论：核心思想、关键技术细节与算法流程

#### 2.1 核心思想

- 研究离散时间有限群体（N个同质智能体）乘性噪声LQG社会控制问题，将“无模型学习”与“去中心化社会最优”相结合。
- 核心洞察：若系统矩阵已知，社会最优可通过求解**两个耦合代数Riccati方程（AREs）**获得（引用自[32]）。因此，**若能设计不依赖系统矩阵A、B、C、D的迭代算法来逼近这两个AREs的解，即可实现无模型条件下的社会最优**。
- 方法路线为：**模型基策略迭代（model-based policy iteration）→ 等效无模型实现（利用系统轨迹数据 + 最小二乘求解）→ 收敛性严格证明**。

#### 2.2 关键技术细节

- **问题建模**：N个同质智能体，每个智能体动态方程为具有乘性噪声的离散时间随机差分方程 \( x_j(k+1) = Ax_j(k) + Bu_j(k) + (Cx_j(k) + Du_j(k)) w_j(k) \)；社会成本为各智能体个体成本之和，状态与控制权重矩阵Q、R**不要求半正定**（可为不定矩阵）。
- **最优性条件（模型已知时）**：问题线性可解当且仅当两个AREs（方程组(8)和(9)）存在满足正则条件的解。最优控制具有如下结构：
  \[
  u_{jk} = -\Upsilon_k^{-1}M_k(x_{jk} - \bar{x}_{jk}) - \bar{\Upsilon}_k^{-1}\bar{M}_k\bar{x}_{jk}
  \]
  即由“个体对均值的偏离”与“均值场项”两部分线性反馈叠加而成。

#### 2.3 算法流程（以文字说明）

- **第一阶段：模型基策略迭代（Theorem 1 & 2）**
  - 将第一个ARE转化为递推策略迭代过程：给定当前控制增益 \(K_k^i\)，先由Lyapunov型方程更新 \(P_k^i\)（方程(11)），再由 \(P_k^i\) 更新增益 \(K_k^{i+1}\)（方程(12)）。
  - 证明该迭代序列单调递减且有下界，收敛到ARE(8)的解。
  - 对第二个ARE，将 \(P_k + \bar{P}_k = S_k\) 组合为一个新方程（方程(18)），同样构造策略迭代（方程(19)-(20)），证明单调收敛。
  - **创新点**：当ARE中含有与控制相关的二次项 \(B^T\bar{P}B\) 时，直接矩阵求逆方法不可行，本文通过将两个耦合ARE合并为一个新方程（对S的迭代），绕开了这一障碍。

- **第二阶段：无模型化（模型基迭代的等效数据驱动实现）**
  - 利用贝尔曼方程与轨迹数据之间的等价关系（方程(27)），将含未知系统矩阵的递推关系转化为仅含可观测数据的线性方程组。
  - 通过对状态二阶矩、状态-控制外积等统计量进行向量化，构造数据矩阵 \(\Psi_k\) 和目标向量 \(\Xi_k\)，用**最小二乘**直接求解得到 \(P_k^i\)、中间矩阵 \(\Lambda_1^{k,i}\)、\(\Lambda_2^{k,i}\)，进而更新控制增益。
  - 第二个ARE类似处理（基于 \(\bar{x}\) 的动力学变换，方程(36)-(38)）。
  - 为保证数据充分性，要求满足**秩条件（持续激励条件，Assumption 2/3）**，必要时注入探测噪声（如随机噪声、指数衰减探测噪声、多频正弦信号等）。

- **第三阶段：无限时域情形（Problem 2）**
  - 系统闭环 \(L_2\)-可镇定条件下，两个AREs退化为代数方程(40)-(41)。
  - 模型基策略迭代（Theorem 5 & 6）需要**初始镇定增益**（可由LMI求解或系统辨识获得），其收敛性证明建立在对Lyapunov型算子 \( \mathcal{L}_i(X) \) 谱性质的分析上——若存在特征值≥1则导出矛盾，从而保证每次迭代增益均为镇定增益，且序列单调递减收敛。
  - 无模型版本（Theorem 7 & 8）类似地通过数据矩阵和最小二乘实现，并沿用谱分析证明所有迭代增益都是镇定增益。
  - **两个时域的桥接**（Theorem 9）：若将有限时域算法的初始增益设为与无限时域相同的常数镇定增益，则当时间范围趋于无穷时，有限时域策略迭代渐近等价于无限时域策略迭代。
  - **鲁棒性分析**（Theorem 10 & 11）：给出了小扰动下的输入-状态稳定性（ISS）界，表明当增益估计误差充分小时，Riccati解估计的误差有界，且若增益误差趋于零则解误差趋于零。

- **有限时域与无限时域的三大差异**（Remark 8）：
  1. **稳定性要求**：无限时域需要初始镇定增益和系统稳定性保证；有限时域无此要求。
  2. **控制策略时间特性**：无限时域策略为时不变的，有限时域策略为时变的（从终端时刻反向递推计算）。
  3. **数据利用效率**：无限时域可学习单一固定策略从而实现数据高效利用；有限时域需为每一步收集时序特定的数据，参数空间和复杂度显著增大。

### 三、实验设计

- **实验场景**：
  1. **有限时域场景（3维状态，100个智能体）**：参数为 Q = I₃，R = 1，Γ = 0.9I₃，T = 6，σ = 1，乘性噪声增益矩阵C和D取较小幅值。验证了100个智能体下状态轨迹和控制轨迹的行为。
  2. **无限时域场景（2维状态，10个智能体）**：Q为非正定矩阵（特征值为−0.49和1.43），专门验证算法在不定位权矩阵下的有效性。
- **Benchmark与对比**：
  - 本文**未与其他RL/数据驱动算法进行对比实验**，而是以模型已知时AREs的解析/数值解（\(P^*\)、\(\bar{P}^*\)）为基准，计算估计误差 \(\|P^i - P^*\|_F = 0.1123\)、\(\|\bar{P}^i - \bar{P}^*\|_F = 0.0641\)。
  - 参照了文献[32]的模型基条件作为理论最优基准。
- **敏感性分析**：对系统参数扰动（±0.2A/±0.5A、±0.3C/±0.2C等）和群体规模N（N=5、10、10000）进行了鲁棒性测试，结果显示增益估计误差在一定迭代次数后收敛。

### 四、资源与算力

- **文中未披露任何计算资源信息**，未提及GPU型号、数量、训练时长、CPU核数等。
- 从算法性质判断（有限维线性系统、最小二乘求解、两个2~3维数值算例），计算复杂度较低，普通工作站即可完成，但论文未对此做任何说明。

### 五、实验数量与充分性

- **实验组数**：两组核心数值实验（有限时域100智能体、无限时域10智能体）+ 敏感性分析。
- **充分性评估**：
  - 有限时域实验仅给出状态/控制轨迹图，未报告迭代误差曲线或收敛速度数据，验证力度有限（论文图表中缺少有限时域迭代过程图）。
  - 无限时域实验验证了不定权重矩阵的情形，给出了P和\(\bar{P}\)的迭代收敛图（图3）和误差数值，且参数扰动和N的敏感性分析（图4，6种情形）较为丰富，增强了说服力。
  - **与基线对比近乎空白**：未与其他无模型算法（如[49]、[50]中的算法）或传统RL方法进行性能对比，无法直接体现该方法的相对优势。
  - 缺少对有限时域中N取不同值、不同乘性噪声强度的系统实验，实验覆盖度有所不足。
  - 整体实验设计客观、可复现，但广度有限，以验证可行性为主而非充分对标竞争方法。

### 六、主要结论与发现

1. 提出了针对有限群体LQG社会控制问题的**首个模型无关算法**，可在完全未知系统矩阵的条件下获得去中心化社会最优策略，且权重矩阵允许为不定矩阵。
2. **有限时域和无限时域均建立了严格的收敛性保证**：有限时域基于迭代序列单调递减有界性；无限时域基于Lyapunov型算子谱分析，证明每次迭代产生的增益均为镇定增益。
3. **两种时域下RL解的差异**被清晰揭示：稳定性要求、策略时变性、数据利用效率三方面根本不同；且在时间范围趋于无穷时，有限时域算法渐近收敛到无限时域结果。
4. **鲁棒性保证**：小扰动下的输入-状态稳定性（ISS）界被建立，且增益扰动消除时Riccati解误差收敛到零。
5. 数值实验验证了100智能体（有限时域）和10智能体（无限时域、不定Q权矩阵）场景下算法的有效性。

### 七、优点与亮点

- **问题定位精准**：填补了有限群体系统无模型RL社会控制的研究空白，针对性强，切中实际应用需求。
- **理论深度突出**：收敛性分析严谨完整——有限时域采用单调迭代+逼近论证，无限时域采用Lyapunov算子谱性质分析，并首次为不定权重情形提供了收敛性保证。
- **算法设计巧妙**：将两个耦合ARE组合为一个S方程，避免了含控制二次项时矩阵直接求逆的不可行问题；将模型基迭代等价转化为数据驱动线性最小二乘，技术路线清晰。
- **连接有限/无限时域**：通过Theorem 9建立了两类算法的渐近等价关系，提供了统一视角，是理论贡献的亮点。
- **含鲁棒性分析**：非常少见地在社会控制RL算法中给出ISS型鲁棒界，对实际部署有指导意义。

### 八、不足与局限

- **实验验证范围有限**：
  - 有限时域实验缺少迭代收敛过程和误差对比数据；
  - N仅取100（有限时域）和10（无限时域），中等规模（如50~500）覆盖不足；
  - 缺少与现有算法（如[49]和[50]的大群体方法、[32]的模型基方法在已知参数时的性能对比）的直接对比实验。
- **理论假设较强**：
  - Assumption 1要求AREs存在正则解，实际中难以先验验证；
  - 无限时域需初始镇定增益（虽给出LMI求解建议，但增加了实际部署门槛）；
  - 秩条件（持续激励）的验证依赖数据统计量，论文未讨论数据量和噪声特性对收敛速率的具体影响。
- **乘性噪声假设简化**：仅考虑标量高斯噪声且同分布，未扩展至多通道噪声、相关工作噪声或非高斯噪声场景。
- **收敛速率与计算复杂度缺失**：未给出迭代收敛速率的理论界（除ISS界外）和算法复杂度分析，对实际工程选择的参考价值有限。
- **模型泛化局限**：方法局限于线性动力学+LQG设置，未讨论非线性系统、约束输入、通信/观测部分缺失（如部分可观测）等更一般的场景。
- **非正定权矩阵的理论充分条件**：虽在不定位权矩阵下证明了收敛，但未给出确保ARE解存在且正则的显式可检验条件，限制了直接实际应用中对参数选择的指导。

（完）
