---
title: Provably Optimal Learning Algorithms for Assistance Games
title_zh: 辅助博弈的可证明最优学习算法
authors: "Nivasini Ananthakrishnan, Mark Bedaywi, Michael I. Jordan, Stuart Russell, Nika Haghtalab"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08012v1"
tags: ["query:marl"]
score: 8.0
evidence: 重复辅助博弈的分散式算法及可证明遗憾界
tldr: "针对反复交互的辅助博弈问题，本文定义了辅助遗憾指标，并提出了首个可证明高效的分布式学习算法。该算法为知情的人类和不知情的助理分别设计，达到(1-1/e)近似的遗憾率Õ(T^{3/4})。同时证明优于(1-1/e)的近似是计算困难的。利用共享随机串可进一步优化至Õ(T^{1/2})，该速率最优。"
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.08012v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1705, \"height\": 258, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08012v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1703, \"height\": 411, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08012v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1716, \"height\": 109, \"label\": \"Table\"}]"
motivation: 现有辅助博弈缺乏在线场景下的高效学习算法，亟需可证明的遗憾最优方法。
method: 提出去中心化算法，将助理策略视为任意无遗憾算法，人类采用特定学习策略实现近似最优。
result: "实现(1-1/e)近似遗憾率Õ(T^{3/4})，伪去中心化版本达到最优Õ(T^{1/2})。"
conclusion: 首次为在线辅助博弈提供可证明高效算法，揭示近似最优与计算复杂性的平衡。
---

## 摘要
本文研究辅助博弈框架的在线变体，其中知情智能体与不知情智能体在T个时间步上重复交互以优化共同奖励函数。知情智能体（人类）观察世界的潜在状态，而不知情智能体（助手）仅观察人类的行动。我们首次为重复辅助博弈提供了可证明高效的学习算法。我们引入辅助遗憾的概念：交互累积效用与事后最优联合策略（将潜在状态映射到动作对）的累积效用之差。我们为人类和助手提出去中心化算法，实现了(1-1/e)近似辅助遗憾率~O(T^{3/4})，运行时间与动作和状态空间大小成多项式关系。这些算法是通用的；特别是，它们可以适应任何无遗憾算法作为助手。我们证明实现优于(1-1/e)的遗憾近似因子在计算上不可行。此外，我们展示了如何将这些通用无遗憾算法定制到伪去中心化设置中——使用共享随机字符串——以实现~O(T^{1/2})的速率，该速率在log因子意义下最优。

## Abstract
This paper studies an online variant of the assistance games framework, where an informed agent and an uninformed agent repeatedly interact over $T$ timesteps to optimize a common reward function. While the informed agent (the human) observes a latent state of the world, the uninformed agent (the assistant) observes only the human's actions. We provide the first provably efficient learning algorithms for repeated assistance games. We introduce the notion of assistance regret: the gap between the cumulative utility of interactions and that of the optimal joint policies in hindsight, which map latent states to action pairs. We present decentralized algorithms for both the human and the assistant that achieve a $(1-1/e)$-approximate assistance regret rate of $\widetilde{O}(T^{3/4})$, with runtime polynomial in the size of the action and state spaces. These algorithms are general; in particular, they accommodate any no-regret algorithm for the assistant. We prove that achieving a regret approximation factor better than $(1-1/e)$ is computationally intractable. Furthermore, we demonstrate how these generic no-regret algorithms can be tailored to a pseudo-decentralized setting -- using a shared random string -- to achieve a rate of $\widetilde{O}(T^{1/2})$, optimal up to logarithmic factors.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

#### 1. 核心问题与整体含义
- **研究动机**：在人类-智能系统辅助交互中，人类拥有私人偏好（潜在状态），助手只能观察到人类的动作，双方共享共同奖励函数。这种信息不对称导致动作同时承担“获取奖励”和“传递状态”的双重角色，产生协调困难。现有工作缺乏在线场景下的高效学习算法。
- **整体含义**：本文首次为重复辅助博弈（Assistance Games）提供可证明高效的学习算法，定义“辅助遗憾”指标，证明存在分散式算法达到次线性遗憾，并揭示了近似最优性与计算复杂性的基本界限（(1-1/e) 近似是计算可行的最优）。

#### 2. 方法论
- **核心思想**：
  - 将联合策略优化问题转化为在线子模最大化问题：通过“辅助拟阵”表示策略空间，利用“价值-辅助函数”的权重阈值势结构，借助拟阵约束下的子模最大化工具。
  - 提出“稳定-自适应分解引理”（Lemma 4.4）：将辅助遗憾分解为集中式算法的外部遗憾与协调成本（助理的跟踪遗憾 × 算法策略切换次数）。
- **关键技术细节**：
  - **集中式算法（Alg_C）**：基于RAOCO（Randomness-Augmented Online Convex Optimization），将子模函数通过1-Lipschitz的凹松弛转化为在线凸优化，再通过耦合随机性的取整（CR-RAOCO）实现稳定策略切换，保证外部遗憾 O(M_A M_H √T) 且切换次数 O(√(M_A M_H T log T))。
  - **分布式人类算法（Alg_H）**：直接将集中式算法的输出作为人类策略，不额外修改。
  - **分布式助手算法（Alg_A）**：对每个人类动作维护一个独立的跟踪遗憾最小化器（如固定份额算法），实现跟踪遗憾 O(M_H √(M_A p log(M_A T)))，其中 p 为切换分段数。
  - **伪去中心化优化**：利用预先共享的随机串 σ 作为信令，使人类在策略切换时通过一组特殊动作编码新策略，避免助手重新探索，将跟踪遗憾消去，得到 O(M_A M_H √T) 的遗憾界。
- **公式/算法流程**（文字说明）：
  - 见 Algorithm 1：集中式/分布式算法框架；Algorithm 3：CR-RAOCO 流程；Algorithm 4：POMER 稳定 OCO 算法。

#### 3. 实验设计
- **数据集 / 场景**：论文为纯理论工作，未进行实验验证。理论分析基于一般博弈模型，假设偏好空间 Θ、动作空间 A_H、A_A 有限，奖励函数有界于 [0,1]。
- **基准方法**：无直接对比的现有方法，因为本文是首个针对该问题的可证明高效算法。
- **对比方法**：未涉及。

#### 4. 资源与算力
- 文中未提及任何 GPU 型号、数量、训练时长等计算资源信息。所有算法复杂度分析基于理论运行时间（poly(N,M_H,M_A,T)），未进行实际实现或实验。

#### 5. 实验数量与充分性
- 不适用（无实验）。但理论分析充分：
  - 提供了完整的 regret 上界证明（定理4.1、4.2）。
  - 提供了下界证明（定理4.3，基于 NP-hardness）。
  - 提供了 min-max 下界（附录 E.4），说明 √T 依赖最优。
  - 严格分析了计算复杂度多项式性。

#### 6. 主要结论与发现
- **主要结论**：
  - 存在分散式算法实现 (1-1/e) 近似辅助遗憾率 Õ(T^{3/4})，且算法通用，可适配任意无遗憾助手。
  - 若允许初始共享编码（伪去中心化），可达到最优 Õ(√T) 速率。
  - 优于 (1-1/e) 的近似因子在计算上不可行（除非 RP=NP）。
- **发现**：辅助博弈的优化问题可归结为在线子模最大化问题；稳定策略切换与低跟踪遗憾是实现低辅助遗憾的关键解耦性质。

#### 7. 优点
- **理论深度**：首次将辅助博弈与在线子模优化、拟阵理论、稳定 OCO 技术有机结合，给出严格 regret 界。
- **通用性**：人类算法只需封装集中式算法，助手可使用任意跟踪遗憾算法，便于后续扩展。
- **最优性证明**：不仅给出上界，还证明下界（(1-1/e) 逼近和 √T 遗憾率），完整刻画了问题可解边界。
- **技术贡献**：提出“稳定-自适应分解引理”，为后续类似协调问题提供了通用分析框架。

#### 8. 不足与局限
- **实验验证缺失**：纯理论，未在真实或合成环境中验证算法实际表现，应用可行性未知。
- **遗憾率对动作空间大小依赖**：上界中包含 M_A^{3/4}、M_H^{5/4} 等因子，可能不是最优，作者也承认该问题开放。
- **伪去中心化依赖预共享编码**：需要初始同步，限制了完全去中心化场景。
- **仅考虑 oblivious 对手**：若对手可适应性选择偏好，证明线性 regret 不可避免（附录 B），限制了鲁棒性。
- **实际部署偏差**：假设奖励函数已知、有限空间、多项式级状态/动作，现实应用可能面临连续空间或非平稳偏好分布。

（完）
