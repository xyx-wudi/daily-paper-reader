---
title: Cooperative MARL-Based Energy-Efficient Power Control for Two-Hop Relaying Networks
title_zh: 基于合作多智能体强化学习的双跳中继网络能效功率控制
authors: "Xinyu Qu, Yuanzhe Geng, Erwu Liu, Rui Wang"
date: 2026-07-18
pdf: "https://arxiv.org/pdf/2607.16703v1"
tags: ["query:marl"]
score: 10.0
evidence: 协作多智能体强化学习用于中继网络的能效功率控制
tldr: "针对两跳中继网络中难以获取瞬时信道状态信息的问题，提出了一种基于合作多智能体深度Q网络的能效优化方法。每个中继独立决策并最大化共同能效目标，利用部分可观测的过时信道状态信息。理论证明了基于瞬时信道状态信息的博弈方法可为其提供上界。仿真表明该方法性能接近最优解，仅有约5.2%的差距。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16703v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 834, \"height\": 611, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16703v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 808, \"height\": 612, \"label\": \"Figure\"}]"
motivation: 两跳中继网络中瞬时信道状态信息难以获取，需在部分可观测条件下实现分布式能效优化。
method: 提出基于延迟奖励的状态-动作值函数的多智能体深度Q网络框架，各中继独立决策并协同优化能效。
result: "所提方法性能优于其他方案，与最优解的差距仅为5.2%。"
conclusion: 合作多智能体强化学习有效解决了非理想信道信息下的能效优化问题，具有实用价值。
---

## 摘要
本文研究了合作通信网络中的合作博弈问题，其中每个中继自主决策，并旨在实现最大化能效的相同优化目标。我们考虑了非理想情况，即瞬时信道状态信息难以获取，仅有部分可观测的过时CSI可用。为解决该博弈问题，我们定义了一种基于延迟奖励的状态-动作价值函数，并提出了一种多智能体深度Q网络学习框架。随后，我们分析证明了采用瞬时CSI的博弈方法所获得的效用作为所提方法的上界。仿真结果表明，我们的方法显著优于潜在的替代方案，且仅与最优解相差约5.2%。

## Abstract
In this paper, we study a cooperative game in the cooperative communication network, where each relay makes decisions autonomously and aims to achieve the same optimization objective of maximizing energy efficiency. We consider the non-ideal situation where instantaneous channel state information (CSI) is difficult to obtain and only partially observable outdated CSI is available. To solve this game problem, we define a delayed reward-based state-action value function and propose a multi-agent deep Q network learning framework. Then, we prove analytically that utilities obtained by game-theoretic approaches with the instantaneous CSI serve as upper bounds for those of the proposed method. Simulation results reveal that our approach considerably outperforms its potential alternatives and is only about 5.2% away from the optimal solution.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：在双跳中继协作通信网络中，系统能效（EE）最大化是优化功率分配的核心目标。传统方法通常假设存在一个集中控制器，并能获取全局瞬时信道状态信息（CSI），但这在分布式系统中难以实现。实际场景中，信道快速变化导致瞬时CSI获取困难，仅有部分可观测的过时CSI可用。
- **核心问题**：如何在仅有过时CSI的非理想环境下，通过分布式方式（各中继自主决策）最大化系统的整体能效。
- **整体含义**：本文试图将合作多智能体强化学习（MARL）引入该问题，解决由于信息不完全导致的传统博弈方法失效的情况，实现接近最优的分布式功率控制。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：将每个中继视为一个独立智能体，它们协作最大化共同能效目标。由于仅有部分过时CSI可用，提出一种基于延迟奖励的状态-动作值函数（Delayed Reward-based Q-function），并基于多智能体深度Q网络（DQN）框架进行分布式学习。
- **关键技术细节**：
  - **系统模型**：采用两跳放大转发（AF）中继网络，源与目的节点间无直接链路。信道服从高斯马尔可夫块衰落自回归模型（式(2)），用相关系数ρ表征时域相关性。
  - **状态设计**：每个智能体k的观测状态为$S_t^k = [h_{sk}(t-1), h_{kd}(t-1), P_s]$，即过时的源-中继信道、中继-目的信道以及源发射功率。
  - **动作设计**：离散功率等级集合$A_t^k = \{0,1,...,L\}$，对应功率值$P_k = \frac{a_k}{L} P_{k,\max}$。
  - **延迟奖励**：$r_t^k = EE(a_t; s_{t+1}, \cdot)$，即基于下一时隙的全局状态（全局瞬时CSI）和当前联合动作计算的能效，因此称为“延迟奖励”。
  - **Q函数**：定义$\tilde{Q}(s_t, a_t; \theta) = \mathbb{E}_{s_{t+1}}[r_t(s_{t+1}, a_t)] + \gamma \mathbb{E}_{s_{t+1}}[\max_{a_{t+1}}\tilde{Q}(s_{t+1}, a_{t+1}; \theta)]$（见式(12)）。
  - **算法流程**（Algorithm 1）：
    1. 初始化每个智能体的经验回放池$B_k$、评估网络$\theta_k$和目标网络$\bar{\theta}_k = \theta_k$。
    2. 循环训练回合（episode）：
        - 初始化环境。
        - 每个时隙t：
          - 每个智能体观测部分CSI $s_t^k$，使用ε-贪婪选择动作并执行。
          - 观测下一部分状态$s_{t+1}^k$。
          - 基于全局状态和联合动作，环境返回奖励$r_t^k$。
          - 存储经验元组$(s_t^k, a_t^k, r_t^k, s_{t+1}^k)$到$B_k$。
          - 采样小批量，最小化TD误差损失（式(15)），用RMSProp更新$\theta_k$。
          - 每C步重置目标网络。
- **理论分析**：证明当采用瞬时CSI时，博弈论方法（Optimal）获得的效用构成所提MARL方法（基于过时CSI）的上界（式(18)）。

### 3. 实验设计

- **仿真场景**：基于随机生成的遵循式(2)的信道，参数$\rho=0.1$；源功率1.0W；中继最大功率1.0W；5个中继；总功率等级$L+1$可取4（训练实验）和2~8（对比实验）。
- **Benchmark方法**：
  - **Optimal（上界）**：使用全局瞬时CSI的博弈论最优解。
  - **SADQN（单智能体DQN）**：集中式控制器基于全局过时CSI学习联合策略，动作空间为$(L+1)^K$。
  - **GOIBS**：基于全局过时CSI的博弈论求解方法。
  - **POIBS**：每个中继仅基于部分过时CSI最大化其单条链路（$s-k-d$）的能效。
- **超参数**：学习率0.001，经验回放池大小10000，mini-batch 128，训练间隔1个时隙，训练回合100，每回合最大时隙1000，训练时ε=0.9，测试时ε=1.0。

### 4. 资源与算力

- 论文**未明确说明**所使用的GPU型号、数量、训练时长等计算资源信息。

### 5. 实验数量与充分性

- **实验组数**：主要两组实验。
  1. **训练性能对比**（Fig. 1）：展示所提MADQN和SADQN的收敛曲线（5次独立运行的中位数和范围）。结果显示MADQN收敛更快（约40个回合 vs. 60个回合），波动更小。
  2. **不同功率等级下的性能对比**（Fig. 2）：从2到8个功率等级，对比所有方法。结果稳定，趋势合理。
- **充分性**：实验覆盖了关键参数（功率等级数量），并对比了4种基准方法（包括理想上界、非学习方法、集中式RL），验证了所提方法在非理想环境下的有效性。但缺少对更多参数（如信道相关系数ρ、中继数量K、噪声方差等）的敏感性分析，也未进行消融实验（如去除延迟奖励机制的影响）。实验整体设计客观、公平，但可进一步丰富。

### 6. 论文的主要结论与发现

- 所提基于延迟奖励的多智能体DQN方法在仅有过时部分CSI的场景下，能有效学习接近最优的分布式功率控制策略，性能显著优于SADQN、GOIBS和POIBS。
- 与理想上界（Optimal）相比，差距仅约5.2%（在5个功率等级时，0.781 vs. 0.823 bit/J/Hz）。
- 当功率等级增加时，SADQN因动作空间急剧膨胀（$(L+1)^K$）而性能下降甚至反直觉降低，而所提方法保持稳定提升，避免了维数灾难。
- 通过理论分析证明了博弈论方法的效用构成所提方法的上界，为分布式RL方法的性能提供了参照基准。

### 7. 优点

- **方法论创新**：提出了适用于非理想CSI场景的“延迟奖励”Q函数，将奖励与下一时隙的全局状态（即真实的瞬时CSI）关联，从而间接利用不可直接观测的瞬时信息。
- **分布式架构**：每个智能体仅需处理其原始离散动作空间（大小$L+1$），避免了集中式方法的维数爆炸问题，使学习更高效。
- **理论支撑**：从理论上证明了所提方法与理想博弈上界之间的关系，增强了结果的可信度。
- **性能优越**：在多种对比方法中表现最优，且接近理想上界，验证了方法的实用价值。

### 8. 不足与局限

- **实验覆盖不足**：仅在一个固定信道相关性（ρ=0.1）下测试，未分析不同延迟程度（不同ρ）对性能的影响；中继数量固定为5，未进行扩展性验证；未考虑不同源功率、噪声方差等参数变化。
- **缺少消融研究**：未单独分析“延迟奖励”设计或状态设计（如移除源功率信息）的影响，无法确认哪些组件最关键。
- **算法对比有限**：仅对比了DQN变种，未比较其他MARL算法（如MADDPG、QMIX等）或更先进的深度强化学习算法。
- **应用假设较强**：假设源功率已知且固定，中继采用AF协议，且信道模型为高斯马尔可夫模型；在更复杂的实际信道（如多径衰落）或更高级的中继协议（如DF）下，方法可能需调整。
- **未讨论通信开销**：虽然智能体不交换信息，但奖励反馈依赖于全局状态（下一时隙的全局CSI），这在分布式系统中如何获取未明确说明（可能假设由融合中心反馈），实际部署时可能存在延迟或同步问题。

（完）
