Title: A Contextual-Bandit Oversight Game with Two-Sided Informational Asymmetry

URL Source: https://arxiv.org/pdf/2607.00155v1

Published Time: Thu, 02 Jul 2026 00:08:00 GMT

Number of Pages: 17

Markdown Content:
# A Contextual-Bandit Oversight Game with Two-Sided Informational Asymmetry 

Yunjin Tong 

Stanford Graduate School of Business 

Abstract 

We study runtime human oversight of an AI agent when private information runs in both directions: the human privately knows her reward function, while the AI privately knows the quality of the action it proposes. This is the kind of asymmetry that arises naturally when an autonomous robot or software agent has inspected a situation its human supervisor can-not directly assess. Building on Cooperative Inverse Reinforcement Learning (CIRL) and the Oversight Game, we introduce a contextual-bandit team game with two-sided asymmetric in-formation and a play/ask/trust/oversee interface. The bandit structure removes physical state transitions and thereby yields exact one-shot characterizations that would remain conjectural in the full POMDP setting, though the common belief remains a dynamically controlled state across rounds. We give two one-shot characterizations, a team optimum and a behaviorally natural myopic rule, whose gap is a “slab” of avoidable harm: a region in which the AI privately knows the proposed action is harmful and shutdown would help, yet a myopic human, trusting her prior, declines to oversee. We show this gap is the price of non-credible oversight communi-cation, and give a partial analysis of how it resolves dynamically over repeated rounds through passive learning and active signaling with a one-period-lagged oversight response. 

# 1 Introduction 

A central problem in deploying autonomous agents, robotic or software, is calibrating when a human supervisor should intervene. As such agents take on consequential tasks, from a warehouse robot grasping a loaded shelf to a coding agent refactoring production software, the question of when a human should step in and override becomes a design problem in its own right: intervene too rarely and harmful actions slip through, intervene too often and the agent’s autonomy is wasted on costly and unnecessary oversight. Two lines of prior work frame the building blocks we combine. Cooperative Inverse Reinforce-ment Learning (CIRL) [1] casts human–AI interaction as a shared-reward game in which the AI is uncertain about the human’s preferences and must learn them through interaction. CIRL inte-grates preference learning with action selection and can generate active learning, active teaching, and communicative behavior; the human’s private reward parameter is the hidden information, and a common posterior over that parameter is the sufficient statistic for optimal play. What CIRL does not explicitly model is the runtime play/ask/trust/oversee interface studied here, nor an AI-private proposal-quality parameter that the human cannot observe. Its uncertainty is one-sided: it models “what does the human want?” but never “what does the AI know about the world that the human does not?” The Off-Switch Game [2] introduces runtime deferral as an explicit object of study, but only in a single-shot setting. The Oversight Game [3] supplies a runtime interface of the kind we use, in which an AI proposes an action, a human may override, and interaction costs make the 1

> arXiv:2607.00155v1 [cs.AI] 30 Jun 2026

decision nontrivial, but it is a Markov game under full information, with neither preference nor model uncertainty. This paper develops a model in which private information runs in both directions, and in which deferral is a runtime decision. The motivating observation is that an embodied or autonomous agent routinely knows things about the consequences of its own proposed actions that its supervisor cannot directly observe: a robot that has physically inspected its workspace, or a software agent that has read a codebase, has private knowledge of failure modes the human cannot see. This asymmetry runs opposite to CIRL’s. We therefore study a setting with two-sided private information, in which the human privately knows her reward type θ and the AI privately knows an observation-model type ω governing the quality of its proposal, mediated by a play/ask/trust/oversee interface in which the AI chooses whether to defer and the human chooses whether to override. Our model thus adds an opposite-direction informational asymmetry to the usual CIRL preference uncertainty, and this two-sided structure produces a bilinear payoff f (θ, ω ) = ⟨Oω, R θ⟩ that is the algebraic key to our results. A fully general treatment of this setting, with persistent state and Markov dynamics, runs into a known difficulty: the optimal value function resists closed-form characterization, because the asking decision couples future state dynamics, future belief evolution, and future correction opportunities in an intertwined Bellman recursion. We therefore adopt a contextual-bandit model, which removes physical state transitions and thereby simplifies the immediate correction value relative to the belief-information value. This simplification is what buys us exact one-shot characterizations of the team-optimal deferral policy that would remain conjectural in the full POMDP setting; the cost is the absence of persistent state effects, and we flag the POMDP extension as the primary open problem (Section 4.4). We emphasize that the bandit structure removes only the physical dynamics: as we show, the common belief remains a dynamically controlled state across rounds. 

Contributions. 

1. A formal contextual-bandit team game (Definition 1) with two-sided asymmetric information that recovers a stateless shared-reward specialization of the Oversight Game interface and a restricted contextual-bandit assistance-game analogue of CIRL as limit cases. 2. Two one-shot characterizations: the genuine team optimum, an exact finite combinatorial max B,C whose binary off-switch threshold is independent of the human’s prior q (Proposi-tion 1 and corollary 1), and a myopic non-signaling rule whose ask region is the rectangle (b∗, 1) × (q∗, 1) (Proposition 2). The gap between them is the cost of non-credible oversight communication (Remark 3). 3. A partial multi-round analysis showing two mechanisms by which the myopic failure resolves dynamically, passive learning (Proposition 3) and active credible signaling with a one-period-lagged oversight response (Proposition 4), each driving the human’s belief toward the regime where her rule matches the team optimum. 

# 2 A Motivating Example 

We ground the abstract model in a concrete scenario used throughout to build intuition. 

Example 1 (Non-technical operator and an autonomous mobile manipulator) . A warehouse is supervised by a floor operator (human, H) who manages throughput and safety but has no robotics background. An autonomous mobile manipulator ( R), a wheeled robot arm that picks items from 2shelves and places them onto conveyors, is integrated into the fulfillment line. The context is s =“the current state of a loaded storage rack,” and the robot proposes aσ = “execute a high-speed retrieval of the top-shelf item using the long-reach grasp.” 

The robot’s private type ω. Using its onboard depth cameras and force sensors, something the operator cannot do from the floor, the robot detects that the target item sits on a partially collapsed shelf bracket, and that the high-speed long-reach grasp will shift the load, toppling the stack onto the aisle. The binary type space Ω = {ωL, ω H } captures the robot’s private quality assessment of this proposal: ωL (clean, the grasp is safe and the rack is sound) or ωH (hazardous, the bracket will give way). The operator cannot read the sensor stream and so cannot observe 

ω. In this round the robot observes ω = ωH . Equivalently, ω indexes how much trust the human should place in the robot’s implicit assessment of the proposal: ωL means the proposal is as safe as it appears, while ωH means the robot privately knows it is harmful in a way the human cannot detect. 

The observation model Oω(· | s, a σ). This is the distribution over operational outcomes o ∈ O 

that the operator actually observes after execution (dropped-load alarms, aisle obstructions, cycle-time logs), as a function of the robot’s private quality type: Observed outcome o OωH (o) OωL (o)Stack topples, aisle blocked 0.70 0.00 Item dropped, minor spill 0.20 0.00 Normal pick; instability later 0.10 0.05 Pick 30% faster, no incident 0.00 0.80 Faster with minor misalignment 0.00 0.15 Crucially, Oω is not “the laws of physics”, it is the distribution over outcomes visible to a non-technical observer. The operator need not know what a shelf bracket is, she observes the operational metric and updates her belief about ω accordingly. 

The human’s private type θ. The operator knows her reward function Rθ : O → R, encoding how much she values each observable outcome: 

Rθ(stack topples) = −1000 , Rθ(item dropped) = −700 , Rθ(30% faster) = +500 ,Rθ(normal pick) = 0 , Rθ(faster, misaligned) = +200 .

The robot does not know θ, since it does not know whether the operator weights safety over throughput, or how risk-averse she is. 

The bilinear payoff. The expected team payoff from executing aσ is the inner product f (θ, ω ) = 

⟨Oω, R θ⟩:

f (θ, ω H ) = 0 .70( −1000) + 0 .20( −700) + 0 .10 · 0 = −840 ,f (θ, ω L) = 0 .80(500) + 0 .15(200) + 0 .05 · 0 = +430 .

The bilinear structure separates the two sides of the asymmetry. Oω encodes what outcomes the proposal generates (the robot’s side). Rθ encodes how valuable each outcome is (the human’s side). Neither player holds both pieces. 

The failure mode. The common prior over the robot’s type is q := P (ω = ωH ) = 0 .30. The com-mon prior, equivalently the robot’s prior before observing any human behavior, assigns probability 3b := P (θ = θ1) = 0 .5 to the human being type θ1; the operator herself knows that her realized type is θ1. The interaction costs are cask = 100 and cov = 0. The operator’s expected payoff under the proposal is ¯fH (θ1) = (1 − q)f (θ1, ω L) + q f (θ1, ω H ) = 0 .70(430) + 0 .30( −840) = 301 − 252 = +49 > 0.

The operator believes the retrieval is on balance positive, so under the myopic non-signaling rule she would trust rather than oversee, even if asked (here cov = 0, so q∗ = f1L/(f1L − f1H ) = 430 /1270 ≈

0.34, and indeed q = 0 .30 < q ∗). Anticipating that an ask would not trigger a correction, the robot does not ask, and the hazardous grasp is executed. The team optimum, by contrast, finds asking worthwhile: its threshold is b∗ = cask /(|f1H | − cov ) = 100 /840 ≈ 0.12, and since b = 0 .5 > b ∗ the team-optimal gain over always playing is q [ b |f1H | − cask ] = 0 .30(420 − 100) = 96 > 0. So this is a genuine failure under myopic oversight but a strict improvement under a credibly coordinated team. If the ask is understood as a credible signal that ω = ωH , the operator oversees and halts the proposal, and the failure disappears. That contrast is the paper’s main point. 

Remark 1 (The same structure arises for software agents) . The phenomenon is not specific to robots. Consider a non-technical CEO ( H) supervising an AI coding agent ( R) integrated into an engineering pipeline, with proposal aσ = “refactor the authentication module to use async /await .” After inspecting the codebase, something the CEO cannot do, the agent discovers a latent race condition in the session-management code that the refactor will expose, causing intermittent login failures under high concurrency. Here ωH is “buggy” and the operational outcomes the CEO observes are login-failure rates and user complaints rather than toppled stacks; the reward type θ

encodes whether she weights reliability over performance. The bilinear payoff, the +49 prior, and the failure mode are identical to Example 1. Whenever an autonomous agent inspects a situation its supervisor cannot assess and proposes an action whose consequences only the agent foresees, the same two-sided asymmetry and the same slab of avoidable harm appear. 

# 3 The CB-Oversight-CIRL Game 

Definition 1 (CB-Oversight-CIRL game) . A contextual-bandit oversight game with two-sided private information is a tuple 

B = S, A, O, {Ω, O (·; ·)}, {Θ, R (·; ·)}, σ, Over , c ask , c ov , ρ, P 0, T, γ ,

with the following components. 

• S, A, O, Ω, Θ are finite. 

• Observation model. O : S × A × Ω → ∆( O), written Oω(· | s, a ). The observation type 

ω ∈ Ω is AI-private, observed by the AI at t = 0, persistent, and unobserved by the human. 

• Reward model. R : O × Θ → R, written Rθ(o), bounded. The reward type θ ∈ Θ is human-private, observed by the human at t = 0, persistent, and unobserved by the AI. 

• Base policy. σ : S → ∆( A), an immutable pretrained policy mapping contexts to proposed actions; it does not depend on either private parameter. 

• Oversight operator. Over : S × A × Θ × ∆(Ω) → ∆( A ∪ { off }) specifies the correction the human applies when she oversees, as a function of whatever belief β ∈ ∆(Ω) she holds at the moment of correction. Its support lies in the optimal-correction set, supp  Over( s, a σ, θ, β ) ⊆ arg max  

> e∈A∪{ off }

Eβ [fe(θ, ω )1e∈A],

4i.e. it places mass only on maximizers (allowing arbitrary randomized tie-breaking), with the off-switch special case restricting the arg max to {aσ, off }. The relevant β depends on the protocol, which will be specified later. (An exogenous Over is also admissible; all results below use this optimal-correction form.) 

• Context law. ρ ∈ ∆( S), an i.i.d. context distribution: each round draws st i.i.d. 

∼ ρ, indepen-dently of ( θ, ω ) and of the public history. (A fixed publicly known context sequence, or an exogenous ρt(· | hpub  

> t

) with contexts independent of ( θ, ω ) given the public history, are equally admissible; the i.i.d. case is assumed for concreteness and is all that the results below use.) 

• cask , c ov ≥ 0 are interaction costs; T ≥ 1 is the horizon and γ ∈ (0 , 1] the discount factor, with γ = 1 permitted when T < ∞ and γ < 1 required when T = ∞; P0 ∈ ∆(Θ × Ω) is the joint prior over types. 

Information structure. At round 0, ( θ, ω ) ∼ P0 is drawn once and persistent. The human observes θ; the AI observes ω; neither directly observes the other’s type. Both players observe the context st, the proposal aσ,t , the AI meta-action aAI t , the human meta-action aHt on the ask branch (it is unobserved when the AI plays), the executed action aexec  

> t

, and the realized observation ot

at every round. The common belief μt ∈ ∆(Θ × Ω) is computable from the public history and is common knowledge. The private posteriors are the conditionals 

bAI t (θ′) = μt(θ′ | ω), bHt (ω′) = μt(ω′ | θ).

Stage interaction (round t). 

1. Both observe ( st, μ t); the AI additionally observes ω; the human additionally observes θ.2. The proposed action aσ,t ∼ σ(st) is drawn and publicly observed. 3. The interface meta-actions aAI t ∈ { play , ask } and aHt ∈ { trust , oversee } are selected simulta-neously, conditioning on ( st, μ t, a σ,t ) and each player’s private parameter. The AI meta-action 

aAI t is then publicly revealed. The human’s contingent meta-action aHt is public only on the branch where the AI asks; if the AI plays, the human’s trust/oversee choice is neither observed by the AI nor payoff-relevant (she cannot override a play), so it conveys no information about 

θ. The human does not observe aAI t before selecting whether she is willing to oversee. If the realized branch is ask-oversee, the correction action is selected after this revelation; under a credible protocol it is therefore evaluated using the belief that an ask induces (see step 4). The realized ask is public and enters μt+1 , the cross-round signaling channel exploited in Section 4.3. 4. The executed action is 

aexec  

> t

=



aσ,t if aAI t = play ,aσ,t if ( aAI t , a Ht ) = (ask , trust) ,e ∼ Over( st, a σ,t , θ, β t) if ( aAI t , a Ht ) = (ask , oversee) ,

with aexec  

> t

∈ A ∪ { off } and the human cannot override when the AI plays. The correction belief is 

βt =

(

bHt (·) = μt(· | θ), myopic non-signaling protocol ,bHt,B (· | θ), credible ask protocol (ask-set B),

5where bHt,B (ω | θ) := μt(ω | θ, ω ∈ B) = μt(θ, ω )1{ω ∈ B} P  

> ω′∈B

μt(θ, ω ′) is the post-ask posterior formed against the current common belief μt (the one-shot analogue bHB of (2) is its 

μt ≡ μ specialization). That is, the human corrects on her prior conditional when the ask is not a credible signal, and on the post-ask posterior when it is. 5. If aexec  

> t

∈ A, the observation ot ∼ Oω(· | st, a exec  

> t

) is drawn and publicly observed. If aexec  

> t

=off, no observation is drawn for this round only, not permanently. 6. Both players receive the shared stage reward, defined piecewise so that no outcome is refer-enced on the shutdown branch: 

rt := 

(Rθ(ot) − cask 1{aAI t = ask } − cov 1{aAI t = ask , a Ht = oversee }, aexec  

> t

∈ A, 

− cask 1{aAI t = ask } − cov 1{aAI t = ask , a Ht = oversee }, aexec  

> t

= off .

(Equivalently, adjoin a dummy outcome ⊥ with Rθ(⊥) = 0 drawn deterministically on the shutdown branch.) The oversight cost is charged only when oversight is actually invoked, i.e. when the AI asks and the human oversees; the human’s choice of oversee carries no cost when the AI plays (and indeed cannot bind, since the human cannot override a play). The shared reward rt is the team objective, not an additional public observation: the AI observes the operational outcome ot but not the numerical value Rθ(ot) or rt, so a play round does not let the AI read θ off the realized reward. 7. Both players update μt to μt+1 by Bayes’ rule against everything publicly revealed this round, whose joint likelihood under a candidate type pair ( θ′, ω ′) factors through the AI meta-action aAI t , the human meta-action aHt on the ask branch only (it is unobserved when the AI plays), the realized oversight correction (when ask-oversee), the executed action aexec  

> t

, and the operational observation Oω′ (ot | st, a exec  

> t

). The observation factor is the primary channel through which both learn about ω; the realized override, available only after an ask, is the primary channel through which the AI learns about θ.

Policies and value. In the finite-horizon model the meta-policies may depend on time, 

πAI t : S×∆(Θ ×Ω) ×Ω×A → ∆( {play , ask }), πHt : S×∆(Θ ×Ω) ×Θ×A → ∆( {trust , oversee }),

and reduce to stationary maps πAI , π H in the stationary infinite-horizon case. The conditional value of a type pair is V π(θ, ω ) = EπP Tt=1 γt−1rt | θ, ω , and the team objective is the single ex ante scalar 

V π(μ0) := Eπμ0

hPTt=1 γt−1rt

i

= P 

> θ,ω

μ0(θ, ω ) V π(θ, ω ),

which the coordinator maximizes; the one-shot benchmark of Proposition 1 is exactly this μ-weighted ex ante optimization. Because the interface meta-actions and the executed action both affect the public posterior, the multi-round problem does not separate across rounds. Removing Markov state transitions takes the physical state out of the endogenous state variable, but the com-mon belief remains dynamically coupled across time: the coordinator’s sufficient state is ( t, s t, μ t)in the finite-horizon model, or ( st, μ t) in the stationary infinite-horizon model. The one round in which separation does hold trivially is the one-shot game T = 1, which is the object of Section 4.2; the coordinator there chooses δAI : Ω → { play , ask } and δH : Θ → { trust , oversee } against the fixed common belief μ.6Relation to predecessor models. Setting |Ω| = 1, cask = cov = 0, and fixing both meta-policies to (ask , oversee) yields a restricted contextual-bandit assistance-game analogue of CIRL, with the bandit structure replacing the Markov state; it is not full CIRL, since the AI cannot freely choose ordinary environment actions and the human acts only through the oversight mechanism. Setting 

T = 1, |Θ| = 1, and the off-switch operator Over ∈ { aσ, off } recovers the one-shot Off-Switch Game [2]. Setting |Θ| = |Ω| = 1 yields a stateless shared-reward specialization of the Oversight Game interface [3], which is itself a Markov game and may carry distinct player rewards; our setting drops its dynamics and specializes to the shared-reward, two-private-type case. 

# 4 Results 

4.1 Setup: bilinear payoff and dominated actions 

Fix a round and drop time subscripts. Fix context s and proposal aσ. For any executed action 

a ∈ A, define the action payoff 

fa(θ, ω ) := Eo∼Oω (·| s,a )

Rθ(o) = Oω(· | s, a ), R θ(·) O, foff (θ, ω ) := 0 , (1) with the convention that shutdown yields 0. We write fσ(θ, ω ) := faσ (θ, ω ) for the proposal payoff; when no executed action is named, f means fσ. The inner-product factorization separates the AI’s private information ( Oω) from the human’s ( Rθ). In Example 1, fσ(θ, ω H ) = −840 and 

fσ(θ, ω L) = +430. Define the human-side estimate (her expected proposal payoff, averaged over her belief about 

ω): ¯fH (θ) := Eω′∼μ(·| θ)

fσ(θ, ω ′) = ¯ObH (θ), R θ , ¯ObH (θ) := P 

> ω′

μ(ω′ | θ) Oω′ (· | s, a σ).

For a fixed executed correction e, define the override gain relative to playing the proposal, 

De(θ, ω ) := fe(θ, ω ) 1e∈A − fσ(θ, ω ).

The correction the human actually applies depends on the belief she holds when she oversees, which in turn depends on what the ask reveals. For an AI ask-set ∅̸ = B ⊆ Ω, define the post-ask posterior (the belief that “the AI asked” induces, under a commonly understood protocol in which asks occur exactly on B): 

bHB (ω | θ) := μ(ω | θ, ω ∈ B) = μ(θ, ω ) 1{ω ∈ B}

P 

> ω′∈B

μ(θ, ω ′) , (2) with the convention that if the denominator is zero (such θ having zero probability conditional on an ask) then bHB (· | θ) is arbitrary; this does not affect ∆( B, C ). For B = ∅ no correction belief is needed. Let e∗ 

> B

(θ) ∈ arg max e EbHB (·| θ)[fe(θ, ω )1e∈A] be the human’s optimal correction at that posterior, with induced gain DB (θ, ω ) := De∗ 

> B(θ)

(θ, ω ). We use bHB in the team-optimal benchmark (where the ask is credible) and the prior conditional bH (θ) in the myopic rule (where it is not). Under the off-switch operator, e∗ 

> B

(θ) = off iff EbHB (·| θ)[fσ(θ, ω )] < 0, in which case 

DB (θ, ω ) = −fσ(θ, ω ) on the cells ω ∈ B; otherwise DB = 0 there. 

Remark 2 (Ask-trust dominance (one-shot)) . In a one-shot payoff comparison with no continuation value of information, ask-trust has no intrinsic benefit, since fσ(θ, ω ) − cask ≤ fσ(θ, ω ). It is never chosen for its own sake; it appears only as an unavoidable cost on cells ( θ, ω ) with ω ∈ B and 

θ / ∈ C, i.e. when asking at type ω is worthwhile for other human types but this particular θ does not oversee. (This −cask is exactly the term carried by ∆( B, C ) in (3).) In the multi-round setting this changes. Ask-trust can be valuable purely as a signal, paying cask now to move the human’s belief and enable future oversight (Proposition 4). 74.2 One-shot analysis: team-optimal benchmark vs. myopic oversight 

We give two one-shot characterizations. Proposition 1 is the genuine team optimum (the coordinator chooses both players’ rules jointly). 1 Proposition 2 is the weaker, behaviorally natural rule in which the human treats the AI’s ask as uninformative. The two differ in an instructive way (Remark 3). 

Team-optimal one-shot benchmark. The coordinator chooses both rules jointly; equivalently, the team commits to a commonly understood protocol, so when the AI asks, the human updates to the post-ask posterior bHB of (2) and applies e∗ 

> B

. In the gain (3) below, the ask cost is paid on every ω ∈ B (for all θ), while the oversight cost and override gain accrue only on the joint event 

{ω ∈ B, θ ∈ C}.Although the model permits randomized meta-policies, the one-shot team optimum is attained by a deterministic one. Let xω ∈ [0 , 1] be the probability that type ω asks, and write x = ( xω)ω.Optimizing the human’s trust/oversee choice and her correction action for each θ, the team’s relative gain over always-playing can be written 

G(x) = −cask 

X

> ω

μΩ(ω) xω + X

> θ

max 

n

0, max 

> e

X

> ω

μ(θ, ω ) xω

fe(θ, ω )1e∈A − fσ(θ, ω ) − cov 

o

.

The first term is linear in x and each summand of the second is a pointwise maximum of finitely many linear functions of x, hence convex; G is therefore convex on the hypercube [0 , 1] |Ω|. A convex function on a polytope attains its maximum at an extreme point, so there is an optimal x with every xω ∈ { 0, 1}. It is therefore without loss of optimality to restrict attention to deterministic type-contingent meta-policies, represented by subsets B ⊆ Ω (the AI types that ask) and C ⊆ Θ(the human types that oversee). 

Proposition 1 (Team-optimal one-shot benchmark under credible ask protocol) . In the one-shot game ( T = 1 ) under a credible ask protocol, a deterministic coordinator policy is a pair (B, C ),where B ⊆ Ω is the set of AI types that ask and C ⊆ Θ is the set of human types that oversee. Relative to always playing, its gain is 

∆( B, C ) = −cask 

X

> ω∈B

μΩ(ω) + X

> ω∈B

X

> θ∈C

μ(θ, ω )DB (θ, ω ) − cov 

, (3) 

where μΩ(ω) = P 

> θ

μ(θ, ω ) and DB is evaluated at the post-ask posterior (2) . A team-optimal one-shot policy is any (B∗, C ∗) ∈ arg max B⊆Ω, C ⊆Θ ∆( B, C ), with value V TO = Eμ[fσ(θ, ω )]+∆( B∗, C ∗).Proof. See Section A.1. The optimizer is generally not the separable myopic rule: the coupling runs through bHB , since the human’s correction depends on which ω trigger an ask. We specialize to the binary off-switch case (the accurate model of a non-technical overseer, cf. Example 1: accept aσ or reject to off , with no technical correction; off is per-round, not decommissioning). 

Assumption 1 (Binary sign pattern) . |Θ| = |Ω| = 2 , Θ = {θ0, θ 1}, Ω = {ωL, ω H }. Write 

fij := fσ(θi, ω j ). Assume f1L > 0 > f 1H , f0j ≥ 0 for j ∈ { L, H }, and 0 < c ask < −f1H − cov . The common belief is a product measure with marginals b := μ(θ1) ∈ (0 , 1) and q := μ(ωH ) ∈ (0 , 1) .    

> 1We adopt a single global tie-breaking rule throughout: at indifference, the human chooses trust and the AI chooses play . This resolves the boundary cases of Propositions 1 and 2 and fixes the strict crossing in Proposition 3.

8Assumption 1 restricts attention to the minimal nontrivial case. The type spaces are binary. 

θ1 is the human type with skin in the game ( f1L > 0 > f 1H : the proposal is beneficial under ωL

and harmful under ωH ), while θ0 is a type that values the proposal nonnegatively under both ωL

and ωH (f0j ≥ 0), so that shutdown is never strictly preferred to the proposal for θ0. The single cost condition 0 < c ask < −f1H − cov (which already forces −f1H > c ov , since cask > 0) ensures that avoided harm net of oversight cost exceeds the ask cost, and by itself guarantees b∗ ∈ (0 , 1); the multi-round threshold b∗∗ = b∗/γ is guaranteed to lie in (0 , 1) only together with Assumption 3 below, which is exactly the condition b∗∗ < 1. The product-measure belief parameterizes the 

common prior (equivalently the robot’s prior before observing any human behavior) via two scalars: 

b = μ(θ1), the prior probability the human is type θ1, and q = μ(ωH ), the prior probability the proposal is harmful. The human herself knows her realized θ; b is the robot’s uncertainty about it, not the human’s. In Example 1, f1H = −840, f1L = +430, q = 0 .30. The one-shot prior is taken in the interior q ∈ (0 , 1); the calculation in Corollary 1 extends directly to the boundary case q = 1, which is the post-ask belief invoked in the multi-round analysis of Section 4.3. 

Corollary 1 (Binary off-switch threshold) . Under Assumption 1 and the off-switch operator, define 

b∗ := cask 

−f1H − cov 

. A canonical team-optimal policy is 

B∗ = {ωH }, C∗ = {θ1} iff b > b ∗,

and always playing is optimal iff b < b ∗; at b = b∗ both are optimal. The team-optimal value is 

V TO = Eμ[fσ(θ, ω )] + q  b(−f1H − cov ) − cask 



> +

.

Thus the team-optimal ask threshold depends on b but not on q.Proof. See Section A.1. The non-uniqueness of C∗ is payoff-irrelevant when B∗ = ∅, and when cov = 0 adding types whose correction keeps aσ does not change payoffs. The threshold is q-free because asking on B∗ = {ωH }

reveals ωH , so θ1 shuts down on the post-ask posterior regardless of the prior q; q only scales the value (rarer harm, less total benefit). 

Myopic (non-signaling) one-shot policy. Now suppose the human does not treat the AI’s ask as evidence about ω, because the protocol is not commonly known, the ask is not a credible signal, or the interface does not surface it as one. She then evaluates oversight against her prior conditional bH (θ) = μ(· | θ), overseeing iff ¯fH (θ) + cov < max e EbH [fe1e∈A]; under off-switch this is 

θ ∈ Θ− := {θ : ¯fH (θ) < −cov }, and for such θ her chosen correction is shutdown, with per-cell gain 

−fσ(θ, ω ) over playing. Taking this human rule as fixed, the AI asks iff doing so raises the team payoff, i.e. iff Ψ( ω) := X

> θ∈Θ−

μ(θ | ω)−fσ(θ, ω ) − cask − cov μ−(ω) > 0, μ−(ω) = X

> θ∈Θ−

μ(θ | ω).

The contrast with the team optimum is sharp: here the human shuts down on her prior belief (a fixed set Θ −), whereas in Corollary 1 she shuts down on the post-ask posterior. 

Proposition 2 (Myopic one-shot characterization) . Under the myopic human rule above, the policy is δH (θ) = oversee iff θ ∈ Θ− and δAI (ω) = ask iff Ψ( ω) > 0. Under Assumption 1 and off-switch, with q∗ := f1L + cov 

f1L − f1H

∈ (0 , 1) and b∗ as above, 

9b = μ(θ1)

q = μ(ωH )

0 b∗

q∗

MYOPIC 

> asks here

THE SLAB 

> avoidable harm

Figure 1: The team optimum asks on the half-strip {b > b ∗}; the myopic rule asks only on the rectangle. The gap is the slab {b > b ∗, q ≤ q∗}: the AI knows the action is harmful and shutdown would help, yet the myopic human trusts her prior and the harm is realized. Exactly the operator case q = 0 .30 < q ∗ ≈ 0.34 (with cov = 0) of Example 1, where she sits just inside the slab. (i) θ1 ∈ Θ− iff q > q ∗; θ0 /∈ Θ− always; 

(ii) if q > q ∗, then Ψ( ωH ) > 0 iff b > b ∗ and Ψ( ωL) < 0; if q ≤ q∗, then Θ− = ∅ and Ψ( ωH ) = Ψ( ωL) = −cask < 0 for every b;

(iii) the ask region is the rectangle (b∗, 1) × (q∗, 1) .Proof. See Section A.2. 

Remark 3 (Main insight: the cost of non-credible oversight) . Compare the two ask regions in ( b, q )space (Figure 1). The team optimum (Corollary 1) asks on the half-strip {b > b ∗}; the myopic rule (Proposition 2) asks only on the rectangle {b > b ∗} ∩ { q > q ∗}. The difference is exactly the slab 

{ b > b ∗, q ≤ q∗ },

in which the AI privately knows the action is harmful ( ωH , f1H < 0) and shutdown is team-improving, yet the myopic human, trusting her prior, declines to oversee, so the AI does not ask and the harm is realized. This is precisely the failure mode of Example 1. With q = 0 .30 < q ∗ ≈ 0.34, the operator’s prior expected payoff is ¯fH (θ1) = 0 .70(430) + 0 .30( −840) = +49 > 0 > −cov , so 

θ1 /∈ Θ− and the human trusts. Anticipating no correction, the AI does not ask, and the hazardous grasp ( ω = ωH , f1H = −840) is executed. Under the team-optimal protocol, by contrast, an ask from the AI would shift the operator’s belief to the point mass at ωH , giving EbHB [fσ(θ1, ω )] = 

f1H = −840 < 0, and she would shut down immediately. The economic content is that q∗ is not a constraint of the decision problem; it is the price of non-credible communication. If the ask is a credible, commonly understood signal, the team-optimal protocol “the AI asks iff ωH , so oversee θ1 on an ask”, the human’s oversight choice binds exactly when her action matters (the world is ωH ), and the prior q drops out of the threshold. Private AI 10 knowledge of danger is then sufficient. The qualification “necessary but not sufficient” applies only when the channel that would make the ask credible is absent. Designing the interface so that asks are credible (or so that asks are scarce and therefore informative) collapses the myopic rectangle onto the team-optimal half-strip and eliminates the slab of avoidable harm. 

4.3 Multi-round: how the myopic failure resolves 

The slab {b > b ∗, q ≤ q∗} of Remark 3 is a one-shot phenomenon. It presumes the human’s belief 

q is fixed. Over repeated rounds, q is not fixed. We show two mechanisms by which the myopic human’s belief is driven into the region q > q ∗ where her rule coincides with the team optimum, one passive, one an explicit credible-signaling protocol under a one-period-lagged oversight response (made precise before Proposition 4). Throughout, the relevant regime is q ≤ q∗ with true type 

ω = ωH (the harmful case the human’s prior does not yet support). We note first that shutdown is per-round, not absorbing, so there is no “cost of shutdown”; the information value of observations is a property of this regime, in which the team plays and accrues information as a byproduct. 

Assumption 2 (Informative proposals at the realized context) . Fix the played proposal aσ at context s. For the true type ωH and the competing type ωL:

(a) (common support) OωH (o | s, a σ) > 0 ⇐⇒ OωL (o | s, a σ) > 0 for all o ∈ O ; and 

(b) (positive information) DKL 

 OωH (· | s, a σ) ∥ OωL (· | s, a σ) =: η > 0.

We state Assumption 2 at the actually played ( s, a σ) because passive learning only observes the realized proposal sequence; it is not enough that some hypothetical ( s, a ) distinguishes the types. Part (a) is what keeps the log-likelihood-ratio increments finite. In Example 1 common support fails: OωL puts zero mass on the toppling/dropped outcomes while OωH does not. Under ωH , an outcome assigned zero probability under ωL occurs with probability 0 .9 (= 0 .70 + 0 .20) in each played round, so ωH is identified in finite geometric time almost surely; the finite-η Wald bounds of Proposition 3 are not applied to this degenerate example. Assumption 2 covers the generic finite-η

case to which those bounds do apply. 

Passive learning. In the example, the grasp executes, the dashboard shows a toppled stack, and the operator updates q upward without needing to understand shelf mechanics, she learns “high-speed retrievals from this robot tend to drop loads.” Under Assumption 2, each play round at ( s, a σ) generates strictly positive expected information about ω, since η > 0. To index the belief by information received, let qn denote the posterior after n played observations, with q0 the initial prior; each played round at the fixed ( s, a σ) contributes one observation, so qn advances by one i.i.d. likelihood-ratio increment per played round. We consider two play disciplines. To establish a.s. convergence of the belief we let the AI play the fixed proposal indefinitely ; to bound the time spent in the failure region we let it play the proposal for observations 1 , . . . , τ ∗, which is the discipline consistent with equilibrium behavior under the myopic rule at q ≤ q∗ (Proposition 2), after which the myopic policy generally changes. Since the myopic human oversees only at q > q ∗

(Proposition 2) and trusts at q = q∗ by the tie-breaking convention, the relevant exit time is the number of played observations until strict crossing, τ ∗ := inf {n ≥ 0 : qn > q ∗}.

Proposition 3 (Passive-learning convergence) . Fix the played proposal at (s, a σ), the initial belief 

q0 < q ∗, and the true type ω = ωH , and let M+ := max o∈supp( OωH )

 log OωH (o|s,a σ )  

> OωL(o|s,a σ)

+, with qn the posterior after n played observations. Under Assumption 2: 

11 (i) If the fixed proposal is played indefinitely, then qn → 1 almost surely. 

(ii) If the fixed proposal is played for observations 1, . . . , τ ∗, then E[τ ∗] < ∞, and with L := log q∗ 

> 1−q∗

− log q0 

> 1−q0

> 0 it satisfies the two-sided bound 

Lη ≤ E[τ ∗] ≤ L + M+

η .

To leading order E[τ ∗] ≈ L/η when L is large relative to the increment law; increasing η

reduces this leading-order term. Proof. See Section A.3. 

Active signaling. The AI can use ask as a credible signal about ω, even when the human will trust. In the example, the robot sends ask before executing; under a separating policy (ask iff 

ω = ωH ), observing ask drives qt+1 = 1, moving the operator past q∗ in one round. For the value comparison we fix the context st = s across rounds, so that the proposal dis-tribution σ(s) and expected payoffs fij are stationary. Hence once q = 1 is reached, the type-θ1

proposal remains harmful in expectation and the off-switch team shuts it down each subsequent round (rejecting each fresh retrieval request), incurring −(cask + cov ) per round in perpetuity rather than a one-time cost. This is the continuation imposed by the fixed separating policy evaluated below; it is not claimed to be dynamically optimal. Conditional on θ1, repeated ask-and-shutdown is optimal under Assumption 1. Conditional on θ0, continuing to ask is deliberately suboptimal, paying cask each round for a proposal already known to be safe, and is retained only because πAI 

> sep

is defined as a fixed stationary policy. 

Lagged myopic response. Throughout this subsection the human follows a one-period-lagged myopic oversight rule. At round t her trust/oversee meta-action is selected using the pre-action belief qt and does not condition on the simultaneously selected AI meta-action; the publicly observed ask is incorporated into the posterior only for subsequent rounds, producing qt+1 = 1 under the separating policy. The ask is thus credible for future belief updating, but the human’s current-round oversight response is constrained to the pre-action belief. This is what distinguishes the present analysis from the team-optimal credible protocol of Section 4.2, in which the human conditions on the ask within the same round; under a full same-round Bayesian response she would oversee immediately and the one-period delay below would vanish. 

Assumption 3 (Harm dominates the ask cost) . cask < γ  |f1H | − cov 

.

This is exactly the condition for b∗∗ ∈ (0 , 1) below; it says one round of discounted avoided harm, net of oversight cost, exceeds the ask cost. 

Proposition 4 (Value of a fixed one-period-delayed separating-ask policy relative to perpetual play) . Under Assumptions 1 and 3, the off-switch operator, the lagged myopic response rule above, 

ω = ωH , q ≤ q∗, and infinite horizon with discount γ ∈ (0 , 1) , define the separating ask policy πAI 

> sep

(ask iff ω = ωH , in every round) and the pure-play baseline πAI  

> pp

(always play, no oversight). 

(i) Under πAI 

> sep

, observing ask at round t implies qt+1 = 1 , and a type-θ1 human then oversees whenever asked, regardless of b. (The threshold b > b ∗ is the team-optimal one-shot ask condition of Corollary 1 at q = 1 ; it makes asking worthwhile ex ante for an AI that remains uncertain about whether the human is type θ1, so the team shuts down for θ1 from round t + 1 

onward.) 

12 (ii) The team-value difference satisfies Vsep − Vpp > 0 iff b > b ∗∗ , where 

b∗∗ := cask 

γ |f1H | − cov 

 = b∗

γ ∈ (0 , 1) .

(iii) b∗∗ = b∗/γ > b ∗ since γ ∈ (0 , 1) .This proposition evaluates a particular stationary signaling policy against perpetual pure play; it does not characterize the optimal active-signaling policy, nor does it compare against the passive-learning path. Proof. See Section A.4. In Proposition 4 (i), throughout the failure regime q ≤ q∗, the human trusts every round, so no override signal is generated and the AI’s belief about θ remains at its prior b = μ(θ1); only qt

evolves. Proposition 4 (iii) shows signaling threshold is strictly higher than the team-optimal one-shot threshold b∗ of Corollary 1, because the separating ask pays cask at the present round (when the myopic human still trusts) to buy the belief jump that makes all future oversight effective. 

Remark 4 (The two mechanisms compared) .

Passive learning Active signaling Rounds to exit failure regime E[τ ∗] ∈ [L/η, (L + M+)/η ] 1Initial signaling cost 0 cask 

Baseline compared against n/a perpetual pure play Required condition Assumption 2 b > b ∗∗ = b∗/γ , Assumption 3 The two results have different baselines and are not directly compared here. Proposition 4 shows the fixed separating policy is preferable to perpetual pure play under the stated threshold; comparing it against the passive-learning policy that plays until τ ∗ and then switches to oversight requires incorporating the distribution of τ ∗, and remains unresolved. What both indicate is that increasing the information rate η reduces the leading-order exit time L/η , so a more informative base policy σ tends to shorten the passive escape; we do not claim that maximizing the KL divergence η

exactly minimizes E[τ ∗] across arbitrary proposal distributions, since the overshoot term also varies with the observation law. A credible separating ask can instead shortcut the escape in one round when b > b ∗∗ .

4.4 Open problems 

1. Optimal multi-round policy. The team-optimal policy jointly deploying passive learning and active signaling as a function of ( b, q, ω ) is not characterized; whether it retains a threshold structure analogous to Corollary 1 is open. 2. Correlated beliefs. Corollary 1 and Propositions 2 and 4 assume product beliefs; the structure of the ask and signaling regions on the full simplex ∆(Θ × Ω), and how prior correlation between θ and ω reshapes the team-optimal/myopic gap, is open. 3. POMDP extension. Replacing the i.i.d. context with a Markov state and adding a transi-tion kernel Tω : S × A → ∆( S) privately known to the AI, the analogue of Proposition 1 in that setting, remains open. 13 5 Summary 

Section 1 positioned our model against CIRL, which captures preference learning but assumes one-sided uncertainty and the play/ask/trust/oversee interface absent from it, and the Oversight Game, which supplies such a deferral interface but assumes full information. Definition 1 combines the two into a contextual-bandit team game with two-sided asymmetry, where the human privately knows 

θ and the AI privately knows ω, with bilinear payoff f (θ, ω ) = ⟨Oω, R θ⟩. Removing physical state transitions is what makes the analysis tractable, but the common belief remains a dynamically controlled state, so the multi-round problem does not separate across rounds. Section 4 gives two one-shot characterizations. The team-optimal policy (Proposition 1) is an exact finite combinatorial maximization max B,C ∆( B, C ); in the binary off-switch case (Corollary 1) it asks at ωH and oversees θ1 iff b > b ∗, independently of q. The myopic non-signaling rule (Proposition 2) instead asks only on the rectangle ( b∗, 1) × (q∗, 1). The difference is the slab 

{b > b ∗, q ≤ q∗} (Remark 3): there the AI privately knows the action is harmful and shutdown would help, but a myopic human, trusting her prior q < q ∗, declines oversight, so the harm is realized. This is exactly the robot operator example ( q = 0 .30 < q ∗ ≈ 0.34, with cov = 0). The economic reading is that q∗ is not a constraint of the problem but the price of non-credible oversight communication: under the team-optimal protocol, in which the ask is a credible signal that ω = ωH ,the human’s oversight binds precisely when it matters and q drops out of the threshold. Section 4.3 gives a partial analysis of how the failure resolves over time even when the human remains myopic. For passive learning (Proposition 3): under indefinite play qn → 1 a.s., and when the proposal is played until strict threshold crossing the expected crossing time satisfies the Wald bounds L/η ≤ E[τ ∗] ≤ (L+M+)/η . For active signaling (Proposition 4), under a one-period-lagged myopic response a fixed separating ask beats perpetual pure play once b > b ∗∗ = b∗/γ , exiting the failure regime in one round with an initial signaling cost cask .We are careful about scope. The clean one-shot results hold for product beliefs and the off-switch operator (the realistic non-technical-overseer model); the team-optimal characterization for general correction sets is an exact finite but combinatorial maximization; and the multi-round section is a partial analysis of two separate mechanisms rather than a complete resolution. The optimal multi-round policy, the correlated-belief case, and the POMDP extension remain open (Section 4.4). 

# References 

[1] D. Hadfield-Menell, S. J. Russell, P. Abbeel, and A. Dragan. Cooperative inverse reinforcement learning. Advances in Neural Information Processing Systems (NeurIPS) , 29:3909–3917, 2016. [2] D. Hadfield-Menell, A. Dragan, P. Abbeel, and S. Russell. The off-switch game. International Joint Conference on Artificial Intelligence (IJCAI) , 2017. [3] W. Overman and M. Bayati. The oversight game: Learning to cooperatively balance an AI agent’s safety and autonomy. arXiv:2510.26752 , 2025 (revised 2026). 

# A Proofs 

A.1 Proof of Proposition 1 and Corollary 1 

General characterization. With simultaneous moves and the credible-ask protocol, a deter-ministic policy is ( B, C ). Decompose its value against always-play cell by cell. On {ω / ∈ B} the 14 AI plays, the human’s choice is irrelevant and costless (cost timing of Definition 1), and the payoff is fσ, no change from baseline. On {ω ∈ B, θ / ∈ C} the AI asks and the human trusts: payoff 

fσ − cask , a change of −cask . On {ω ∈ B, θ ∈ C} the AI asks and the human oversees, applying the correction e∗ 

> B

(θ) optimal at her post-ask posterior bHB : payoff fσ + DB (θ, ω ) − cask − cov , a change of DB (θ, ω ) − cask − cov . Summing the changes weighted by μ(θ, ω ) gives (3). Maximizing over the finite lattice 2 Ω × 2Θ yields a maximizer. The rule is not separable: e∗ 

> B

(hence DB and C∗) depends on B, and the optimal B depends on C. ■

Binary off-switch (Corollary 1). The only proposal cell with fσ < 0 is ( θ1, ω H ) (by Assump-tion 1, f1L > 0 and f0j ≥ 0). We claim the optimum is B∗ = {ωH }, C ∗ = {θ1} when b > b ∗.First, with B = {ωH } the ask reveals ωH , so the post-ask posterior bHB (· | θ1) is the point mass at ωH ; then EbHB [fσ(θ1, ·)] = f1H < 0, so e∗ 

> B

(θ1) = off and DB (θ1, ω H ) = −f1H = |f1H |. For θ0 the posterior gives f0H ≥ 0, so e∗ 

> B

(θ0) = aσ and DB (θ0, ·) = 0; including θ0 in C only adds the oversight cost −cov μ(θ0, ω H ) ≤ 0, so we may take C∗ = {θ1}. Next we show adding ωL to B never helps. With B = {ωL, ω H } the post-ask posterior reverts to the prior over ω, and θ1’s correction is a single action applied on both cells (the human cannot condition on ω). Two cases: (a) if e∗ 

> B

(θ1) = aσ,then DB = 0 on both cells but the ask cost is now paid on ωL as well, strictly lowering ∆; (b) if 

e∗ 

> B

(θ1) = off, then on the ωL cell DB (θ1, ω L) = −f1L < 0 (shutting down a good proposal), plus the extra ask and oversight costs, again lowering ∆, here by exactly (1 − q)[ b(f1L + cov ) + cask ]. In either case, adding ωL weakly lowers the gain and, under cask > 0, strictly lowers it on the ωL event; hence a canonical optimum never asks at ωL, i.e. B∗ = {ωH }. The remaining singleton B = {ωL}

is also dominated: every relevant proposal payoff is then nonnegative ( f1L > 0, f0j ≥ 0), so the optimal correction either leaves the proposal unchanged or shuts down a nonnegative-payoff action, while the positive ask cost is still incurred on ωL; hence ∆( {ωL}, C ) ≤ 0 for every C, no better than ∆( ∅, ·) = 0. Evaluating the surviving candidate against always-play (product beliefs): ∆( {ωH }, {θ1}) = μ(θ1, ω H ) ( |f1H | − cov ) − cask μΩ(ωH ) = qb(|f1H | − cov ) − cask 

,

while ∆( ∅, ·) = 0. Hence the team asks iff b(|f1H | − cov ) > c ask , i.e. b > b ∗; at b = b∗ the gain is 0 and both policies are optimal. The factor q ≥ 0 multiplies the entire bracket, so the sign, the ask decision, is independent of q. ■

A.2 Proof of Proposition 2 

Policy form. The myopic human fixes δH (θ) = oversee iff θ ∈ Θ− using her prior conditional; for 

θ ∈ Θ− her committed correction is shutdown, applied whenever she oversees (she cannot condition on ω). Holding this fixed, asking at ω changes the payoff, on each θ ∈ Θ−, by −fσ(θ, ω ) (shutdown gain) −cov , and pays cask for all θ; this is Ψ( ω). 

Part (i). Under off-switch and product beliefs, ¯fH (θi) = (1 − q)fiL + qf iH . For θ0: f0L, f 0H ≥ 0so ¯fH (θ0) ≥ 0 > −cov ; θ0 /∈ Θ− always. For θ1: ¯fH (θ1) decreases strictly from f1L > 0 (at q = 0) to f1H < −cov (at q = 1), crossing −cov at q∗ = ( f1L + cov )/(f1L − f1H ) ∈ (0 , 1); hence θ1 ∈ Θ− iff 

q > q ∗.

Part (ii). When q > q ∗, Θ − = {θ1}. The shutdown is applied at both ω (prior commitment). For ωL: Ψ( ωL) = b(−f1L) − cask − bc ov < 0 since f1L > 0 (shutting down at ωL destroys value). For ωH : Ψ( ωH ) = b(−f1H ) − cask − bc ov = b(|f1H | − cov ) − cask > 0 iff b > b ∗. When q ≤ q∗, Θ − = ∅

and Ψ ≡ − cask < 0, so the AI never asks. 15 Part (iii). The AI asks iff ω = ωH , b > b ∗ and q > q ∗ (the last because for q ≤ q∗ the human would not oversee and Ψ( ωH ) = −cask < 0). This is the rectangle ( b∗, 1) × (q∗, 1). ■

A.3 Proof of Proposition 3 

By Assumption 2(a) (common support at the played ( s, a σ)), the log-likelihood-ratio increments 

Xi := log  OωH (oi | s, a σ)/O ωL (oi | s, a σ) are finite for every observable oi, hence bounded on the finite O, and i.i.d. under the true type ωH (the proposal is the fixed ( s, a σ) each round). Their mean is E[Xi] = DKL (OωH (· | s, a σ)∥OωL (· | s, a σ)) = η > 0 by Assumption 2(b). For part (i), suppose the fixed proposal is played indefinitely, so every played round contributes an i.i.d. increment. The log-odds process Λ n = Λ 0 + Sn with Sn = Pni=1 Xi and Λ 0 = log q0 

> 1−q0

is a random walk with positive drift η, so by the strong law Sn/n → η a.s., giving Λ n → ∞ and qn → 1a.s. For part (ii), suppose the fixed proposal is played for observations 1 , . . . , τ ∗, so the increments up to τ ∗ are i.i.d. as above. Write λ∗ := log q∗ 

> 1−q∗

for the log-odds threshold and L := λ∗ − Λ0 > 0for the log-odds distance (positive since q0 < q ∗). Because the myopic human trusts at q = q∗ by the tie-breaking convention, the regime exits only on strict crossing, so the relevant stopping time, counted in played observations, is τ ∗ = inf {n : Λ n > λ ∗} = inf {n : Sn > L }.

Integrability. The increments Xi are bounded with positive mean, so there exists λ > 0 with 

E[e−λX 1 ] < 1. By a Chernoff bound on the lower tail of the walk, Pr( τ ∗ > n ) ≤ Pr( Sn ≤ L) ≤ eλL  E[e−λX 1 ]n,

which decays geometrically in n; hence τ ∗ has a finite expectation (indeed all moments). 

Wald bound. The stopped sum satisfies Sτ ∗ = L + ζ, where the overshoot satisfies 0 < ζ ≤ M+

(strictly positive because the crossing is strict and the final increment carrying the partial sum across 

L is positive), with M+ = max o∈supp( OωH )

 log OωH (o|s,a σ )  

> OωL(o|s,a σ)

+. Wald’s identity gives E[Sτ ∗ ] = η E[τ ∗], so η E[τ ∗] = L + E[ζ] with 0 < E[ζ] ≤ M+, i.e. 

Lη ≤ E[τ ∗] ≤ L + M+

η .

The approximation E[τ ∗] ≈ L/η is the asymptotic statement obtained as L grows large while the increment law (hence M+) stays fixed; we do not claim exact monotonicity in η, since the overshoot depends on the full increment law and not on η alone. ■

A.4 Proof of Proposition 4 

Part (i). Under πAI 

> sep

, the likelihood of ask given ωL is 0, so by Bayes’ rule observing ask gives 

qt+1 = 1 > q ∗. From round t + 1 on the world is known to be ωH . At q = 1 a type-θ1 human strictly prefers shutdown to the proposal ( E[fσ(θ1, ·)] = f1H < 0), so she oversees whenever asked, independently of b. Conditional on the actual type being θ1, ask-and-shutdown is itself worthwhile irrespective of b, since Assumption 1 gives |f1H | > c ask + cov . The role of b > b ∗ is to make asking worthwhile ex ante for an AI that remains uncertain about the human type: b > b ∗ is precisely the team-optimal one-shot ask threshold of Corollary 1, which at q = 1 prescribes B∗ = {ωH }, C ∗ =

{θ1}, so if the AI’s continuation is to keep asking whenever asking is one-shot team-improving in expectation, then b > b ∗ is exactly the condition under which it keeps asking each round, and the team shuts down for θ1 and trusts for θ0 (since DB (θ0, ·) = 0: at the revealed ωH , f0H ≥ 0, so the optimal correction keeps aσ). Note that under the fixed policy πAI  

> sep

the AI in fact keeps asking on 

θ0 as well; this is the deliberately suboptimal feature of the fixed policy, and the value computation in part (ii) accounts for it. 16 Part (ii). Under ω = ωH , q ≤ q∗, the human trusts at round t (θ1 /∈ Θ−). We compute per-type values, using the stationary continuation: once q = 1 (from round t + 1 on), the type-θ1 proposal is shut down every round at cost cask + cov , and the type-θ0 proposal is asked and trusted every round (since θ0 /∈ Θ− even at q = 1), yielding f0H − cask per round. 

V θ1 

> sep

= f1H − cask 

| {z } 

> round t: ask, trust

+ γ (−cask − cov )1 − γ , V θ0 

> sep

= f0H − cask 

1 − γ , V θi 

> pp

= fiH 

1 − γ .

The b-weighted difference is, after simplification, 

Vsep − Vpp = b



V θ1 

> sep

− V θ1

> pp



+ (1 − b)



V θ0 

> sep

− V θ0

> pp



= b γ (|f1H | − cov ) − cask 

1 − γ .

(The θ0 term contributes −(1 −b)cask /(1 −γ) and the θ1 term contributes b[γ(|f1H |− cov )−cask ]/(1 −

γ); the −cask pieces combine.) Since 1 − γ > 0, Vsep − Vpp > 0 iff b γ (|f1H | − cov ) > c ask , i.e. iff 

b > b ∗∗ = cask /[γ(|f1H | − cov )]. Assumption 3 gives cask < γ (|f1H | − cov ), hence b∗∗ ∈ (0 , 1). 

Part (iii). Directly, b∗∗ = cask /[γ(|f1H | − cov )] = b∗/γ , and γ ∈ (0 , 1) gives b∗∗ > b ∗. ■

17