---
title: Robust Receding Horizon Games with Additive Uncertainty
title_zh: 具有加性不确定性的鲁棒滚动时域博弈
authors: "Dinesh Patra, Tanish Jain, Ashish R. Hota"
date: 2026-07-05
pdf: "https://arxiv.org/pdf/2607.04213v1"
tags: ["query:marl"]
score: 8.0
evidence: 多智能体、滚动时域博弈、纳什均衡
tldr: 针对存在加性扰动的多智能体博弈控制问题，每个智能体拥有私有约束和共同耦合约束。提出一种结合管道约束收紧与有限时域广义纳什均衡的鲁棒滚动时域控制框架，并采用基于离散代数Riccati方程的终端代价和正不变终端集。该框架保证对任意有界扰动的递归可行性，且名义状态渐近收敛到稳态变分广义纳什均衡，实际状态收敛到其最小鲁棒正不变集邻域。
source: arxiv
selection_source: fresh_fetch
motivation: 现有滚动时域博弈未充分考虑加性扰动，导致递归可行性难以保证且收敛性未知，亟需鲁棒控制框架。
method: 融合管道约束收紧与有限时域GNEP，设计DARE终端代价和解耦正不变终端集，利用潜在博弈结构分析收敛性。
result: 递归可行性始终满足；名义状态渐近收敛到稳态vGNE，实际状态收敛到其最小鲁棒正不变集邻域。
conclusion: 所提框架有效解决了扰动下多智能体博弈的鲁棒控制与收敛分析问题，为实际应用提供理论保障。
---

## 摘要
我们研究了一个滚动时域博弈问题，其中多个智能体在加性扰动、私有状态和输入约束以及共享耦合约束下驱动线性系统。我们提出了一个鲁棒的博弈论控制框架，该框架将基于管道的约束收紧与有限时域广义纳什均衡问题（GNEP）相结合，并配备了基于离散代数Riccati方程（DARE）的终端代价和解耦的正不变终端集。该框架保证了每个有界扰动实现的递归可行性。利用由跟踪代价诱导的势博弈结构，我们进一步建立了每个智能体名义状态向稳态变分广义纳什均衡（vGNE）的渐近收敛性，并表明每个智能体的实际状态收敛到由最小鲁棒正不变集确定的vGNE邻域。

## Abstract
We study a receding horizon game in which multiple agents drive linear systems subject to additive disturbances, private state and input constraints, and shared coupling constraints. We propose a robust game-theoretic control framework that combines tube-based constraint tightening with a finite-horizon generalized Nash equilibrium problem (GNEP), equipped with a discrete algebraic Riccati equation (DARE)-based terminal cost and a decoupled positively invariant terminal set. The framework guarantees recursive feasibility for every bounded disturbance realization. Exploiting the potential-game structure induced by tracking costs, we further establish asymptotic convergence of each agent's nominal state to a steady-state variational generalized Nash equilibrium (vGNE), and show that each agent's actual state converges to a neighborhood of the vGNE determined by the minimal robust positively invariant set.

---

## 论文详细总结（自动生成）

好的，作为资深学术论文分析助手，我将根据您提供的论文内容，为您生成详细、结构化的中文总结。

### **论文详细总结：具有加性不确定性的鲁棒滚动时域博弈**

#### 1. 论文的核心问题与整体含义 (研究动机和背景)

*   **核心问题**：现有针对多智能体系统的滚动时域博弈（Receding Horizon Game, RHG）框架，普遍假设系统为确定性线性动态，即**未考虑任何扰动或不确定性**。然而，实际系统（如智能电网、自动驾驶车队）中普遍存在模型失配、外部干扰等不确定因素。这使得现有框架在不确定环境下无法保证约束满足（递归可行性）和稳定性，从而限制了其在实际中的应用。
*   **研究动机**：论文旨在填补理论空白，首次系统性地研究在**存在有界加性扰动**时，多智能体进行非合作博弈（每个智能体优化自身成本）的鲁棒滚动时域控制问题。核心挑战在于：1) 如何在扰动下保证所有智能体都满足其私有和共享约束；2) 如何在非合作（隐私保护）的前提下，通过滚动时域方式实现系统收敛到一个博弈均衡点（稳态）。
*   **整体含义**：这项工作将经典的鲁棒模型预测控制（Robust MPC）思想，特别是**基于管道的约束收紧方法**，成功应用于非合作的滚动时域博弈中。它为在不确定性环境中运行的分布式、竞争性多智能体系统（如机器人、能源管理）提供了首个具备严格递归可行性和收敛性保证的理论框架。

#### 2. 论文提出的方法论

*   **核心思想**：采用“名义-误差”分解（即管道控制）的思想来处理不确定性。每个智能体规划一个**名义轨迹**（无扰动），扰动的影响被限制在一个“管道”（鲁棒正不变集）内。通过离线收紧所有约束，保证名义轨迹可行就等价于实际轨迹鲁棒可行。同时，利用构建的**势博弈**（Potential Game）结构，将系统收敛到稳态变分广义纳什均衡（vGNE）。
*   **关键技术细节**：
    1.  **模型与分解**：将实际状态/输入分解为名义部分（决策变量）和误差部分：\( x_k = s_k + e_k \), \( u_k = v_k + K_p e_k \)。误差动态为 \( e_{k+1} = \Phi_p e_k + w_k \)，其中 \(K_p\) 是确保 \(\Phi_p\) 舒尔稳定的预稳定反馈增益，\(w_k\) 是扰动。
    2.  **约束收紧（Tube-based Tightening）**：
        *   私有约束：通过计算最小鲁棒正不变集（mRPI, \(Z^\infty_i\)），将原始私有约束 \(X_i, U_i\) 收紧为 \( S_i = X_i \ominus Z^\infty_i \)，\( V_i = U_i \ominus K_p Z^\infty_i \)。
        *   共享约束：计算每个邻居 j 的误差对共享约束的最大最坏情况贡献向量 \(d_j\)（通过支撑函数），并广播该标量向量。最终，全局共享约束收紧为：\( \sum_j (F_j s_k^j + G_j v_k^j) \le b_i - \sum_j d_j \)。
    3.  **终端成分设计**：
        *   **终端代价**：基于DARE解得最优反馈增益 \(K_f\) 和矩阵 \(P\)，定义终端代价 \(l_f(s) = ||s - s^*||_P^2\)。
        *   **终端约束（解耦）**：通过一个资源分配方案，将收紧后的共享约束上限（\(\bar{b}_i\)）分解为每个智能体独立的份额（share）\( \alpha_j \)。每个智能体只需满足自己的终端约束 \( F_j s + G_j \kappa(s) \le \alpha_j \)，从而解耦了耦合的终端约束。最终构建一个最大正不变集作为终端集。
        *   **终端控制律**：\( \kappa(s) = v_i^* + K_f(s - s_i^*) \)。
    4.  **滚动时域博弈**：在每个时刻，各智能体并行求解一个有限时域GNEP，优化其名义轨迹，使其满足收紧后的动力学和约束、终端约束、以及一个“管道约束”（初始名义状态与实际测量状态之差必须在mRPI集内）。求解后的第一个名义控制与反馈控制一起应用于系统。
*   **算法流程**：
    *   **离线阶段**：每个智能体计算自己的mRPI集、收紧私有/共享约束、协商并获得稳态vGNE、基于DARE计算终端代价和终端集（需交换少量标量信息）。
    *   **在线阶段**：测量当前状态，分布式求解有限时域GNEP，应用组合控制律（名义+反馈），推进到下一时刻。

#### 3. 实验设计

*   **论文性质**：这是一篇**纯粹的理论性论文**，没有设计任何数值仿真或实验。论文的核心贡献在于提出了一个理论框架并对其性质进行了严格的数学证明。
*   **实验缺失**：文中**未提及任何数据集、benchmark或对比方法**。所有结论均基于数学定理和证明，没有通过案例研究进行验证。

#### 4. 资源与算力

*   **未明确说明**。作为理论性论文，文中并未涉及任何计算资源、GPU型号、训练时长或硬件配置。但其提出的在线求解GNEP的分布式算法（如[24]、[29]）隐含了计算需求。

#### 5. 实验数量与充分性

*   **实验数量**：0组。
*   **充分性分析**：从理论论文的角度来看，其“实验”是**数学证明**。论文证明了两个关键定理（递归可行性和稳定性），这些证明逻辑链条完整，能够自洽地支撑其理论主张。因此，**在理论层面是充分的**。但是，缺乏数值或真实世界的案例验证，使其在**应用层面**（如验证理论假设在具体场景中的有效性、评估计算效率、展示其与现有方法的性能差异）的充分性**不足**。实验的客观性无从评估。

#### 6. 论文的主要结论与发现

1.  **递归可行性**：提出的框架能够保证，对所有有界扰动实现，如果初始时刻可行，则后续所有时刻的优化问题都是可行的。这是一个关键的鲁棒性保证。
2.  **名义状态收敛**：每个智能体的名义状态将渐近收敛到稳态vGNE（\(s_i^*, v_i^*\)）。论文利用势博弈结构，将系统总成本作为李雅普诺夫函数，证明了其沿解轨迹严格递减，从而证明了收敛性。
3.  **实际状态收敛**：每个智能体的实际状态将收敛到稳态vGNE（\(s_i^*\)）的一个邻域内，该邻域由最小鲁棒正不变集（\(Z^\infty_i\)）决定。这说明了系统最终会收敛到均衡点附近一个紧缩的“管道”内。

#### 7. 优点

*   **理论首创性**：这是首个系统性地解决**带扰动的滚动时域博弈**问题的理论框架，填补了该领域的一个重要空白。
*   **保证严格**：方法提供了所有有界扰动下的递归可行性和渐近稳定性证明，这比经验式方法或仅保证名义稳定性的方法更强大、更可信。
*   **隐私保护**：与传统的分布式MPC不同，该方法中智能体之间的信息交换极少，仅需广播一个**标量向量**（最坏情况贡献），无需暴露各自的动态模型、成本函数或预测轨迹，具有很强的隐私保护特性。
*   **终端约束解耦**：通过巧妙的资源分配方案，成功地将耦合的终端约束解耦为每个智能体的独立约束，使得终端不变集可以在本地计算，极大地简化了设计。

#### 8. 不足与局限

*   **应用范围限制**：
    1.  **线性模型**：方法仅适用于线性系统动态。对于非线性、混合或其他复杂动力学，该方法不适用。
    2.  **加性扰动**：假设不确定性是加性的。对于乘性不确定性、参数变化或结构不确定性，该框架需要修改。
    3.  **成本结构限制**：稳定性证明依赖于势博弈结构，这要求所有智能体的成本函数都是跟踪形式的、并与其他智能体解耦。对于更一般的耦合成本或经济成本，其方法不适用。
*   **保守性**：基于刚性管道的约束收紧方法，本质上是处理最坏情况，可能导致可行域非常保守，尤其在扰动较大的情况下，使其无法在更接近约束边界处运行。
*   **计算复杂度**：虽然在线求解GNEP是分布式的，但每一步都需要收敛到其vGNE。对于大规模系统或高动态环境，这个分布式求解过程的计算和通信开销可能非常大，其在线实时性值得关注。
*   **实验验证缺失**：如第3/5点所述，缺乏任何实验或案例研究来验证其在具体问题中的表现、效率和与现有方法的性能对比，这是该工作的一个明显短板。

（完）
