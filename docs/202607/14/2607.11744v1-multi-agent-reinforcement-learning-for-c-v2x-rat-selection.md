---
title: Multi-Agent Reinforcement Learning for C-V2X RAT Selection
title_zh: 面向C-V2X RAT选择的多智能体强化学习
authors: "Moritz Schaffenroth, Uwe Kölbel, Heike Lepke, Alexander Prinz, Alfred Höß"
date: 2026-07-13
pdf: "https://arxiv.org/pdf/2607.11744v1"
tags: ["query:marl"]
score: 9.0
evidence: 应用于V2X的多智能体强化学习
tldr: 随着V2X应用的多样化，不同业务对时延和可靠性提出异构需求，而现有的Uu、PC5及双链路各有优劣。本文提出基于多智能体强化学习MAPPO的RAT选择算法，在单个车辆控制场景中，按时交付率从0.508提升至0.535；在多车辆协同场景下，从0.548提升至0.567，同时训练时间减半。该方法的增益主要来源于高级应用场景，表明自适应通信策略对未来V2X通信具有重要价值。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.11744v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 850, \"height\": 545, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.11744v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1415, \"height\": 646, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.11744v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1864, \"height\": 686, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.11744v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 918, \"height\": 551, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.11744v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 936, \"height\": 609, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.11744v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 931, \"height\": 327, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.11744v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 932, \"height\": 371, \"label\": \"Table\"}]"
motivation: 解决异构V2X应用中多种通信技术的实时择优问题，满足高级应用的差异化需求。
method: 采用多智能体强化学习算法MAPPO，为每个车辆智能体联合决策选择Uu、PC5或同时使用双链路。
result: 相比DRL基线，按时交付率在单车场景从0.508提升至0.535，多车场景从0.548提升至0.567，训练时间减半。
conclusion: 多智能体建模能够有效提升复杂V2X通信场景下的自适应决策性能，尤其适用于未来高级应用。
---

## 摘要
车辆越来越多地配备先进的V2X通信能力。早期的V2X应用利用诸如协作感知消息等服务，而最近的发展则允许更高级的应用，包括协作驾驶、共享感知和传感器共享服务。更广泛的应用混合导致了对延迟和可靠性的异构需求。同时，多种V2X通信技术各有优缺点。混合V2X通信可以在适当的时候利用各自的优势来满足应用需求。本文研究了蜂窝Uu链路、NR-V2X PC5侧链路以及同时使用两个信道之间的决策问题。我们通过使用多智能体强化学习算法MAPPO来解决这个问题，并将其与五种基线方法进行比较，包括深度强化学习(DRL)方法、静态决策树方法和静态信道选择策略。这些方法在城市场景和一组选定的通信用例中进行评估。评估结果表明，与DRL方法相比，在单控制车辆设置中，按时交付率从0.508提高到0.535；当所有车辆遵循学习策略时，按时交付率从0.548提高到0.567，并且训练时间减少了一半。这些增益主要来自高级应用场景，而非仅涉及CAM消息的场景。这表明未来的应用将受益于这种自适应通信策略，并且多智能体建模对于解决底层决策问题是有用的。

## Abstract
Vehicles are increasingly equipped with advanced V2X communication capabilities. While early V2X apps utilized services such as Cooperative Awareness Messages, recent developments have allowed more advanced applications including cooperative driving, shared perception, and sensor-sharing services. The broader mix of applications leads to heterogeneous requirements for latency and reliability. At the same time multiple communication technologies for V2X are available with pros and cons. Hybrid V2X communication can exploit the distinct advantages at the right moment to fulfill the requirements of the applications. This work studies the decision problem between cellular Uu link, NR-V2X PC5 sidelink, and the simultaneous use of both channels. We address this problem by using the multi-agent reinforcement learning algorithm MAPPO and compare it to five baselines consisting of a deep reinforcement learning (DRL) approach, a static decision tree approach and static channel selection strategies. The methods are evaluated in an urban scenario and with a set of selected communication use cases. The evaluation results show that when compared to the DRL approach, the on-time delivery ratio improves from 0.508 to 0.535 in a single-controlled-vehicle setting and from 0.548 to 0.567 when all vehicles follow the learned policy and reduces the training time by half. The gains result mainly from the advanced applications scenarios, as opposed to scenarios involving exclusively CAM messaging. This indicates future applications will benefit from such adaptive communication strategies and that multi-agent modelling is useful for addressing the underlying decision problem.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究背景**：车辆V2X通信能力不断增强，从早期的协作感知消息（CAM）发展到协作驾驶、共享感知、传感器共享等高级应用。这些应用对时延和可靠性提出了异构需求。同时，存在多种V2X通信技术：蜂窝Uu链路（5G）提供高带宽、广覆盖、远程连接；NR-V2X PC5侧链路提供超低时延（约1毫秒）的直接通信。单一技术难以同时满足所有需求。
- **核心问题**：如何在Uu、PC5以及同时使用两者（混合模式）之间做出实时决策，以最大化通信性能？这是一个多智能体环境（多个车辆竞争有限信道资源），需要自适应选择。
- **整体含义**：提出基于多智能体强化学习（MARL）的方案，有望提升未来异构V2X应用场景下的通信效率，特别是支持高级应用。

## 2. 论文提出的方法论

- **核心思想**：采用**集中式训练、分散式执行（CTDE）** 的多智能体强化学习框架，使用**MAPPO（Multi-Agent Proximal Policy Optimization）** 算法。每个车辆作为一个智能体，基于本地观测独立决策选择RAT，训练时共享全局信息以促进协调。
- **关键技术细节**：
  - **观测空间**：10维向量，包括Uu和PC5的信道忙碌比、Uu SINR、自车速度，以及6个应用层参数（优先级、时延目标、消息大小、周期性标志、间隔、通信范围）。
  - **动作空间**：3个离散动作——只使用Uu、只使用PC5、同时使用Uu和PC5。
  - **奖励函数**：复合奖励，结合局部和全局目标：
    ```
    r_i(t) = (1-λ) * [ ∑_{m∈R_i} (w_m * s_m / n_m) - γ_dup * h_i(t) ] + λ * d(t)
    ```
    其中：`R_i`为智能体i在时间t内结果已确定的消息集合；`w_m`为消息优先级；`s_m`为在时延内成功接收消息的接收者数；`n_m`为通信范围内的接收者总数；`h_i(t)`为智能体i造成的混合重复接收次数；`γ_dup`为重复惩罚系数；`d(t)`为全局按时交付率；`λ=0.3`为局部-全局权重。
  - **训练范式**：共享actor网络，所有车辆面对相同决策问题，执行时按顺序逐个消息决策。使用ns3-ai接口连接ns-3仿真和Python RLlib。
- **算法流程（文字说明）**：
  1. 在ns-3/SUMO数字孪生环境中运行仿真，每辆车每生成一条消息即触发一个决策步骤。
  2. 智能体观测本地信道状态和应用参数，输出动作（选择RAT）。
  3. 仿真执行该动作，计算交付结果，更新环境。
  4. 根据奖励函数计算即时奖励，收集经验。
  5. 使用MAPPO（共享critic访问全局状态）进行集中式训练，更新actor和critic网络。

## 3. 实验设计

- **使用的场景/数据集**：
  - **地图**：两个不同的城市道路网络（Urban Map A和Urban Map B），来自同一地区。
  - **流量生成**：随机车辆路线和出发时间，插入周期2.5~10秒；使用两种交通密度因子。
  - **应用混合（App-mix）**：四种配置——仅CAM消息；混合应用（Mixed）；偏向Uu的应用（Mostly-Uu）；偏向PC5的应用（Mostly-PC5）。每个混合包含9种选定的V2X用例（来自5GAA标准），非自适应车辆默认信道不同。
  - **仿真参数**：Uu在3.5 GHz、80 MHz带宽；PC5在5.89 GHz、20 MHz带宽，NR-V2X Mode 2，固定MCS=10。
- **对比方法（baselines）**：
  - **静态Uu-only**：始终选Uu。
  - **静态PC5-only**：始终选PC5。
  - **静态Hybrid**：始终同时使用两者。
  - **决策树**：基于Khalid et al. [2]的规则，将DSRC映射为PC5。
  - **集中式DRL（DDQN）**：基于Yacheur et al. [3]的Double Deep Q-Network，使用相同观测空间但不同奖励函数（基于接收确认、性能满意度、链路质量）。
  - 本文方法：**MAPPO**。
- **训练设置**：
  - DDQN：60轮迭代，每集最多200步，共约6.7×10^5环境步。
  - MAPPO：60轮迭代，每集100步，共约4.8×10^5环境步。
  - 每个方法训练10个不同随机种子的模型。
- **评估配置**：4种App-mix × 2种地图 × 2种起始时间 = 16种评估配置。每个配置重复10次（相同随机种子）。评估分两种控制级别：**单控制车辆**（仅一个智能体，其余非自适应）和**全控制车辆**（所有车辆均使用同一策略）。

## 4. 资源与算力

- **明确说明**：论文提到DDQN训练平均耗时约**2.2天**，MAPPO平均约**17.6小时**（即训练时间减半）。但未说明具体GPU型号、数量或其他硬件配置。只提到在“local setup”上训练。

## 5. 实验数量与充分性

- **实验数量**：
  - 训练阶段：每个RL方法10个随机种子，共20个模型。
  - 评估阶段：16种评估配置 × 10次重复 × 2种控制级别 = 320组实验。此外还做了对不同λ值的消融实验（图4）和根据App-mix分解的胜出计数（表III）。
- **充分性与公平性**：
  - **优点**：对比了多种静态、规则、DRL基线；考虑了两种控制设置（单车/多车），更能体现多智能体特性；统计显著性检验（配对t检验, n=320, p=0.0126）支持MAPPO优于DDQN。
  - **不足**：
    - 仅使用2张地图，且来自同一地区，无法检验泛化能力。
    - DDQN和MAPPO使用不同奖励函数（分别基于Yacheur et al.和本文自定义），未进行奖励函数的消融研究。
    - DDQN超参数来自原始论文，MAPPO使用RLlib默认超参数，可能不是最优。
    - 训练步数不相等（DDQN更多），但MAPPO更短时间，公平性存疑。
    - 仅评估了2种起始时间，未探索不同密度、不同随机种子组合的全面性。

## 6. 论文的主要结论与发现

- **主要结论**：
  - MAPPO在按时交付率上显著优于DDQN：单控制车辆下，均值从0.508提升至0.535；全控制车辆下从0.548提升至0.567；且训练时间减半。
  - MAPPO在高级应用场景（Mixed、Mostly-Uu、Mostly-PC5）中优势明显，但在纯CAM场景下胜出的是静态方法（Uu-only或PC5-only）。表明简单方法在基础场景中已足够，高级应用才需要自适应策略。
  - 多智能体建模（CTDE）对解决车辆间资源竞争问题有效。
  - 奖励函数中λ=0.3时性能最佳（图4）。

## 7. 优点

- 方法创新：将MARL引入C-V2X RAT选择问题，考虑多车辆竞争与协调，比单智能体DRL更符合实际。
- 实验设计较为全面：对比多种基线（静态、规则、DRL）；评估两种控制级别；使用多个应用混合和地图；进行统计显著性检验。
- 训练效率高：MAPPO在更少的环境步和更短训练时间内达到更好性能。
- 奖励函数设计兼顾局部QoS满足和全局效率（惩罚混合重复、结合全局交付率）。
- 利用CTDE范式，共享策略，提高样本效率。

## 8. 不足与局限

- **泛化能力有限**：仅使用同一地区的两张地图，未在高速、乡村等不同场景验证。
- **对比公平性存疑**：DDQN与MAPPO使用不同奖励函数，未做消融；训练步数不等（DDQN更多）但MAPPO耗时更短，可能受硬件或实现影响。
- **超参数未充分调优**：MAPPO使用RLlib默认PPO超参数，DDQN使用Yacheur et al.原始超参数，可能未针对本次仿真环境最优。
- **缺乏真实环境验证**：仅在仿真中评估，未在真实测试床中验证。
- **未考虑非自适应和自适应混合的交通**：现实场景中可能同时存在，文中提到未来工作。
- **部分指标未充分分析**：如未对延迟、重复率的差异进行统计检验；未分析不同交通密度下的表现。
- **省略细节**：未说明MAPPO和DDQN的网络架构（层数、节点数）、学习率等关键参数；未公开代码或模型。

（完）
